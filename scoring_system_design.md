# A股主板评分系统设计分析

> 状态：设计讨论（未实施）
> 创建：2026-05-16
> 关联：现有 `core/screening/brick_pattern/scoring.py` (V4)，砖形图定式评分；`backtest.py` 历史回测；`stats_page` 数据采集
> 用语：所有"白线/黄线/B1/S1/N型/30+30"等术语，含义见 [trading_strategies.md](trading_strategies.md) / [trading_rules.md](trading_rules.md)

---

## 〇、目标与范围

### 要做什么
对全 A 股主板（剔除 ST、新股、北交所、科创板）每日筛选，给每只股票一个**综合评分**，按分数排序输出 TopK 候选名单，供用户做超短线/波段决策。

### 不要做什么
- 不替代用户的看图判断（评分只是粗筛 + 排序）
- 不做仓位/择时（用户战法体系已经覆盖）
- 不做高频/Tick 级（数据约定就是日线）

### 现状基准
现有 `scoring.py` 已经走通「定式专属(30) + 通用质量(30) + MACD(25) + 信号强度(15) − 风险扣分」的四层结构，并且 V4 已经在「靠回测验证因子有效性 → 调权重」的工作模式下迭代了一轮。**新系统直接复用这套结构，不另起炉灶。**

---

## 一、纠结点 1：长时段统一标准会不会偏？

### 问题本质
现有评分用的是**绝对阈值**：

```python
if day_change >= 9.5:  items["信号日涨幅"] = 7
elif day_change >= 5:  items["信号日涨幅"] = 8
elif day_change >= 3:  items["信号日涨幅"] = 6
```

回测拉到 5~10 年，会同时混入：

| 时期 | 典型特征 | 对评分的影响 |
|------|---------|-------------|
| 2020 牛市末段 | 涨停频出，普涨 | "T日涨5%"很普通，给8分高估了 |
| 2022 熊市 | 万马齐喑 | "T日涨3%"已经算强势，给6分低估 |
| 2024 震荡 | 板块轮动快 | 不同板块同一阈值含义不同 |
| 2025-2026 | (当前) | — |

**直接平均的结果**：评分系统倾向于"在所有时期都还行"的中庸因子，对极端行情下真正有用的因子（比如熊市里的"地量地价"）会被冲淡。

### 三种解决方向（可叠加）

#### 方向 A：截面归一化（最重要，强烈推荐）
**做法**：把**绝对阈值**改成**当日截面排名**。

举例：
```
现状：T日涨幅 5%  →  8分（与日期无关）
新法：T日涨幅 5%，今天主板涨幅排名前 3%  →  8分
     T日涨幅 5%，今天大涨日，排名前 30%  →  4分
```

**为什么这是最重要的一步**：
- 牛市里 5% 不算什么，熊市里 5% 是涨停级别 — 截面排名自动校准
- 实施成本低：每天计算一次"主板涨幅分位"，因子查表
- 对应砖形图战法：「红柱 ≥ 绿柱 2/3」本质也是相对/统计概念（当前红绿柱比在所有候选日的位置）

**适合截面化的因子**（按现有 V4 清单）：
- 信号日涨幅、T日涨幅 → 当日涨幅分位
- 翻红力度比 → 当日所有上涨股票的力度比分位
- 短趋斜率 → 当日所有股票的斜率分位
- 涨幅质量（封板）→ 当日封板率（行情指标）

**不适合截面化的因子**（保留绝对阈值）：
- K线形态（实体/影线比） → 物理形态本身就是绝对的
- 均线排列 → 是/否问题
- 短趋 vs 多空 → 跟价格相对关系

#### 方向 B：基于 OAMV 活跃市值的行情阶段分类
**做法**：用项目已有的 **OAMV 活跃市值**（基于中证A股 930903 计算的虚拟K线，见 `core/indicators/algorithms.py:284 compute_oamv`）作为行情温度计，识别"当前处在多头还是空头阶段、上涨/下跌的节奏是快还是慢"，给每个阶段一套参数。

**为什么不用"涨停家数 / 60日新高 / 上涨家数比"这类通用市场宽度指标**：
- 砖形图战法看的是**热钱节奏的连续性**，而不是单日截面的"今天强不强"
- OAMV = 成交活跃度 × 价格，斜率方向就是热钱进/出的方向，斜率陡缓就是节奏快/慢 — 这正是砖形图想识别的"是不是处在普涨段、要不要追"
- 通用市场宽度（涨停数/上涨家数比）每天抖动剧烈，边界容易踩线翻转；OAMV 是平滑的虚拟K线，自带 SMA(AMOUNT,10,1) 和 MA5 双重平滑，趋势识别稳定

##### 实证锚点（已观察到的两段典型行情）

| 时间窗 | OAMV 形态 | 含义 |
|--------|-----------|------|
| 2025-12-19 → 2026-01-28 | 持续上涨且涨幅增速明显 | 多头区间，市场交易热情高，处于普涨阶段 |
| 2025-09-03 → 2025-12-12 | 持续下跌 | 空头区间，热钱撤离 |

参数体系应该让评分系统在这两段里表现出**完全不同的偏好**（前者放权追涨/趋势因子，后者增权抗跌/缩量因子）。

##### 阶段划分（两轴四象限）

第一轴：**方向**（多头 / 空头）— 用 OAMV 与自身均线的相对位置 + 斜率符号双重确认
第二轴：**节奏**（快速 / 平缓）— 用 OAMV 斜率的绝对值，或近 N 日斜率相对长 N 日斜率的加速度

| 阶段 | 判定（T 日及之前）| 参数偏好 |
|------|-------------------|---------|
| **快速上涨**（普涨段） | OAMV > MA20(OAMV) 且 短期斜率 > 长期斜率 > 0 | 追涨因子（信号日涨幅、翻红力度比、量比）放权；风险扣分中"追高"项放宽；30+30 法则照常 |
| **平缓上涨**（轮动段） | OAMV > MA20(OAMV) 但 短期斜率 ≈ 长期斜率，且 > 0 | 默认 V4 参数；偏向"N 型起跳"定式 + K 线形态因子（个股 alpha 重要） |
| **快速下跌**（杀跌段） | OAMV < MA20(OAMV) 且 短期斜率 < 长期斜率 < 0 | 风险扣分加严（高位放量阴线、断头铡刀）；追涨因子降权；建议直接收紧 TopK 数量或空仓 |
| **平缓下跌**（缩量探底段） | OAMV < MA20(OAMV) 但 斜率接近 0 | 增权抗跌、缩量地量、底部暴力K因子；风险扣分中"洗盘 vs 出货"区分要更精细 |

##### 实现要点

- **数据源**：已有 `load_oamv_csv()`（`core/data/io.py:182`），文件 `stock_daily_data/oamv_930903_CSI.csv`，开箱即用
- **方向判定**：`OAMV_close` vs `MA(OAMV_close, 20)`；为避免临界翻转，用"连续 3 日同向"才确认切换
- **节奏判定**：短期斜率用 `slope(OAMV_close, 5)`，长期斜率用 `slope(OAMV_close, 20)`；用 `短/长` 比值或 `短-长` 差值分箱（建议先看分布再定阈值）
- **平滑切换**：阶段切换有滞后，T 日新阶段从 T+1 开始生效，避免边界日两套参数都没匹配上

##### 注意点

- 阶段数**不能多**。两轴四象限是上限，再细分（比如"加速上涨/匀速上涨/减速上涨"三档）样本就薄到调不动了
- 判定**必须前向**：T 日的阶段标签只能用 T 日及之前的 OAMV 算出来；写入 `scoring_regime/{date}.json` 时 assert 一下
- OAMV 本身是**全市场指标**，不区分板块。如果未来要做板块评分，再考虑给热门板块单算一条 OAMV
- **不要把 OAMV 当个股因子用**：它是给评分系统切换"参数档位"的开关，不参与个股截面打分（截面打分由方向 A 负责）

##### 阶段标签同时作为"训练集分组键"

OAMV 阶段标签不只用来切换实盘参数档位，**还作为历史训练样本的分组键**——历史上所有"多头日"的票累积成多头训练集，所有"空头日"的票累积成空头训练集，**分别**算分位阈值、IC、权重。

**为什么这样做**：传统"过去 5 年滚动窗口"会把多空两类时期混在一起，因子有效性被均值化。按阶段分组训练后：
- 多头版评分会更看重追涨/趋势因子；空头版会更看重抗跌/缩量因子
- 同阶段历史样本跨多个完整牛熊周期累积，比"近 5 年滚动"的统计意义更稳

**已用 OAMV 实测数据验证可行性（2010-01 ~ 2026-05，共 3947 个有效交易日）**：

| 阶段 | 全样本天数 | 近 5 年天数 | 连续段中位长度 |
|------|-----------|------------|--------------|
| 快速上涨 | 945 (23.9%) | 301 | 4 日 |
| 平缓上涨 | 860 (21.8%) | 265 | 4 日 |
| 快速下跌 | 1242 (31.5%) | 369 | 5 日 |
| 平缓下跌 | 900 (22.8%) | 276 | 4 日 |

- ✅ 近 5 年每个象限 ≥ 265 天，**足以稳定算 IC**（IC 一般需要 100~200 天起步）
- ✅ 用户两段实证（2025-12-19→2026-01-28 上涨 / 2025-09-03→2025-12-12 下跌）分别有 92% / 82% 落在期望象限，**判定算法可信**
- ⚠️ 连续段中位仅 4~5 天，必须加"**连续 3 日同向才确认切换**"的平滑窗，避免每周抖动

**实施分两步**：

1. **MVP（阶段 1~2 起步）**：用**二分（多头 vs 空头）**而非四象限。近 5 年多头 ≈ 566 天 / 空头 ≈ 645 天，47%/53% 均衡，训练两套权重最稳
2. **进阶（阶段 3+）**：等二分稳定、各阶段 IC 收敛后，再细化到四象限（快/平 × 上/下）

#### 方向 C：把行情指标作为因子直接喂入
**做法**：在评分系统里加一组"环境因子"，让模型自己学到环境的影响。

候选环境因子：
- 上证/沪深300 的 N 型结构状态（白线 vs 黄线位置）
- 主板涨停家数、跌停家数、涨幅中位数
- 板块强度（个股所属板块今日涨幅排名）

**与方向 B 的区别**：B 是把环境作为"开关"切换参数，C 是把环境作为"维度"加入打分。C 信息利用更充分，但需要更多样本支撑。

##### 数据可行性评估（结论：MVP 不需要新数据源）

> 数据源约束：本项目所有股票数据**只能用 Tushare**（CLAUDE.md 硬性规则）。下表所有"现成"判定都已限定在 Tushare 范围内。

| 候选因子 | 现状 | 数据来源 | 缺口 |
|---|---|---|---|
| 上证/沪深300 N 型 + 白黄线位置 | ✅ 现成 | `stock_daily_data/index_000001_SH.csv`、`index_930903_CSI.csv`（`pro.index_daily` 已落盘）| 无，本地算 MA20/MA60 + 高低点抬高 |
| 主板涨幅中位数、上涨家数比 | ✅ 现成 | 全量 `stock_daily_data/{symbol}.csv` 截面聚合 | 无 |
| 主板涨停/跌停家数（精确版） | ⚠️ 接口需积分 | `pro.stk_limit`（120 积分给涨跌停价）/ `pro.limit_list_d`（5000 积分给名单）| 主板剔除 ST 后用 \|涨幅\| ≥ 9.9% 近似即可；只有要做"封板含金量、炸板率"才必须升级 |
| 板块强度（个股所属板块涨幅排名）| ✅ 现成 | `industry_daily_data/{ts_code}.csv` 申万一级（`pro.sw_daily` 已落盘）+ `industry_mapping.json` | 无 |
| OAMV 作为环境因子（数值化喂入）| ✅ 现成 | `oamv_930903_CSI.csv` + `compute_oamv()` | 无，把 OAMV 的短/长斜率直接作为数值因子，与方向 B 的"档位开关"用途互补 |
| 市场宽度衍生（情绪持续度、量能级别）| ✅ 现成 | 同上，纯本地聚合 | 无 |
| 板块轮动度（28 板块涨幅标准差、领涨-落后差）| ✅ 现成 | 同 `industry_daily_data/` | 无 |

**可以延后再拉的接口**（按"非必要不做"原则）：

| 因子 | 接口 | 积分 | 性价比 |
|------|------|------|--------|
| 涨停封板时间、炸板率 | `pro.limit_list_d` | 5000（付费） | 等回测发现需要"封板含金量"再加 |
| 北向资金净流入 | `pro.moneyflow_hsgt` | 2000 | 大资金风向标，但非 MVP 必需 |
| 个股资金流（主力净额） | `pro.moneyflow` | 5000 | 与"成交额+换手率"重复度高，性价比低 |

**结论**：方向 C 的 MVP 完全用**现有 CSV + Tushare 已用接口（`daily` / `index_daily` / `sw_daily` / `daily_basic`）** 起步，不要先扩接口。等因子层 IC 报告（机制 A 输出）发现"市场情绪类因子已有信号但区分度不足"时，再决定是否升级到 `limit_list_d` / `moneyflow_hsgt`。

#### 推荐组合
**A（截面归一化）做主干，B（行情分类）做辅助微调**。先做完 A 看一轮回测，如果不同时期分位收益曲线已经可比，B 都不一定要做。C 留到后期，因子已经稳定后再考虑加。

### 关于"长样本 vs 短样本"的取舍

| 时间窗口 | 优点 | 缺点 |
|---------|------|------|
| 5~10 年全样本 | 样本量大，统计显著 | 跨多个行情周期，因子有效性会被均值化 |
| 滚动 12~18 月 | 贴近当前行情 | 样本不足，权重抖动大 |
| 分行情类别累积 | 同类相对可比 | 实施复杂 |

**推荐**：以 **5 年滚动窗口 + 截面归一化** 作为基本设定。每年用前 5 年训练，但因为有了截面归一化，"训练"主要是验证因子单调性、调整阈值/分箱边界，不是拟合系数。

---

## 二、纠结点 2：每日实盘 → 反馈 → 优化的闭环怎么设计

### 反馈用什么信号

目标场景已定为**超短线（T+1/T+2 为主，T+3 为辅）**，因此 **T+1 / T+2 / T+3 三个收益窗口都要记录**，分别看 IC，对应战法"2 日不拉就走 / 持仓最多 4~6 天"的真实操作节奏。

| 时点 | 收益率 | 砖型反转 | 用途 |
|------|--------|----------|------|
| **T+1** | T+1 收盘相对 T 日收盘 | 当天是否出现绿砖（红转绿）| 超短线**主指标** + 次日反转信号 |
| **T+2** | T+2 收盘相对 T 日收盘 | 当天是否出现绿砖 | 超短线**主指标**（对应"2 日不拉就走"）|
| **T+3** | T+3 收盘相对 T 日收盘（= 3 日整体涨幅）| 当天是否出现绿砖 | 辅助指标，检验"超过 2 日后是否还有惯性" |

> ⚠️ **重要**：每一列收益都必须**自始至终用同一个买入/卖出口径**。例如三个收益都统一用"T 日收盘买、T+N 收盘卖"。**不允许**为了好看而混合口径（比如 T+1 用开盘、T+2 用收盘）。

### 反馈的三个层级

#### 层级一：因子层（最细粒度，最有用）

每天每只候选股记下：
- 每个评分子项得分（已经有 `ScoreBreakdown` 结构）
- 实际收益

每周/每月做：
- **IC（信息系数）**：每个子项分数与未来收益的秩相关
- **单调性**：分箱后，分越高的组收益是不是越高
- **稳定性**：滚动窗口里 IC 是否稳定

**输出**：「翻红力度比」三个月 IC = 0.01（基本无效），「K线形态」IC = 0.08（有效）。这就是 V4 注释里写的「翻红力度比大幅降权(7→3,无效因子)」的来源 — 现状已经在做，只是手工跑、半自动。**新系统要把这个流程产品化**。

#### 层级二：股票层（中粒度）

每天选出 TopK，记录：
- TopK 平均收益、胜率
- TopK vs 主板平均的超额收益
- 各分数段（S/A/B/C/D）的胜率曲线

**期望形状**：S 级 > A 级 > B 级 > ... 的单调递减。如果出现"A 级胜率 > S 级"或者各级差不多，说明评分系统区分度不够。

#### 层级三：系统层（粗粒度，给用户看）

- 每月评分系统的"模型业绩"：累计收益、最大回撤、夏普
- 每月各因子的权重变化（如果开启自动调权）
- 每月发现的"新规律"或"失效因子"

### 反馈→优化的三种机制

#### 机制 A：人工 Review + 半自动调权（推荐起点）

**流程**：
1. 系统每周生成「因子健康报告」：IC、单调性、近期变化
2. 用户决定本周是否调权重
3. 调权后再做一轮历史回测验证

**优点**：
- 可解释、不会被短期噪声乱调
- 与现有 V4 工作流一致
- 用户能保持对系统的"主权"

**缺点**：需要人工介入，无法完全无人值守

#### 机制 B：贝叶斯增量更新（中庸）

**做法**：每个因子的权重当作分布（不是点估计）。每天的实盘数据作为新观测，更新分布的后验。

```
权重 ~ Normal(μ_prior, σ_prior)
新数据 → 更新 μ_post, σ_post
```

**关键**：先验 σ 设小一些。这样**新数据要"非常不寻常"才能撼动原权重**，避免被一周的噪声带偏。

**适合场景**：因子集合稳定后，想让权重慢慢漂移适应市场。**不适合早期"还在确认哪些因子有用"的阶段**。

#### 机制 C：滚动重训练（最激进）

**做法**：每月用过去 12 个月数据，跑一次线性回归 / LightGBM，重置所有权重。

**坑**：
- 短期样本噪声大，权重会跳来跳去
- 可能会把"上个月某板块的机会"过拟合成"全市场规律"
- 可解释性差

**只在因子集合非常成熟后才考虑**。早期不要碰。

### 推荐起点

**机制 A（人工 Review + 半自动）+ 完整的层级一/二/三日志**。先把"每天的评分、每天的实盘结果"全部存下来，跑两三个月看清楚再决定要不要走 B 或 C。

---

## 三、数据流与存储设计

### 关键存储

| 文件/表 | 内容 | 频率 |
|---------|------|------|
| `scoring_daily/{date}.parquet` | 当日全主板评分（每只股票每个子项的分） | 每日 1 次 |
| `scoring_picks/{date}.json` | 当日 TopK 候选 + 评分明细 | 每日 1 次 |
| `scoring_outcomes/{date}.parquet` | 三窗口逐步回填：T+1/T+2/T+3 每日收益率 + T+1/T+2/T+3 每日是否出现绿砖 | T+1 起逐日补，T+3 完整 |
| `scoring_factor_health/{month}.json` | 因子 IC/单调性月度报告（按 OAMV 阶段分组：多头 / 空头 各一份）| 每月 1 次 |
| `scoring_regime/{date}.json` | 当日 OAMV 阶段标签（多头/空头 二分；进阶后扩到四象限）+ 短/长斜率值 + 平滑窗状态 | 每日 1 次 |

> 现有 `output/day_positions.json`、`screening_cache/` 已经是同质数据，可以参考它们的命名/读写模式。

### 关键时间锚

**避免未来信息泄露**最严格做法：
- T 日收盘后：用 T 日及之前的数据计算 T 日评分
- T+1 / T+2 / T+3 日收盘后：分别回填对应窗口的收益列
- T+1 收益完整后可看 T+1 IC；T+3 完整后可看完整三窗口 IC + 最大涨幅/回撤

→ **T+1 IC 可以次日就看；完整三窗口报告需要滞后 3 个交易日**。三窗口同时存档的好处是中间日就能预览短窗 IC，不用等满。

---

## 四、与现有砖形图红包翠战法的对齐

评分系统**不是要替代**用户的砖形图战法，而是要**把战法语言显式化为因子**，让评分高的票看图也基本符合"蹲下去猛地弹起来"的红包翠特征。

> 战法核心见 [.claude/trading_strategies.md §三 超短线砖形图红包翠战法](.claude/trading_strategies.md)，以及现有实现 [core/screening/brick_pattern/](core/screening/brick_pattern/)（`detectors.py` 三定式 + `scoring.py` V4 四模块 + `scoring_risk.py` 风险扣分）。

### 三层结构：硬过滤 / 定式入口 / 评分因子

#### 第一层：硬过滤（不参与评分，不满足直接剔除）

| 战法纪律 | 过滤规则 | 现状 |
|----------|----------|------|
| **黄线之上** | 当日收盘价 ≥ 长期均线（项目里的"多空线"） | ⚠️ 待加。战法明文"弱势票不碰" |
| **新股 / ST / 停牌** | 上市 < 60 天、ST、当日停牌 | ⚠️ 待加（CLAUDE.md 已要求剔除） |
| **老庄股** | 长期箱体震荡（如 24 个月内未出现 N 型结构 + 均线缠绕） | ⚠️ 待加。战法明文"碰了也赚不到大钱" |
| **高开 > 4%** | 用于次日操作的入场过滤（评分阶段可不加，落到操作时再判） | 可选 |

#### 第二层：定式入口（命中任一才进评分）

完全复用现有实现，不重做：

| 定式 | 检测函数 | 砖形图含义 |
|------|---------|-----------|
| **N 型起跳** | `detect_n_shape_jump` | 上涨→回调→再上涨，配合 KDJ；红包翠最经典形态 |
| **横盘起跳** | `detect_sideways_jump` | 3~10 天横盘后突破，涨幅 ≥ 横盘期间 2 倍 |
| **上升波段延续** | `detect_uptrend_continue` | 连续 ≥ 3 红砖后 1~2 绿砖再翻红 |

**必备前提**（三定式共有，`check_prerequisites`）：砖形图绿转红 + 差值力度达标 + 短趋 > 多空。这三条是定式入口的"地基"，不再独立打分。

#### 第三层：评分因子（V4 已有 + 战法待补）

**V4 已有的主干因子**（这些是评分主体，决定大部分排名）：

| 战法元素 | 对应因子 | 现状 |
|----------|----------|------|
| **绿转红力度**（差值/前日 ≥ 0.5） | 翻红力度比 | ✅ V4 已有，回测显示无效已降权到 3 分。保留但不再增权 |
| **短趋 > 多空** | 短趋vs多空 | ✅ V4 已有 3 分 |
| **K 线形态**（红包翠 = 实体长、影线短的大阳）| K线形态 | ✅ V4 已强化到 10 分（跨定式最强通用因子） |
| **信号日涨幅**（弹起来的力度） | 信号日涨幅 | ✅ V4 已有 8 分 |
| **均线多头排列** | 均线排列 | ✅ V4 已有 3 分 |
| **短趋斜率向上** | 短趋斜率 | ✅ V4 已有 3 分 |
| **MACD 环境** | MACD 辅助 | ✅ V4 已有 25 分专项 |

**战法待补：额外加分项**（非强制、小权重，不要喧宾夺主）：

| 战法元素 | 出处 | 建议形态 |
|----------|------|---------|
| **红柱 ≥ 绿柱 2/3** | `trading_strategies.md:221` 砖形图六规则之② | 命中即 +N 分（小权重，2~3 分量级即可）。计算近 N 砖红/绿砖累计长度比 |
| **地量** | `trading_rules.md:159` "地量 + N 型 + 买入信号 = 最佳买点" | 命中即 +N 分（小权重）。判定：信号日成交量缩到近 30~60 日最低区间 |

> 这两项是战法**经验加分项**，不是必备条件——做成"命中则加分、不命中不扣"的非对称形式，避免无意中变成隐性硬过滤。

**战法待补：风险/位置类**（建议作为硬过滤或风险扣分，不作为正向因子）：

| 战法元素 | 已有 helper | 建议接入方式 |
|----------|------------|-------------|
| **N 型下跌段未结束**（位于 N 型下跌阶段中）| `helpers.py:295 _is_in_n_shape_decline` | 建议**作为硬过滤**——下跌段命中的属于"假定式"，应直接剔除而非扣分 |
| **第三浪 / 已涨过的位置** | `helpers.py:325 _detect_third_wave` | 加入风险扣分：已在第三浪末端的高位定式，大幅扣分（红包翠找起涨点，不追末端） |
| **DIFF/DEA 金叉时间** | `helpers.py:276 _detect_diff_dea_cross` | 纳入 MACD 子项细化：刚金叉 vs 金叉多日，加分不同 |

#### 风险扣分项（`scoring_risk.py` 现状）

| 战法风险 | 扣分规则 | 现状 |
|---------|----------|------|
| **前 10 日一字板跌停** | 触发硬过滤 | ✅ `filter_limit_down` 已实现 |
| **横盘过长**（> 10 天） | 仅横盘起跳触发 | ✅ `filter_long_sideways` 已实现 |
| **前 10 日放量大阴线（斩杀线）** | 触发硬过滤 | ✅ `filter_heavy_volume_drop` 已实现 |
| **高位放量阴线 / S1 出货信号** | 距高点 < 5% 时扣 -30，否则扣 -15；含 N 型回调豁免、信号日修复豁免等 | ✅ `scoring_risk.py:70 _check_heavy_volume_drop` 已实现，**不需要新加** |
| **第三浪末端 / 累计涨幅过大** | 加大扣分（红包翠是找起涨点，不是追末端） | ⚠️ 待加，配合 `_detect_third_wave`（同上表） |

> **关于"长阴短柱 / 击穿对手盘"的避免误杀**：作为额外说明，不需要在评分上偏重处理。`_check_heavy_volume_drop` 现有逻辑里的 `is_heavy = volume[day] >= avg_volume * 1.5` 判定**已经天然排除缩量阴线**——只要量没放出来就不会触发扣分。所以"缩量洗盘不误杀"这条战法纪律实际已经隐含覆盖，不需要再单加因子。

### 不放进评分系统的战法纪律（属于交易执行层）

砖形图战法里还有一些规则不适合做成因子，它们是**操作纪律**而不是选股因子：

| 战法纪律 | 落点 |
|---------|------|
| 当日尾盘买 / 次日早盘买 | 操作时点，不影响选股 |
| 高开 > 4% 不追 | 次日操作过滤，评分时未知 |
| 仓位分半，最多持 2 票 | 仓位管理，与选股无关 |
| 止损 = 买入 K 线最低点 | 止损规则，回测口径里体现 |
| 2 日不拉就走 / 持仓 4~6 天 | 持仓周期，决定了 **T+3 回填窗口**（已在第二节确定）|
| 脱离成本区 5% 以上止盈 | 止盈规则，与选股无关 |

### 与 V4 评分的关系

新设计**不推翻 V4**，而是：
1. 保留 V4 的「通用质量(30) + MACD(25) + 信号强度(15) − 风险扣分」四层结构
2. 在 V4 基础上**补缺**上表中标 ⚠️ 的战法因子
3. 把"硬过滤"层（黄线、新股 ST、老庄股、N 型下跌段）显式分离出来，不再混在评分里靠扣分模拟

> ⚠️ **重要**：评分系统选出来的票，最终判断权仍在用户。评分只是把"今天 4000 多只主板里值得看图的 30~50 只"圈出来，是否扣扳机由你看图决定。

---

## 五、实施路线（建议分四阶段）

### 阶段 0：基础设施（先做完才能谈优化）
1. 把现有 brick_pattern 评分扩展到全主板候选池
2. 落地每日评分存档 (`scoring_daily/`)、TopK 存档 (`scoring_picks/`)
3. 建立 T+1 / T+2 / T+3 三窗口自动回填机制 (`scoring_outcomes/`)
4. 跑通"每天评分 → 次日看 T+1 IC、T+3 后看完整三窗口"的最小闭环

**完成标志**：能拉出"过去 60 天每天的 TopK 实际表现"。

### 阶段 1：截面归一化（解决纠结点 1）
1. 把绝对阈值因子（信号日涨幅、T日涨幅、翻红力度比、短趋斜率）改成截面分位
2. 用过去 5 年数据回测，验证不同时期 TopK 收益曲线是否更平稳
3. 对比改造前后的 IC

**完成标志**：S 级胜率在 2020/2022/2024 三个不同时期之间的差异显著缩小。

### 阶段 2：因子健康度报告（解决纠结点 2 的层级一/二）
1. 实现 IC、单调性、分位收益曲线的自动计算
2. 月度报告自动生成（在新建的独立 `ScoringPage` 评分诊断页内）
3. 人工 Review 调权工作流

**完成标志**：用户每月花 30 分钟看报告就能决定要不要调权重。

### 阶段 3：行情分类与战法因子（精细化）
1. 加 OAMV 阶段切换（快速上涨/平缓上涨/快速下跌/平缓下跌）的参数档位
2. 加 B1 检测、底部暴力K、S1 等战法专属因子
3. 风险扣分项细化"洗盘 vs 出货"的区分
4. 视情况引入贝叶斯权重更新（机制 B）

**完成标志**：评分系统选出的图形和用户人工选股的重合度 > 70%。

---

## 六、风险与权衡（必须事先想清楚）

| 风险 | 应对 |
|------|------|
| **过拟合**：5 年回测好看，实盘崩 | 分阶段验证：训练区间 + 留出最近 6 个月做独立测试集；权重调整必须经过留出集二次验证 |
| **生存者偏差**：退市/ST 股票的历史不在样本里 | 训练数据要包含**当时**全主板，不能只用今天的活跃股 |
| **未来信息泄露**：拿了 T+1 收盘价去算 T 日因子 | 所有特征必须严格用 T 日及之前数据；写一个 `assert no_future_data(features, t)` 检查 |
| **样本不平衡**：S 级股票太少，统计不显著 | 分位数报告要看 count，count < 30 的不下结论 |
| **频繁调权**：每周看到一点波动就调，反而越调越差 | 调权门槛：连续 2 个月 IC 显著变化才动；每次只调 1~2 个权重，不要批量 |
| **战法冲突**：评分系统选了一只"图形不像"的票 | 评分系统是粗筛，看图判断是终审。不要让评分覆盖人工判断 |

---

## 七、决策结论（已拍板）

以下 5 个关键参数已由用户全部拍板，作为后续实施的基线。

### 1. 目标场景：超短线选股池

- 主指标：**T+1 / T+2 收益率**（1~2 天内决定走留）
- 辅助指标：T+3 收盘收益（对应砖形图战法"持仓最多 4~6 天"的中段）
- 评分要重视：信号强度、绿转红力度、当日涨幅、地量+红包翠形态
- **不做波段方向**（T+5~T+20 那一档不在本评分系统服务范围）

### 2. TopK 的 K：K = 20

- 战法明文"仓位分半，最多同时持 2 票"（`trading_strategies.md:223`）
- **K = 20**：从 20 张候选图里挑 2 票，给用户更多余地，分散看图视角；同时月度 IC 样本量更稳（400~500）
- 用户界面 = 因子健康度统计样本 = 同一份 Top 20，不再分两套

### 3. 训练时间窗口：按 OAMV 阶段动态累积（不再用固定年限）

- **MVP（阶段 1~2）**：用二分（多头 / 空头）累积近 5 年内同阶段日子，分别训练两套权重
- **进阶（阶段 3+）**：细化到四象限（快速上涨 / 平缓上涨 / 快速下跌 / 平缓下跌）
- 阶段判定加"**连续 3 日同向才确认切换**"的平滑窗，避免每周抖动
- 详见第一节方向 B 末尾的"阶段标签同时作为训练集分组键"段落

### 4. 股票池范围：用现有 `stocklist.csv` + 日线数据本地判定

| 过滤项 | 判定方式（用现有数据）|
|--------|---------------------|
| **只保留主板** | `ts_code` 不以 `30` / `68` / `8` / `4` 开头（剔除创业板/科创板/北交所）|
| **剔除 ST** | `name` 字段包含 `ST` 或 `*ST` |
| **剔除停牌** | 当日 `stock_daily_data/{symbol}.csv` 无记录或 `volume = 0` |
| **新股 < N 天** | 暂不过滤（`stocklist.csv` 无上市日期字段；如需精确过滤可后续从 `pro.stock_basic` 拉一次性补字段，但 MVP 不必做）|
| **市值过滤** | 暂不过滤（`stocklist.csv` 无市值字段）|

`stocklist.csv` 现有 3055 行，包含 `ts_code, symbol, name, area, industry` 五列。**用这些字段就够 MVP 用**。

### 5. 手工干预深度：每日诊断面板

- 用户希望**每天看到**评分明细 + 诊断数据，而不是每月一次报告
- **UI 投入**：新建独立顶级页面 `ScoringPage`（在左侧导航与"看盘/选股/统计"等同级），每日刷新展示：
  - 当日 OAMV 阶段标签 + 历史阶段时序图
  - 当日 TopK 候选 + 每只票的评分子项明细
  - 前 N 日 TopK 的 T+1 实盘表现（次日就有数据）
  - 因子滚动 IC 趋势（按多/空头分别展示）
- 月度报告作为深度补充，不作为主交互入口

---

## 八、不在本次设计内的话题（避免发散）

- 板块/行业评分（更复杂的截面问题，留作未来扩展）
- 基本面因子（业绩、估值，与日线技术评分体系是两套逻辑）
- 实盘下单接口、自动交易（项目定位是查看器+训练器，不做实盘）
- 多周期评分（周线 + 日线联合，初版只做日线）

---

## 附录：与现有 V4 评分的对比

| 维度 | V4 现状 | 新设计目标 |
|------|---------|-----------|
| 覆盖范围 | 砖形图 3 种定式 | 主板（剔除 ST/停牌/创业/科创/北交所）每日打分 |
| 阈值方式 | 绝对阈值 | 截面分位为主，绝对为辅 |
| 时期适应 | 单一参数 | **按 OAMV 阶段分组训练**（MVP 多头/空头二分，进阶四象限）|
| 目标场景 | 砖形图三定式通用 | **超短线 T+1/T+2 主、T+3 辅** |
| 反馈口径 | 手工跑 backtest 看 IC | **T+1/T+2/T+3 三窗口自动回填**，按多空头分组算 IC |
| 反馈频率 | 半年级别调一次权 | 每日诊断面板 + 月度深度报告 |
| 战法对齐 | 部分对齐（K线形态、MACD） | 三层结构：硬过滤 / 定式入口 / 评分因子（V4 已有 + 红柱比/地量等额外加分项） |
| 数据落盘 | screening_cache（结果） | 评分明细 + 三窗口实盘回填 + 阶段标签 + 月度报告（四层） |

V4 已经走通了核心方法论（"靠回测验证因子，按结果调权"），新系统是把这套方法论**自动化、产品化、扩大覆盖面**，不是推翻重来。
