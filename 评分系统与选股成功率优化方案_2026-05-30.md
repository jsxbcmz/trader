# 砖形图评分系统 & 选股成功率 优化方案

> 📌 来源: 🤖 Agent 分析（基于 `output/` 全量回测数据 + `scoring_system_design.md` 评分设计）
> 生成时间: 2026-05-30 | 数据区间: 2026-05-22 ~ 2026-05-29（5 个交易日，7 轮预测）
> 配套文档: `评分系统优化分析_2026-05-30-ds.md`（偏分析），本文档偏**可落地实施**（具体到字段/阈值/代码改造点）

---

## 〇、TL;DR（一页结论）

1. **评分系统选股没问题，问题在「次日方向预测」**。评分对 T+3 收益有效（S 级 +16.5%、A 级 +10.9%），但对 T+1 方向准确率只有 **34%**，还不如手动的 **60%**。
2. **三大亏损来源**：高开低走（50%）、板块 V 型反转（33%）、普跌日拖累（17%）。这三类目前评分系统**完全没建模**。
3. **核心病根**：评分把「涨停股」和「普通强势股」混在一套公式里打分，导致涨停股因为信号日涨幅(8)+T日涨幅(10)+K线形态(10) 集体抱团 Top20，但它们次日方向最不可测。
4. **本次给出 6 项可直接改代码的优化**，按 ROI 排序：板块反转扣分 > 涨停次日独立评估 > 短期过热扣分 > 普跌日降权 > 涨停/非涨停分组 > MACD 金叉天数调权。
5. **配套：把 T+1 预测「是否正确」回填进 `screening_predictions`**，否则永远无法量化优化是否真的提升了成功率。

---

## 一、output 目录数据盘点

`output/` 是整条选股流水线的产物，分四类子目录，各自承担一段职责：

| 目录 | 内容 | 关键字段 / 用途 |
|------|------|-----------------|
| `screening_raw/` | 每日全量扫描原始结果 + 市场上下文 | `screening_detail_*.json` 含每只命中股的评分明细：`q/m/s/p/r/score` + `q_detail/m_detail/s_detail/r_detail`、`brick`(砖值)、`st/ls`(短趋/多空线)、`vol_ratio`、`is_limit_up`、`ind_perf`(行业涨幅) |
| `screening_analysis/` | 人工可读的每日 .md 分析报告 | Top 12 评分表 + 重点股深度拆解 + 次日预测摘要 + 普跌日判定 |
| `screening_predictions/` | 结构化预测 JSON + 回顾 | `*.json` 是预测；`review_*.json` 是预测兑现回顾，含 `open_chg/day_chg/result/error_analysis` |
| `manual_predictions/` | 用户手动分析的预测 | 字段同上，`source:"manual"`；是评分系统的「人工对照组」 |

**数据规模**：覆盖 2026-05-22 ~ 05-29 共 5 个交易日，自动预测约 60 条、手动预测约 40 条，已回填验证 4 轮。样本量偏小（统计意义有限），但**错误模式高度一致**，足以定性。

**数据可用性提醒**：
- `screening_detail_*.json` 只有 05-29 一天有完整字段，**历史日缺评分明细** → 因子级 IC 回测暂时做不了，只能做方向准确率统计。
- `manual_predictions` 的 `score/grade` 多为 `null` → 手动预测靠经验而非评分，正好作为「纯量化 vs 纯经验」的对照。

---

## 二、回测：评分系统到底准不准

### 2.1 次日方向准确率（自动 vs 手动）

| 回测轮次 | 大盘均涨 | 自动准确率 | 手动准确率 |
|----------|:--------:|:---------:|:---------:|
| 05-22 → 05-25 | 偏强 | — | **6/6 = 100%** |
| 05-25 → 05-26 | -0.92% 偏弱 | 39% | 35% |
| 05-26 → 05-27 | -1.72% 普跌 | 30% (3/10) | 67% (4/6) |
| 05-27 → 05-28 | +0.31% 正常 | 30% (3/10) | 83% (5/6) |
| 05-28 → 05-29 | -1.43% 普跌 | 36% (5/14) | — |
| **汇总（剔除小样本）** | — | **≈34%** | **≈60%** |

> ⚠️ 自动选股次日方向准确率 **34%，低于抛硬币**。同样的票，手动判断能到 60%——说明「看图 + 经验」对**次日方向**有信息增量，而当前评分公式没捕捉到。

### 2.2 但评分对「中期收益」是有效的

历史案例按 S/A/B 分级对照 3 日收益：

| 等级 | 平均 3 日收益 |
|------|:-----------:|
| S 级 (85+) | **+16.5%** |
| A 级 (70~84) | **+10.9%** |
| B 级 (55~69) | **+10.0%** |

✅ 梯度清晰 → **评分的「选股能力」是有效的**，它能把值得看的票圈出来。
❌ 但 **评分高 ≠ 次日方向对**（如天创时尚 A 级 79 分，次日开 -2.56%）。

**结论：不要动评分的选股主干，要补的是「次日方向」这一层。** 评分负责「选出好票」，方向预测要单独建模。

### 2.3 三大错误模式（来自 05-28→05-29 最完整一轮）

| 错误类型 | 占比 | 典型案例 |
|----------|:----:|----------|
| **涨停次日高开低走** | 50% | 北玻 +6.3% 开 → 收 -9.96%；万通 +3.93% → 收 -3.81%；众源 +3.31% → 收 -6.07% |
| **板块 V 型反转** | 33% | 玻璃昨 +4.66% 领涨 → 今 -5.01% 暴跌，中国巨石/北玻全军覆没 |
| **普跌日拖累** | 17% | 南方精工 +0.73% 开 → 收 -4.82%，大盘 -1.43% 碾压 |

这三类错误，**评分系统目前一个都没建模**——这正是优化的靶心。

## 三、核心缺陷定位（对照评分代码）

评分代码位于 `core/screening/brick_pattern/`：`detectors.py`（三定式）+ `scoring.py`（V4 四模块）+ `scoring_risk.py`（风险扣分）+ `helpers.py`（指标 helper）。

### 缺陷 1：涨停股和普通股共用一套公式 → 涨停股抱团 Top20

涨停日 `信号日涨幅(8) + T日涨幅(10) + K线形态(10)` 三项几乎全满，27 分白送，导致涨停股天然挤进 Top20。但 2.3 节显示**涨停次日方向最不可测**。

**证据**：05-28 命中 50 只里 9 只涨停进 Top10，次日仅 2 只勉强算对（科林电气、格林达），其余全亏。

➡ 不该靠评分预测涨停次日方向，应给涨停股**独立评估维度**。

### 缺陷 2：完全没有「板块维度」因子

`scoring.py` 的 q/m/s/p 全是个股技术指标，`r`（风险扣分）也只看个股 K 线。**板块涨跌只在报告里展示，从不进评分**。

**证据**：玻璃板块昨 +4.66% 领涨 → 次日 -5.01% 暴跌（V 型反转规律），评分对中国巨石/北玻仍给高分高置信。板块动量的「持续性」和「反转性」是占 33% 错误的根源。

### 缺陷 3：没有「短期过热」因子

5 日内已涨 25%+ 的票，砖形图再漂亮也接近透支。手动预测对此判断很准（模塑科技 5 日涨 15% → 判「冲高回落」✅），但 `scoring.py` / `scoring_risk.py` 里**没有任何累计涨幅相关扣分**（只有单日的「三波追高 -8」和「高位放量阴线」）。

### 缺陷 4：普跌日没有系统性降权

报告里手动写了「普跌日置信度降一档」，但**评分公式本身不感知大盘**。`screening_detail` 里有 `market_bg.is_bearish` 字段却没被 `score` 消费。普跌日偏多预测几乎全错（05-26、05-29），降权应自动化。

### 缺陷 5：MACD 金叉天数权重过小

`helpers.py` 已有 `_detect_diff_dea_cross`，但「刚金叉」只 +1 分，权重太低，没体现「刚金叉爆发力最强」的规律。

## 四、可落地优化方案（带真实改造点 & 完整代码）

### 4.0 先厘清「在哪一层改」（关键架构事实）

读了真实代码后，必须先修正一个认知，否则改错地方：

> **评分函数（`scoring.py` / `scoring_risk.py`）的入参 `indicators` 只包含单只股票的 K 线/指标数组（`close/open/high/low/volume/brick/short_trend/long_short/macd_*/ma*`），完全拿不到行业涨幅、大盘均涨、前一日板块数据。**

调用链如下：

```
scripts/screen_full.py（全量扫描入口，连 db/market.db，能查行业/大盘）
   └─ 逐只 screen_single_stock() → 判定定式
   └─ 命中后调 4 个评分函数：
        compute_common_quality_score()   # 质量30
        compute_macd_auxiliary_score()    # MACD25
        compute_signal_strength_score()   # 信号15
        compute_p3_bonus()                # 加分0~5
        compute_risk_penalty()            # 风险扣分（在 scoring_risk.py）
   └─ raw_total = q + m + s + p - r
```

因此优化分两类落点：

| 优化 | 落点 | 原因 |
|------|------|------|
| 板块因子（优化1） | **`screen_full.py` 后处理层** | 评分函数拿不到行业数据，只能在扫描脚本里查 DB 后对最终分 `score` 做调整（`raw_total` 裸分明细不动） |
| 普跌日降权（优化4） | **`screen_full.py` 后处理层** | 同理，大盘均涨只有扫描脚本知道 |
| 涨停次日质量（优化2） | **`screen_full.py` 后处理层** | 需要行业 + 5日涨幅，不进评分 |
| 短期过热（优化3） | **`scoring_risk.py` 新增 `_check_overheat`** | 5日涨幅可由 `indicators["close"]` 本地算，能进评分函数 |
| MACD金叉天数（优化6） | **`scoring.py` 的 `compute_macd_auxiliary_score`** | 纯个股指标，直接改 |
| 分组展示（优化5） | **`screen_full.py` 输出 + 报告模板** | 纯展示 |

> 原则不变：**不动已验证有效的评分选股主干**（K线形态10、信号日涨幅8、T日涨幅10 等），只新增「风险扣分」和「后处理调整」两层。

---

### 优化 3：短期过热因子（改 `scoring_risk.py`，唯一能进评分函数的）

**为什么先讲它**：它是 6 项里唯一数据自洽（`indicators["close"]` 就能算 5 日涨幅）、能直接进评分函数的，改动最干净。

**改造点**：`core/screening/brick_pattern/scoring_risk.py` 新增 `_check_overheat`，并在 `compute_risk_penalty` 的「通用风险」段注册。

```python
# ── 新增风险9：短期过热（5日累计涨幅过大）──
def _check_overheat(close, index):
    """5 日累计涨幅 > 15% 扣分，> 25% 重扣。红包翠是找起涨点，不追末端。"""
    triggered = False
    penalty = 0.0
    desc = ""
    if index >= 5 and close[index - 5] > 0:
        cum_chg_5d = (close[index] - close[index - 5]) / close[index - 5] * 100
        if cum_chg_5d > 25:
            penalty = -15.0
            triggered = True
            desc = f"短期过热(5日涨{cum_chg_5d:.1f}%>25%)"
        elif cum_chg_5d >= 15:
            penalty = -5.0
            triggered = True
            desc = f"短期偏热(5日涨{cum_chg_5d:.1f}%)"
    detail = RiskFilterDetail(
        filter_type=RiskFilterType.CHASE_HIGH,   # 复用现有枚举，避免改 models
        triggered=triggered,
        description=desc,
        penalty=penalty,
    )
    return detail, ("短期过热" if triggered else None)
```

在 `compute_risk_penalty` 里注册（紧跟 `_check_chase_high_pullback` 后）：

```python
    _record(_check_chase_high_pullback(close, index))
    _record(_check_overheat(close, index))          # ← 新增这一行
```

> 注意：`compute_risk_penalty` 末尾有 `total_penalty = max(sum(...), -30.0)` 的扣分下限。新增 -15 在已有扣分基础上叠加，仍受 -30 封顶保护，不会过扣。

**依据**：模塑科技 5 日 +15% → 手动判「冲高回落」✅；北玻短期涨幅大 → 次日 -9.96%。

---

### 优化 6：MACD 金叉天数权重调优（改 `scoring.py`）

**改造点**：`compute_macd_auxiliary_score`（`scoring.py:227`）里 N型/横盘分支的「金叉确认」项（`scoring.py:274~284`）。`golden_dist`（来自 `_detect_diff_dea_cross`，line 245）已算出金叉天数。

**现有真实代码（line 274~284，已核实）**：

```python
        # 金叉确认 (0~7, 可扣分)
        if golden_dist > 0 and golden_dist <= 5:
            items["金叉确认"] = 7          # 1~5 日内金叉都给满分
        elif golden_dist == 0:
            items["金叉确认"] = 4          # 当日金叉反而只给 4（不合理）
        elif diff[index] > dea[index]:
            items["金叉确认"] = 3
        elif index >= 1 and diff[index] > diff[index - 1]:
            items["金叉确认"] = 2
        else:
            items["金叉确认"] = 0
```

问题：① 当日金叉（`golden_dist==0`，爆发力最强）反而只给 4，而 1~5 日给 7，逻辑倒挂；② 金叉过久（已涨一波）不扣分。

**改为（优化6）**：

```python
        # 金叉确认 (0~7, 可扣分) —— 优化6：当日金叉给满、过久衰减扣分
        if golden_dist == 0:
            items["金叉确认"] = 7          # 当日金叉，爆发力最强（原 4 → 7）
        elif 1 <= golden_dist <= 2:
            items["金叉确认"] = 7          # 刚金叉，仍给满分
        elif 3 <= golden_dist <= 5:
            items["金叉确认"] = 5          # 延续期（原 ≤5 一律给7，收紧到5）
        elif diff[index] > dea[index]:
            items["金叉确认"] = 3          # 多头排列但已非刚金叉
        elif index >= 1 and diff[index] > diff[index - 1]:
            items["金叉确认"] = 2
        else:
            items["金叉确认"] = 0
```

> ⚠️ 注意：`_detect_diff_dea_cross(diff, dea, index, lookback=5)` 的 `lookback=5`，所以 `golden_dist` 最大只到 5，**不存在 `>15` 的取值**——原方案里「>15 扣 -2」无法触发，已删除。若要识别「金叉过久」，需先把 `lookback` 调大，属于更大改动，本次不做。
> ⚠️ 改这项会动到评分主干，**必须先跑回测验证 IC 不下降**再上线。属于 P2 低优先级，不急。

---

### 优化 1：板块动量衰减（改 `screen_full.py` 后处理层）

**为什么不在评分函数改**：见 4.0，评分函数拿不到行业数据。必须在 `screen_full.py` 里，先建「行业 → 今日均涨、昨日均涨」映射，再对命中股的最终分 `score`（即 `match.final_score`）做调整。

> ⚠️ **改的是 `score` 不是 `raw_total`**：`screen_full.py` 里 `score = match.final_score`（最终分）、`raw_total = q+m+s+p-r`（裸分拆解明细）是两个独立字段。板块扣分作用于 `score`（影响排序），`raw_total` 保持原样（保留评分拆解可追溯）。

> ⚠️ **前置改动（已核实当前代码缺这块）**：现有 `screen_full.py` 的 `results.append({...})` 里**没有 `industry` 字段**，取股票列表的 SQL（line 28~32）也只 SELECT 了 `sl.symbol, sl.name`。`stock_list` 表本身有 `industry` 列（已核实 `database.py:121`），所以落地优化1/2 前，必须：
> 1. 把取列表 SQL 改成 `SELECT sl.symbol, sl.name, sl.industry FROM stock_list sl ...`，循环变量改 `for idx, (symbol, name, industry) in ...`；
> 2. 在 `results.append({...})` 里补一行 `"industry": industry,`。
> 这样后续 `sector_penalty(industry, ...)` 才有数据可用。

**改造点**：`scripts/screen_full.py`，在扫描循环前构建行业上下文，循环内对每只命中股调用 `sector_penalty`。

```python
# ① 扫描前：构建行业涨幅映射（今日 + 昨日）
def build_industry_perf(conn, target_date, prev_date):
    """返回 {industry: today_avg_chg}, {industry: prev_avg_chg}, set(昨日Top3行业)"""
    def _ind_avg(date):
        rows = conn.execute(
            "SELECT sl.industry, "
            "AVG((d.close - p.close) / p.close * 100) AS chg "
            "FROM stock_daily d "
            "JOIN stock_daily p ON d.symbol = p.symbol "
            "JOIN stock_list sl ON d.symbol = sl.symbol "
            "WHERE d.date = ? AND p.date = ("
            "  SELECT MAX(date) FROM stock_daily WHERE symbol = d.symbol AND date < ?) "
            "GROUP BY sl.industry", (date, date)
        ).fetchall()
        return {ind: chg for ind, chg in rows if ind}
    today = _ind_avg(target_date)
    prev = _ind_avg(prev_date)
    top3_prev = set(sorted(prev, key=prev.get, reverse=True)[:3])
    return today, prev, top3_prev

# ② 循环内：对每只命中股算板块扣分
def sector_penalty(industry, is_limit_up, ind_today, ind_prev, top3_prev):
    penalty = 0.0
    flags = []
    prev_chg = ind_prev.get(industry)
    today_chg = ind_today.get(industry, 0.0)
    # 昨日板块极强(>4%) → 次日V型反转高危
    if prev_chg is not None and prev_chg > 4.0:
        penalty -= 6
        flags.append(f"板块昨日领涨{prev_chg:+.1f}%(V反风险)")
        if industry in top3_prev:
            penalty -= 3
            flags.append("昨日全市场Top3板块")
    # 逆板块涨停：板块今日跌>2% 但个股涨停 → 独木难支
    if today_chg < -2.0 and is_limit_up:
        penalty -= 8
        flags.append(f"逆板块涨停(板块{today_chg:+.1f}%)")
    return penalty, flags
```

在 `results.append({...})` 处，给每条记录补两个字段并修正分数：

```python
        sec_pen, sec_flags = sector_penalty(
            industry, is_limit_up, ind_today, ind_prev, top3_prev)
        # ...
        "sector_penalty": sec_pen,
        "sector_flags": sec_flags,
        "score": round(max(0, match.final_score + sec_pen), 1),  # 板块扣分作用于最终分
```

**参数依据**：玻璃昨 +4.66% → 次日 -5.01%（命中 -6，且在 Top3 再 -3）；春秋电子在元器件 -3.64% 逆势涨停 → 次日承压（命中逆板块 -8）。

---

### 优化 4：普跌日自动降权（改 `screen_full.py` 后处理层）

**改造点**：`screen_full.py` 扫描后、排序前，先判普跌日（全市场均涨 < -1%），再对所有 `score` 打折。

```python
# 扫描结束后、results.sort 之前
market_avg = conn.execute(
    "SELECT AVG((d.close - p.close) / p.close * 100) "
    "FROM stock_daily d JOIN stock_daily p ON d.symbol = p.symbol "
    "WHERE d.date = ? AND p.date = ("
    "  SELECT MAX(date) FROM stock_daily WHERE symbol = d.symbol AND date < ?)",
    (target, target)
).fetchone()[0] or 0.0
is_bearish_day = market_avg < -1.0

if is_bearish_day:
    for r in results:
        r["score"] = round(r["score"] * 0.85, 1)   # 裸分打 85 折
        r["regime_flag"] = f"普跌日(均涨{market_avg:.2f}%)-置信度降一档"
```

并把 `market_avg` / `is_bearish_day` 写进输出 json 的 header（供报告和回填用）。

> ⚠️ **与优化1 的叠加顺序（重要）**：优化1 和优化4 都改 `score` 字段，必须**先板块扣分、后普跌打折**——即循环内先做优化1 的 `score = final_score + sec_pen`，扫描结束后优化4 再对这个已扣板块分的 `score` 整体 ×0.85。顺序固定，且各自只执行一次，避免重复打折。

**依据**：05-27（均涨 -1.72%）、05-29（均涨 -1.43%）普跌日偏多预测几乎全错。

---

### 优化 2：涨停次日独立评估（改 `screen_full.py` 输出 + 报告）

**改造点**：`screen_full.py` 对 `is_limit_up=True` 的股票额外算 `limit_up_quality`，写进输出 json，供报告/预测使用。**不进 `score`**（涨停日评分对 T+3 收益仍有效，只是 T+1 方向弱）。

```python
def limit_up_quality(vol_ratio, brick_val, ind_today_chg, cum_chg_5d):
    q = 0
    q += 1 if 0.5 <= vol_ratio <= 1.5 else (-1 if vol_ratio > 3 else 0)  # 缩量锁仓优于放量分歧
    q += 1 if brick_val < 100 else (-1 if brick_val > 120 else 0)        # 砖值高位风险
    q += 1 if ind_today_chg > 0 else (-1 if ind_today_chg < -2 else 0)   # 板块共振 vs 逆势
    q += -1 if cum_chg_5d > 20 else (1 if cum_chg_5d < 10 else 0)        # 短期透支
    if q >= 2:
        return "strong"
    if q <= -1:
        return "weak"
    return "neutral"
```

`cum_chg_5d` 在循环内已可由 `df['close']` 算出。输出字段：

```python
        "limit_up_quality": limit_up_quality(vol_ratio, brick_val, today_chg, cum_chg_5d) if is_limit_up else None,
        "cum_chg_5d": round(cum_chg_5d, 2),
```

**报告展示规则**：涨停股标 🔴 单独成组；`weak` 标「⚠️ 涨停质量弱，次日防高开低走」，`strong` 标「✅ 涨停质量强」。

---

### 优化 5：涨停 / 非涨停分组展示（改 `screen_full.py` 输出 + 报告模板）

不改公式，输出 json 增加分组，报告 Top 表按 `is_limit_up` 拆两组：

- 🔴 **涨停组**（`is_limit_up=True`）：次日方向看优化2的 `limit_up_quality`，不直接信「偏多」。
- 🟡 **强势未封板组**（涨幅 3%~9.5%）：可信度高于涨停组的次日方向预测。

让用户有意识地分别对待两类信号——这正是缺陷 1 的展示层解法。

## 五、把手动经验形式化为因子

手动预测 60% 远超自动 34%，差距来自三条可形式化的经验规律：

### 规律 1：手动更善于「看空」→ 把 S1 出货纳入更强扣分

手动准确判断的偏空案例：雅克科技（高位巨量 S1 → -3.85% ✅）、华工科技（高位赶顶 → -0.94% ✅）、有研新材（S1 放量阴线反弹无力 → -4.21% ✅）。

`scoring_risk.py` 已有 `_check_heavy_volume_drop`（S1 出货扣 -15~-30），但仅看个股。**建议组合条件加强**：`S1 出货 + 高位(brick>120) + 板块偏弱(ind_perf<0)` 三者同时命中时，额外 -5 并强制方向标「偏空」。

### 规律 2：手动有「涨停次日高开幅度」纪律 → 落到预测输出

手动预测反复写「高开 >4% 不追 / >5% 不追 / >3% 警惕承接不足」。这是次日**操作**纪律，无法在 T 日评分时知道（高开是 T+1 竞价才有），但应写进预测 JSON 的 `key_risks` 和操作建议字段，作为次日竞价的实时修正条件：

```
若 T+1 竞价高开 > 4%  → 标「高开过大，不追，等回踩」
若 T+1 竞价低开       → 标「偏多判断失效，止损」
```

### 规律 3：手动区分「板块龙头 vs 跟风」→ 接入优化 1/2

手动对中国巨石判「玻纤龙头惯性冲高」（当天对），但次日板块 V 反不可控。建议在预测里给涨停股标 `🏆 板块龙头`（板块涨幅第一）或 `📎 跟风涨停`（板块内排名靠后），跟风涨停降置信度——这正是优化 1（逆板块扣分）和优化 2（龙头位维度）要落地的。

---

## 六、闭环：预测正确率回填机制

**当前最大短板：无法量化「优化是否真的提升了成功率」。** `review_*.json` 只回填了 1 轮，且只记 `result`，没有按因子归因。建议：

1. **每日自动回填** `screening_predictions/{date}.json` 增加字段：
   ```json
   {
     "open_chg": 2.99, "day_chg": 2.87,
     "pred_correct": true,            // 方向是否对，用于校准
     "limit_up_quality": "strong",    // 优化2 的标签，回填后验证其有效性
     "sector_reverted": false         // 板块是否V反，验证优化1
   }
   ```
2. **回填全部历史 7 天**（现仅 1 轮），样本累积到 ≥200 条才有统计意义。
3. **因子级 IC**：等 `screening_detail` 每日存全字段后（目前仅 05-29 全），按 `q_detail`/`m_detail` 各子项分组算与 T+1/T+3 收益的相关性，淘汰无效因子（如翻红力度比回测已证无效，可进一步降权）。
4. **优化前后 A/B 对比**：上线优化 1~4 后，对比改造前后自动预测准确率，目标从 34% → **≥50%**（达到/超过抛硬币，逼近手动 60%）。

---

## 七、实施优先级 & 验收标准

| 优先级 | 优化项 | 改动文件 & 函数 | 预期影响 | 难度 | 验收标准 |
|:------:|--------|----------------|:--------:|:----:|----------|
| 🔴 P0 | 优化1 板块动量衰减扣分 | `screen_full.py` 新增 `build_industry_perf` / `sector_penalty` | 挽回 33% V反误判 | S | 玻璃类 V 反案例 score 被 -6~-9，不再给高置信偏多 |
| 🔴 P0 | 优化4 普跌日降权 | `screen_full.py` 排序前 `score×0.85` | 挽回 17% 普跌拖累 | S | 普跌日全体 score 降档，header 标 `is_bearish_day` |
| 🟡 P1 | 优化3 短期过热扣分 | `scoring_risk.py` 新增 `_check_overheat` + 注册 | 减少追高 | S | 5日涨>15% 扣-5、>25% 扣-15，明细可见 |
| 🟡 P1 | 优化2 涨停次日独立评估 | `screen_full.py` 新增 `limit_up_quality` + 输出字段 | 次日方向 +15% | M | 涨停股带 `limit_up_quality`，weak 票被预警 |
| 🟢 P2 | 优化5 涨停/非涨停分组 | `screen_full.py` 输出 + 报告模板 | UI 改进 | S | 报告分 🔴涨停组 / 🟡未封板组 两组展示 |
| 🟢 P2 | 优化6 MACD金叉天数调权 | `scoring.py` `compute_macd_auxiliary_score` 金叉确认项 | 细微提升 | S | 当日金叉给 7（原 4）、3~5日收紧到 5；**先过回测验证 IC 不下降** |
| 🟢 P3 | 第六章 回填机制 + 因子 IC | `screening_predictions` 流水线 + `core/scoring/outcomes.py` | 提供量化闭环 | M | 7天回填完成，`pred_correct` 可统计 |

> ⚠️ **改动定位铁律（来自 4.0）**：板块因子（优化1）、普跌降权（优化4）、涨停质量（优化2）这三项**绝不能写进 `scoring.py`/`scoring_risk.py` 的评分函数**——评分函数的 `indicators` 入参只有单只股票 K 线，拿不到行业/大盘数据。它们只能在 `scripts/screen_full.py` 这层（连着 `db/market.db`、能查行业和大盘）做后处理。只有优化3（5日涨幅，个股自有数据）和优化6（MACD，个股指标）能进评分函数。

> 📍 **进度（2026-05-30）**：P0/P1 全部 6 项（优化1~4 + 优化2 + 优化5）已开发完成并单测通过，T6 回填闭环代码就绪。详见第九章「实施进度」小节。仅优化6(T7，需回测)、分钟级(M1~M4) 未做。**实跑待 `market.db` 数据补齐到 05-29 后进行**。

### 总验收目标

- **自动预测次日方向准确率：34% → ≥50%**（P0+P1 上线后），中期逼近手动 60%。
- **保持评分选股能力不退化**：S/A 级 3 日收益梯度仍成立（S>A>B）。
- **建立可持续闭环**：每日预测自动回填 `pred_correct`，每周可出准确率趋势。

### 落地步骤建议

1. 先做 P0 两项（优化1 板块扣分 + 优化4 普跌降权），都在 `screen_full.py` 后处理层、查 DB 即可，1~2 天可上线。
2. 再做 P1 两项（优化3 短期过热扣分进 `scoring_risk.py`、优化2 涨停质量标签），数据字段都现成。
3. 同步搭第六章回填机制，否则上线后无法验证是否真提升。
4. 累积 2 周回填数据后，再决定 P2（含需回测的优化6）是否调参。
5. **始终保留人工终审**：评分只是粗筛，是否扣扳机由看图决定（与 `scoring_system_design.md` 第七章决策一致）。

---

## 八、分钟级复盘验证机制（新增方向，本次仅设计不实现）

> 📌 决策记录（2026-05-30）：数据源 = **同花顺（iFinD/网页接口）**；频率 = **1 分钟线**；覆盖范围 = **仅当日命中候选股 + 涨停股**（每天约 30~50 只）；目标 = **用次日盘中实际走势验证前一天的预测，反哺评分**；本次只写方案，代码后续再做。

### 8.0 为什么要做（直击 50% 的核心错误）

第二章已证明：**最大亏损来源是「涨停次日高开低走」（占错误 50%）**。而高开低走的根因是**日线根本看不见的盘中过程**。

一根涨停大阳线，日线 OHLC 四个数完全相同，背后却可能是两种相反的故事：

| 同样一根涨停日线 | 健康封板 | 烂板 / 盘中出货 |
|------------------|----------|-----------------|
| 封板时刻 | 早盘 9:35 秒板 | 尾盘 14:40 勉强封 |
| 开板次数 | 0 次，一字躺板 | 反复开板 3 次 |
| 封单 | 越买越厚 | 薄封单 |
| **次日大概率** | **溢价高开走强** | **高开低走收阴** |

**日线把这两种票画成几乎一样的 K 线，分钟级数据能一眼区分。** 这正是评分系统对「涨停次日方向」预测力只有 34%（不及抛硬币）的物理原因——信息根本不在日线里。

更重要的是你提的角度：**不是只拿分钟线多加几个因子，而是拿次日盘中真实走势，去验证前一天预测对不对，从而校准预测能力。** 这是给闭环补上「真正的标准答案」。

### 8.1 核心思路：把「标准答案」从一个点升级为一条路径

现有回填（`scoring.db` 的 `outcomes` 表）只记了 `t1_return`（次日收盘涨跌）和 `t1_is_green`（是否绿砖）——**标准答案是一个点（收盘价）**。

问题：同样次日收 +0.41%，可能是两种完全不同的过程：
- 高开 +3% 一路阴跌回落到 +0.41% → 实际是**高开低走**，预测「惯性高开」其实**错了**
- 低开 -2% 尾盘翻红到 +0.41% → 预测「惯性高开」**方向也没对**

**只用收盘价判分，等于用错误的标准答案给预测打分，越校准越偏。** 分钟级把标准答案从「一个点」升级为「一条路径」，才能给出路径级判定：开盘对不对、盘中走势对不对、尾盘对不对，错在哪一段一目了然。

> 一句话定位：**T 日选股评分 → T+1 盘中路径验证 → 错误模式回流 → 改进 T 日封板质量评分**，形成真闭环。这是对第六章「回填机制」的盘中级增强，不是另起炉灶。

### 8.2 分钟级衍生指标清单（不存原始分钟线，只存算好的）

> 原则：**原始 1min 数据用完即弃，只持久化算好的衍生指标**。原因：① 原始分钟线量大（每票 242 行/天）；② 真正有预测价值的是从分钟线提炼的「行为特征」，不是原始价量。

每只候选股次日盘中，从同花顺 1min 数据算出以下指标（按对「高开低走」的预测价值排序）：

| 指标 | 1min 算法 | 解读 |
|------|----------|------|
| **封板时刻** `seal_time` | 首次触及当日涨停价的分钟戳 | < 9:45 早盘秒板=强；> 14:30 尾盘封=弱 |
| **开板次数** `unseal_count` | 封板后又跌破涨停价的次数 | 0 次=一字躺板；≥2 次=主力出货 |
| **全天最高点时刻** `high_time` | 当日最高价出现的分钟戳 | 9:45 前见顶=诱多高开低走信号 |
| **收盘 vs 分时均价** `close_vs_vwap` | 收盘价 / 当日 VWAP - 1 | >0 收在均价线上=强；<0=诱多 |
| **尾盘 15 分钟涨跌** `tail_chg` | 14:45→15:00 涨跌幅 | 尾盘跳水=次日大概率低开 |
| **早盘量能占比** `morning_vol_pct` | 9:30~10:30 成交量 / 全天 | 早盘放量拉升=真突破；尾盘对倒=假 |
| **分时回撤幅度** `intraday_drawdown` | (最高价 - 收盘价) / 最高价 | 衡量「冲高回落」程度 |
| **是否炸板收阴** `is_failed_limit` | 盘中触涨停但收盘未封且收绿 | 最强的次日看空信号 |

**数据源已实测验证（2026-05-30）**：接口 `https://d.10jqka.com.cn/v6/time/hs_{symbol}/defer/last.js` 可作为稳定来源。

- **稳定性**：HTTP 200、0.4 秒返回、无需登录/cookie/referer，裸请求即可；`hs_` 前缀沪深通用（深市返回 `marketType:HS_stock_sz`、沪市 `HS_stock_sh`）。
- **返回结构**：JSONP 包裹（`quotebridge_v6_time_hs_xxx_defer_last({...})`），剥壳后取 `obj["hs_{symbol}"]`。顶层关键字段：`name`（股票名）、`pre`（昨收，算涨跌幅/涨停价的基准）、`date`（交易日）、`data`（分时序列）。
- **`data` 每行 5 列**（实测确认，非通用 OHLCV）：`时间, 现价, 当分钟成交额(元), 累计均价VWAP, 当分钟成交量(股)`。每天固定 242 个分钟点（含集合竞价收盘点）。
- **字段自洽性已验证**：宁波中百(600857) 全天 `Σ成交额 / Σ成交量 = 15.235`，与接口给的收盘 VWAP `15.234` 一致。

> 注意：该接口**不直接给 high/low/封单量**，分时回撤等需用「逐分钟现价的累计最高」近似当日最高；`close_vs_vwap` 反而**更省事**——接口直接给累计 VWAP，不用自己加权算。封单量拿不到，不影响 8.2 主干指标。

**接口验证后对 8.2 清单的两处调整**：

1. **`seal_time` 涨停价判定要用 `round(pre*1.1, 2)`**，不能简单用「现价涨幅 ≥ 9.5%」。实测晋控电力 `5.07×1.1=5.577→5.58` 才是真涨停价，用百分比阈值会误判封板时刻。
2. **新增 2 个该接口独有、近乎零成本的指标**：

| 新增指标 | 1min 算法 | 解读 |
|------|----------|------|
| **均价穿越次数** `vwap_cross_count` | 现价上下穿越累计 VWAP 的次数 | 反复穿越=多空分歧大、易高开低走 |
| **尾盘资金强度** `amount_weighted_late` | 用第3列「成交额」而非成交量算 14:00 后占比 | 比纯成交量更能反映尾盘大单意图 |

### 8.3 路径级判定：预测方向 × 真实分时路径 的比对规则

把预测 JSON 里的 `expected_direction` 与 8.2 算出的衍生指标做匹配，输出一个**路径级裁定** `intraday_verdict`，取代现在「只看收盘价」的粗糙判定。

判定先把真实分时路径归为 5 类「形态」（由衍生指标推导）：

| 路径形态 | 判定条件（衍生指标组合） |
|----------|------------------------|
| `真高开走强` | 高开 + `close_vs_vwap > 0` + `high_time` 在午后 |
| `高开低走` | 高开 + `high_time` < 9:45 + `close_vs_vwap < 0` + `intraday_drawdown` 大 |
| `低开翻红` | 低开 + 收盘翻红 + `tail_chg > 0` |
| `低开低走` | 低开 + `close_vs_vwap < 0` |
| `窄幅震荡` | 全天振幅小、`intraday_drawdown` 小 |

再用「预测方向 × 真实形态」查表，给出裁定：

| 预测方向 | 真实路径形态 | `intraday_verdict` | 说明 |
|----------|-------------|--------------------|------|
| 惯性高开 / 偏多 | 真高开走强 | ✅ 真对 | 方向与过程都对 |
| 惯性高开 / 偏多 | 高开低走 | ❌ 高开低走陷阱 | **现有人工 `review_*.json` 只看开/收盘，常把它记成「✅高开成立(但收跌)」或「⚠️偏差」**（如 05-27 review 里和顺石油即如此），路径级裁定能纠正 |
| 惯性高开 / 偏多 | 低开翻红 | ⚠️ 蒙对 | 结果对但开盘判错，不应记为高质量正确 |
| 高开低走 / 冲高回落 | 高开低走 | ✅ 真对 | 看空判断兑现 |
| 偏空 / 看空 | 低开低走 | ✅ 真对 | — |
| 任意偏多 | 低开低走 | ❌ 完全错 | — |

**关键收益**：现在的 34% 是「收盘价模糊准确率」。用路径级裁定重判后，能得到**真实准确率**，并且能把错误归因到具体环节（开盘错 / 盘中错 / 尾盘错），这是校准预测能力的前提。

### 8.4 数据落盘设计（同花顺 1min → 衍生指标表）

**不新建原始分钟线表**（用完即弃）。只在 `db/scoring.db` 新增一张衍生指标表 `intraday_review`，与现有 `outcomes`、`cross_section` 表并列。

> ⚠️ **必须遵循现有架构（已核实代码）**：`scoring.db` 的所有表统一由 `core/data/database.py` 里的 `ScoringDatabase._create_tables()` 用 `CREATE TABLE IF NOT EXISTS` 定义，读写通过封装方法（如 `save_outcomes` / `load_outcomes` / `save_cross_section`）完成，**不允许在脚本里裸 SQL 直连建表/写表**。新表要按同样模式接入。

**第一步**：在 `ScoringDatabase._create_tables()`（`core/data/database.py:299` 附近）仿 `outcomes` 表加一张 `intraday_review`，主键 `(review_date, symbol)`，字段分四组：

- **定位**：`review_date`(被复盘日=T+1)、`score_date`(预测日T)、`symbol`
- **8.2 衍生指标**：`seal_time` / `unseal_count` / `high_time` / `close_vs_vwap` / `tail_chg` / `morning_vol_pct` / `intraday_drawdown` / `is_failed_limit`
- **接口验证后新增**：`vwap_cross_count` / `amount_weighted_late`
- **8.3 路径裁定**：`path_shape` / `expected_direction` / `intraday_verdict`

（类型按 8.2/8.3 语义定：时间戳类 TEXT、计数类 INTEGER、比率类 REAL；建表 SQL 实现时照 `outcomes` 的写法填即可，此处不展开。）

**第二步**：给 `ScoringDatabase` 加两个封装方法（签名 + 思路）：

```python
def save_intraday_review(self, review_date: str, rows: list[dict]) -> None:
    """先 DELETE WHERE review_date=? 再 append 写入，仿 save_outcomes"""

def load_intraday_review(self, review_date: str) -> pd.DataFrame:
    """按 review_date 读出当日全部路径裁定，仿 load_outcomes"""
```

**数据源澄清（已核实，避免张冠李戴）**：当前 `output/` 实际跑通的是 `screen_full.py` → `screening_raw/{date}.json`（含命中候选股，每条有 `is_limit_up`）+ `screening_predictions/{date}.json`（含 `expected_direction`）。设计文档里规划的 `scoring_picks/` + `outcomes` 自动回填链路**目前尚未实际落地运行**（`output/scoring_picks/` 目录不存在）。因此分钟级复盘的**候选股来源应取 `screening_predictions/{T}.json` 的 `stocks[]`**（它已含 `symbol` + `is_limit_up` + `expected_direction`），而不是尚未启用的 `scoring_picks`。

**未来脚本职责**（`scripts/review_intraday.py`，本次不实现，仅约定）：
1. 读前一交易日 `output/screening_predictions/{T}.json` 的 `stocks[]`，取每只票的 `symbol` / `is_limit_up` / `expected_direction`
2. 逐票请求 `https://d.10jqka.com.cn/v6/time/hs_{symbol}/defer/last.js`（仅几十只，每只 ~7KB、0.4s），剥 JSONP 取 `obj["hs_{symbol}"]`，解析 `data` 的 5 列分时
3. 算 8.2 衍生指标（注意 `seal_time` 用 `round(pre*1.1,2)` 判涨停价）→ 推 8.3 `path_shape` → 查表得 `intraday_verdict`
4. 通过 `ScoringDatabase.save_intraday_review()` 写入 `intraday_review` 表；同时回写 `output/screening_predictions/{T}.json` 给对应 stock 增加 `intraday_verdict` 字段
5. 原始 5 列分时数据不落盘

> ⚠️ **接口只返回「最新交易日」**（接口名即 `last.js`，实测 `date=20260529` 为最近一个交易日，无法指定历史某天），所以**必须每天收盘后当天跑**，错过当天就再也补不回那天的分时——这一点比"时间窗限制"更硬，决定了它必须挂进每日收盘后 cron。好处是：这是 `d.10jqka.com.cn` 的**免费 web 接口，不需要 iFinD SDK / 积分 / 登录**，零接入成本。

**📌 存储决策（2026-05-30 拍板）：只存衍生指标，原始 1min 不存。**

| 项目 | 处理 |
|------|------|
| 衍生指标（8.2 的 8 个 + 接口验证新增 2 个 = 10 个字段）+ 路径裁定（8.3） | **永久存** 进 `intraday_review` 表 |
| 原始 1min 分时数据（每票 242 行/天） | **算完衍生指标即丢弃，不落盘** |

- **理由**：真正有预测价值的是从分钟线提炼的「行为特征」（封板时刻、开板次数、路径形态），不是原始价量；衍生指标一张表几年也只有几 MB，零负担。
- **已知代价（明确接受）**：若将来想到新的衍生指标，因原始 1min 已丢弃且接口只返回最新交易日（无法取历史分时），**老数据无法回溯重算**——新指标只能从启用之日起向前积累。
- **缓解做法**：8.2 的指标清单一次性尽量取全（宁可先算了存着、暂时不用，也比事后补不回来强）；若某段时间确实需要保留原始数据做专项研究，再临时按日导出 parquet，研究完删除，不进常规库。

### 8.5 闭环：分钟级标签如何反哺评分

分钟级复盘的价值不止于「打分更准」，而是把积累的路径标签**回流到 T 日评分**，让选股端越来越能避开次日高开低走的票。三条回流路径：

1. **校准 `limit_up_quality` 阈值（优化2）**
   优化2 当前用 `vol_ratio / brick / ind_perf / cum_chg_5d` 拍脑袋判 strong/weak。积累 2~3 周分钟级标签后，可统计「哪些 T 日特征 → T+1 高开低走概率最高」，用真实数据替换拍脑袋阈值。例如发现「`vol_ratio > 3` 的涨停股 T+1 高开低走率 70%」，就把判 weak 的量比阈值从 3 下调。

2. **新增「封板质量」前瞻因子（需 T 日当天分钟线，进阶）**
   8.2 的指标算的是 T+1 的盘中。若把同一套指标用在 **T 日当天**的涨停股上（封板时刻、开板次数），就得到「这只涨停今天封得好不好」，这才是直接预测 T+1 的前瞻因子。这一步需要**对当日涨停股也拉分钟线**（仍在「候选股+涨停股」范围内，量不大）。

3. **量化你的交易纪律**
   你手动预测反复写的「高开 >4% 不追」「若低开则偏多失效」，用分钟级数据可回测：严格执行 vs 不执行，收益差多少。把直觉变成有数据支撑的规则，再决定要不要写进系统的操作建议字段。

> 回流顺序：先做 8.3 路径裁定（纯验证，不动评分，零风险）→ 积累数据 → 校准优化2 阈值 → 最后才考虑加 T 日封板质量前瞻因子。**先验证、后反哺，不要一上来就改评分。**

### 8.6 实施分期 & 与现有方案的关系

分钟级方案是**第六章回填机制的盘中级增强**，不替代前七章任何内容。分期推进：

| 阶段 | 内容 | 改动 | 风险 |
|:----:|------|------|------|
| **M1 验证层** | 写 `review_intraday.py`：拉分钟线→算衍生指标→路径裁定→写 `intraday_review` 表 + 回填预测JSON | 新增脚本/表，**完全不动评分** | 零（只读验证）|
| **M2 真实准确率** | 用 `intraday_verdict` 重算自动/手动预测的真实准确率，定位错误环节 | 纯统计 | 零 |
| **M3 校准优化2** | 用积累的路径标签校准 `limit_up_quality` 判强/判弱阈值 | 调 `screen_full.py` 阈值常量 | 低 |
| **M4 前瞻因子** | 对 T 日涨停股拉分钟线，加「封板质量」前瞻因子 | 进评分/后处理层 | 中，需回测 |

**与前七章的优先级关系**：
- 前七章的 P0/P1（板块扣分、普跌降权、短期过热、涨停质量标签）**优先级更高**，因为它们零成本（数据现成）、立即可上线。
- 分钟级方案 M1/M2 **价值最高但有数据接入成本**（同花顺接口对接），建议在前七章 P0/P1 上线、跑通日级闭环之后再启动。
- M3/M4 必须等 M1/M2 积累 2~3 周数据后才有意义。

### 8.7 风险与注意事项

| 风险 | 说明 | 应对 |
|------|------|------|
| **接口只返回最新交易日** | 该接口名 `last.js`，只给最近一个交易日的分时，无法指定历史某天（见 8.4 实测） | 必须挂每日收盘后 cron 当天跑，错过即永久缺失，绝不依赖事后补数 |
| **接口频率/稳定性** | 网页接口可能限流、字段不稳定 | 只拉候选股+涨停股（几十只），单次量小；加重试与失败记录，缺数据的票标 `NULL` 不阻塞 |
| **样本量小** | 每天几十只，校准阈值需时间 | M3 反哺前至少累积 2~3 周（≥300 条路径标签）才下结论，count<30 的分组不调参 |
| **过拟合盘中特征** | 用少量样本归纳「9:45 见顶必跌」易过拟合 | 路径裁定规则保持简单、可解释；阈值调整必须留出验证集 |
| **时间戳/停牌对齐** | 1min 数据可能缺早盘集合竞价或有停牌段 | 衍生指标计算对缺失分钟做容错，停牌票直接跳过 |
| **不要本末倒置** | 分钟级是验证工具，不是选股主力 | 选股主干仍是日线砖形图评分；分钟级只服务「次日方向验证 + 涨停质量校准」 |

> **一句话总结第八章**：分钟级 1min 数据最大的价值不是多加因子，而是**给预测提供「路径级标准答案」**，让你第一次能看清 34% 准确率到底错在开盘、盘中还是尾盘，进而精准校准评分系统对「涨停次日高开低走」这一最大错误源（50%）的判断。先做零风险的验证层（M1/M2），再谈反哺评分（M3/M4）。

---

## 九、开发任务拆分

按依赖关系和优先级分 5 个 Sprint。每个任务标注：改动文件、依赖前置、验收标准、预估工时。**严格按 Sprint 顺序推进，同 Sprint 内任务可并行。**

---

### 📍 实施进度（2026-05-30 更新）

> 本节记录已落地的开发，与下方任务表对应。✅=已完成并单测通过，⬜=未开始。

| 任务 | 状态 | 实际改动 | 单测 |
|------|:----:|----------|:----:|
| T0 industry 前置 | ✅ | `screen_full.py`：取列表 SQL 补 `sl.industry`，循环变量 `(symbol,name,industry)`，`results.append` 补 `industry` | — |
| T1 板块动量衰减 | ✅ | 函数抽到**新模块** `core/screening/brick_pattern/postprocess.py`（`build_industry_perf`/`sector_penalty`），`screen_full.py` import 后循环内 `score=max(0,final_score+sec_pen)`，补 `sector_penalty`/`sector_flags` | ✅ 已验证 |
| T2 普跌日降权 | ✅ | `screen_full.py` 排序前查 `market_avg`，普跌日(<-1%)全体 `score×0.85` 标 `regime_flag`，header 写 `market_avg`/`is_bearish_day`。顺序：先 T1 板块扣分→后 T2 打折 | — |
| T3 短期过热 | ✅ | `scoring_risk.py` 新增 `_check_overheat`（5日>25%扣-15、≥15%扣-5），`compute_risk_penalty` 注册，受 -30 下限保护 | ✅ 已验证 |
| T4 涨停质量 | ✅ | `postprocess.py` 新增 `limit_up_quality`（strong/weak/neutral），`screen_full.py` 对涨停股算标签+`cum_chg_5d`，不进 score | ✅ 已验证 |
| T5 分组展示 | ✅ | `screen_full.py` 每条打 `group`(limit_up/strong_unsealed/normal)，json 增 `group_counts`，Top 打印按 🔴涨停组(带质量预警)/🟡未封板组/⚪普通组 | 边界 6 例 |
| T6 回填闭环 | ✅ | **未复用 `outcomes.py`**（其依赖的 `scoring_picks/` 链路未启用）。新建 `core/scoring/prediction_review.py`(纯函数)+`scripts/backfill_predictions.py`(回填)+`scripts/prediction_accuracy.py`(自动vs手动统计)。回填字段：`open_chg`/`day_chg`/`open_correct`/`close_correct`/`sector_reverted`/`direction_norm` | ✅ 已验证 |
| T7 MACD金叉调权 | ✅ | 回测验证通过后改 `scoring.py` 金叉确认项：修正「当日金叉仅 4 分、1~5 日 7 分」倒挂 → 当日/近2日金叉 7 分、3~5 日衰减到 5 分。回测脚本验证后已删（一次性验证，结论见下文 T7 段） | ✅ 已验证 |
| M1 分钟级验证脚本 | ✅ | 新建 `core/scoring/intraday_metrics.py`(8.2 十指标+8.3 五形态+裁定查表)、`database.py` 加 `intraday_review` 表+`save/load_intraday_review`、`scripts/review_intraday.py`(拉同花顺接口+剥JSONP+落库+回写json，含 date 错配校验)。**完全不动评分，零风险** | ✅ 已验证 |
| M2 真实准确率重算 | ✅ | `intraday_metrics.py` 加 `aggregate_intraday_verdicts`(真对率+蒙对单列+错误环节归因)、新建 `scripts/intraday_accuracy.py`(读表重算+对比旧收盘价口径) | ✅ 已验证 |
| M3/M4 分钟级反哺 | ⬜ | 需积累 2~3 周(≥300条)路径标签，现做不了 | — |

**M1/M2 实跑结果（2026-05-30，复盘 05-28 预测 → 05-29 盘中）**：

- **M1 落库**：05-28 预测 16 只 → 拉同花顺 05-29 分时，14 只有效落库 `intraday_review` 表（2 只 SSL 偶发失败降级为「无分时数据」，不阻塞）。**实跑即抓出 9 只 `high_open_low_close`（高开低走）**——直接坐实第二章「高开低走占主要错误」。
- **M2 真实准确率**（路径级裁定，05-29 共 11 只可裁定）：

  | 口径 | 准确率 | 说明 |
  |------|:------:|------|
  | **真实准确率(路径级)** | **27.3%** (3/11) | 方向+过程都对 |
  | 宽口径(含蒙对≈旧收盘价口径) | 36.4% | 旧口径高估 1 只蒙对 |

  **错误环节归因**：盘中/尾盘跳水 **5 只**（高开低走陷阱）、开盘判错 2 只。**首次把准确率拆到错误环节**：7 个错误里 5 个是高开低走，精准印证第二章「高开低走占 50%」，且全自动可追踪。
- **结论印证**：「真对 27.3% vs 宽口径 36.4%」实测验证 8.3 论点——只看收盘价会高估准确率。M3/M4 需在此基础上积累 2~3 周路径标签后才能反哺评分。

**⏰ 定时部署（已配置，2026-05-30）**：新增 `scripts/daily_intraday_review.sh`，每交易日收盘后自动跑 M1 复盘落库 + M2 准确率统计。

- **为什么必须每天当天跑**：同花顺 `last.js` 只返回最新交易日分时，错过即永久缺失（见 8.7），所以挂收盘后 cron 是硬要求，非可选。
- **脚本逻辑**：激活 `.venv` → 判交易日（`market.db` 最新日期≠今天则跳过，自动规避周末/节假日）→ 跑 `review_intraday.py` → 跑 `intraday_accuracy.py`，全程写日志。
- **crontab 配置**（生产服务器 `/opt/data/workspace/trader`，周一~五 15:30）：
  ```
  30 15 * * 1-5 /opt/data/workspace/trader/scripts/daily_intraday_review.sh >> /opt/data/output/logs/intraday_review.log 2>&1
  ```
- **下一步**：挂上后开始积累路径标签，约 2~3 周（≥300 条）即可启动 M3（校准 `limit_up_quality` 阈值）。

**🔬 T7 MACD 金叉调权回测（2026-05-30，文档铁律：IC 不下降才能改评分主干）**：新增 `scripts/backtest_macd_golden.py`，用 market.db 全历史（2010~2026，800 只采样、**236 万样本**）验证调权方向。

- **发现的倒挂**：原 `scoring.py` 金叉确认项给「当日金叉(golden_dist=0)」**仅 4 分**，而「1~5 日金叉」给 **7 分** —— 直觉上当日刚金叉才是最强信号，存在逻辑倒挂。
- **① 分桶验证（golden_dist → T+1 收益）**：

  | 分桶 | 样本数 | 平均 T+1 收益 | 胜率 |
  |------|------:|------:|----:|
  | **当日金叉(0)** | 9.1万 | **+0.147%** | 47.1% |
  | 近2日(1-2) | 18万 | +0.007% | 46.7% |
  | 3-5日 | 26万 | **-0.013%** | 47.0% |

  当日金叉 T+1 收益显著高于 3~5 日 → **坐实倒挂，当日金叉应给最高分**。
- **② IC 对比**：旧映射 IC=-0.01316 → 新映射 IC=-0.01253，**Δ=+0.00063（不降反升）**，通过上线门槛。
- **代码变更**：`scoring.py` 金叉确认项改为「当日/近2日金叉 → 7 分，3~5 日 → 5 分，其余回退不变」。回测脚本里的 `score_golden_new` 与线上改后逻辑严格一致，已单测锁定。

**测试说明**：各任务上线前均已编写针对新增纯函数的单测并跑通验证（覆盖 T1/T3/T4/T6/T7/M1/M2）。验证完成后这批一次性单测脚本已清理，核心链路代码与生产脚本完整保留。

**关键实施偏离与原因**：
1. **T1/T4 抽出独立模块 `postprocess.py`**：文档原计划函数内联在 `screen_full.py`，但脚本顶层连 DB 无法被单测 import，故抽成纯模块，既可测又复用。`screen_full.py` 改为 `from core.screening.brick_pattern.postprocess import ...`。
2. **T6 没改 `outcomes.py`**：核实发现 `outcomes.py` 读的 `output/scoring_picks/{date}.json` 目录**不存在**（与第八章判断一致），实际跑通的是 `screening_predictions/`。故 T6 针对 `screening_predictions` 新建回填链路，`pred_correct` 按决策拆成 `open_correct`+`close_correct` 双字段。
3. **`expected_direction` 归类已用真实 28 种取值验证**：看跌词(低走/回落/偏空…)优先于看涨词，"高开低走"正确判 bear，"高开后回踩确认再上"正确判 bull。
4. **报告 `.md` 无代码模板**：`screening_analysis/*.md` 是 Agent 手写、非 Python 生成，故 T5「报告模板」落在 json 分组字段 + stdout 分组打印。

**✅ 实跑完成（2026-05-30，数据已补齐到 05-29）**：`db/market.db` 现覆盖 05-22~05-29 共 5 个交易日。已实跑 T6 回填 + 准确率统计：

- **回填**：自动预测 05-22~05-28 共 47 只、手动预测 52 只全部成功写入 T+1 行情（`open_chg`/`day_chg`/`open_correct`/`close_correct`/`sector_reverted`/`direction_norm` 已写回各 json）。05-29 因无 06-01 次日数据正常跳过。

- **准确率基线**（机器严格判定，仅统计已回填且非中性样本）：

  | 指标 | 自动 | 手动 |
  |------|:----:|:----:|
  | 开盘方向准确率 | **56.8%** (25/44) | 55.0% (11/20) |
  | 收盘方向准确率 | **50.0%** (22/44) | 40.0% (8/20) |

- **⚠️ 口径说明（重要，避免与第二章数字混淆）**：
  1. 第二章的 34%/60% 是 Agent 在 .md 报告里**人工逐案判定**（含"高开成立但收跌"等模糊归类）；本次是**机器按 `expected_direction` 归一化 + 开/收盘涨跌符号**严格判定，两套口径不可直接比。**机器口径才是后续可持续追踪的基线**。
  2. 手动「中性」高达 33 只（大量"谨慎观察/中性"不表态措辞被正确剔除），有效样本仅 20 只，**手动 40% 样本太小不具统计意义**。
  3. 这批 json 是 **P0/P1 优化前生成的存量预测**，故 50%/56.8% 是**优化前基线**，非优化效果。验证 P0/P1 提升需用优化后新预测对比。

- **复跑命令**（数据更新后）：`python scripts/backfill_predictions.py` + `python scripts/prediction_accuracy.py`。

---

### 任务依赖总览

```
Sprint 0（前置）─┬─→ Sprint 1（P0 后处理）─┬─→ Sprint 3（验证闭环）
  T0 补industry  │     T1 板块扣分          │     T6 回填+准确率
                 │     T2 普跌降权          │
                 └─→ Sprint 2（P1 评分内）  │
                       T3 短期过热          │
                       T4 涨停质量 ─────────┘
                 Sprint 2b（P2 低优先）
                       T5 分组展示  T7 MACD金叉调权（需回测）

Sprint 4（分钟级，独立线）M1→M2→M3→M4，建议 Sprint 3 后启动
```

> **铁律**：T1/T4 依赖 T0（无 industry 字段无法做板块/涨停质量判断）；T1 与 T2 都改 `score`，T1 在循环内先扣、T2 在排序前后扣；T7 必须先回测才能上线。

---

### Sprint 0：前置改动（解锁后续所有板块相关任务）

| 任务 | T0 |
|------|------|
| **目标** | 给扫描结果补上 `industry` 字段 |
| **改动文件** | `scripts/screen_full.py` |
| **具体改动** | ① 取列表 SQL（line 28~32）改为 `SELECT sl.symbol, sl.name, sl.industry FROM stock_list sl ...`；② 循环变量改 `for idx, (symbol, name, industry) in ...`；③ `results.append({...})` 补 `"industry": industry,` |
| **依赖** | 无 |
| **验收** | `screening_raw/{date}.json` 每条记录含非空 `industry` 字段 |
| **工时** | 0.5 天 |

---

### Sprint 1：P0 后处理层优化（挽回 50% 错误中的板块+普跌部分）

| 任务 | T1 — 板块动量衰减扣分 |
|------|------|
| **目标** | 昨日领涨板块次日 V 反、逆板块涨停的票自动降分 |
| **改动文件** | `scripts/screen_full.py` |
| **具体改动** | ① 新增 `build_industry_perf(conn, target_date, prev_date)` 构建行业今日/昨日均涨 + 昨日Top3；② 新增 `sector_penalty(industry, is_limit_up, ...)` 返回扣分+flags；③ 循环内对每只命中股算 `sec_pen`，`score = round(max(0, match.final_score + sec_pen), 1)`，并补 `sector_penalty` / `sector_flags` 字段 |
| **依赖** | T0 |
| **验收** | 玻璃类 V 反案例 score 被扣 -6~-9；逆板块涨停扣 -8；输出含 `sector_flags` |
| **工时** | 1 天 |

| 任务 | T2 — 普跌日自动降权 |
|------|------|
| **目标** | 全市场普跌日（均涨 < -1%）整体降低置信度 |
| **改动文件** | `scripts/screen_full.py` |
| **具体改动** | ① 扫描结束、`results.sort` 之前查 `market_avg`；② `is_bearish_day = market_avg < -1.0`；③ 若普跌，对所有 `r["score"] *= 0.85` 并标 `regime_flag`；④ `market_avg`/`is_bearish_day` 写进输出 json header |
| **依赖** | 无（但与 T1 同改 `score`，**必须在 T1 之后执行**：先板块扣分再整体打折，各执行一次） |
| **验收** | 05-27/05-29 普跌日全体 score 降档；header 含 `is_bearish_day` |
| **工时** | 0.5 天 |

---

### Sprint 2：P1 优化（短期过热进评分函数 + 涨停质量标签）

| 任务 | T3 — 短期过热因子 |
|------|------|
| **目标** | 5 日累计涨幅过大的票扣分，避免追末端 |
| **改动文件** | `core/screening/brick_pattern/scoring_risk.py` |
| **具体改动** | ① 新增 `_check_overheat(close, index)`：5日涨>25% 扣 -15、≥15% 扣 -5，复用 `RiskFilterType.CHASE_HIGH` 枚举；② 在 `compute_risk_penalty` 的 `_check_chase_high_pullback`（line 420）后注册 `_record(_check_overheat(close, index))` |
| **依赖** | 无（个股数据自洽，唯一能进评分函数的板块/普跌外优化） |
| **验收** | 5日涨>15% 扣-5、>25% 扣-15，`r_items` 明细可见；仍受 -30 扣分下限保护 |
| **工时** | 0.5 天 |

| 任务 | T4 — 涨停次日独立评估标签 |
|------|------|
| **目标** | 给涨停股单独算 `limit_up_quality`（strong/weak/neutral），不进 score 只做预警 |
| **改动文件** | `scripts/screen_full.py` |
| **具体改动** | ① 新增 `limit_up_quality(vol_ratio, brick_val, ind_today_chg, cum_chg_5d)`；② 循环内对 `is_limit_up` 的票算标签 + `cum_chg_5d`，写入输出字段；③ 报告对 weak 标「⚠️ 次日防高开低走」 |
| **依赖** | T0（需 industry 算 `ind_today_chg`）；可与 T3 并行 |
| **验收** | 涨停股带 `limit_up_quality` + `cum_chg_5d`；weak 票被预警 |
| **工时** | 1 天 |

---

### Sprint 2b：P2 低优先（展示优化 + 需回测的调权）

| 任务 | T5 — 涨停/非涨停分组展示 |
|------|------|
| **目标** | 报告 Top 表按 `is_limit_up` 拆 🔴涨停组 / 🟡未封板组 |
| **改动文件** | `scripts/screen_full.py` 输出 + 报告模板 |
| **具体改动** | 输出 json 增加分组标记；报告分两组渲染，涨停组次日方向看 T4 的 `limit_up_quality` |
| **依赖** | T4（涨停组依赖质量标签） |
| **验收** | 报告分两组展示 |
| **工时** | 0.5 天 |

| 任务 | T7 — MACD 金叉天数调权 |
|------|------|
| **目标** | 当日金叉给满分、延续期收紧 |
| **改动文件** | `core/screening/brick_pattern/scoring.py` |
| **具体改动** | 改 `compute_macd_auxiliary_score` 金叉确认项（line 274~284）：`golden_dist==0` → 7（原4）、`1~2` → 7、`3~5` → 5（原一律7）。注意 `lookback=5`，无 `>15` 取值 |
| **依赖** | 无 |
| **验收** | **先跑回测验证 IC 不下降**才能上线；当日金叉得 7 分 |
| **工时** | 0.5 天 + 回测 |

> ⚠️ T7 动评分主干，是全部任务里唯一**必须先回测**的，不回测不上线。

---

### Sprint 3：验证闭环（量化优化是否真有效，前置于所有调参）

| 任务 | T6 — 预测正确率回填 + 准确率统计 |
|------|------|
| **目标** | 每日自动回填 `pred_correct` 等字段，能算优化前后准确率 |
| **改动文件** | `screening_predictions` 流水线 + `core/scoring/outcomes.py` |
| **具体改动** | ① `screening_predictions/{date}.json` 回填 `open_chg`/`day_chg`/`pred_correct`/`limit_up_quality`/`sector_reverted`；② 回填历史 7 天；③ 准确率统计脚本（自动 vs 手动） |
| **依赖** | T1/T2/T4（回填字段引用其产出）；**是 Sprint 4 与所有调参的前提** |
| **验收** | 7 天回填完成，`pred_correct` 可统计；准确率目标 34% → ≥50% |
| **工时** | 2 天 |

---

### Sprint 4：分钟级复盘验证（独立线，建议 Sprint 3 之后启动）

严格按 M1→M2→M3→M4 顺序，后者依赖前者积累的数据。

| 任务 | M1 — 分钟级验证脚本 |
|------|------|
| **目标** | 拉候选股次日分时 → 算衍生指标 → 路径裁定 → 落库 |
| **改动文件** | 新增 `scripts/review_intraday.py` + `core/data/database.py` |
| **具体改动** | ① `database.py` 的 `ScoringDatabase._create_tables`（line 299）加 `intraday_review` 表（10 个衍生字段+3 个裁定字段，仿 outcomes）；加 `save_intraday_review`/`load_intraday_review`；② `review_intraday.py`：读 `screening_predictions/{T}.json` 的 `stocks[]` → 请求 `d.10jqka.com.cn` 接口 → 剥 JSONP 解析 5 列 → 算 8.2 指标（`seal_time` 用 `round(pre*1.1,2)`）→ 推 8.3 `path_shape` → 查表得 `intraday_verdict` → 落库 + 回写预测 json；原始分时不落盘 |
| **依赖** | T6（候选股来源 + 预测 json 结构） |
| **验收** | 每日候选股的 `intraday_review` 表有路径裁定；**完全不动评分**，零风险 |
| **工时** | 3 天 |

| 任务 | M2 — 真实准确率重算 |
|------|------|
| **目标** | 用 `intraday_verdict` 替代「只看收盘价」，得真实准确率 + 错误环节归因 |
| **改动文件** | 统计脚本（基于 M1 落库数据） |
| **具体改动** | 按 ✅真对/⚠️蒙对/❌陷阱 重算准确率，定位错误集中在开盘/盘中/尾盘 |
| **依赖** | M1 |
| **验收** | 输出真实准确率 + 错误环节分布；纯统计零风险 |
| **工时** | 1 天 |

| 任务 | M3 — 校准涨停质量阈值 |
|------|------|
| **目标** | 用积累的路径标签替换 T4 `limit_up_quality` 的拍脑袋阈值 |
| **改动文件** | `scripts/screen_full.py` 阈值常量 |
| **具体改动** | 统计「哪些 T 日特征 → T+1 高开低走率最高」，回调 `limit_up_quality` 的 vol_ratio/brick 等阈值 |
| **依赖** | M1/M2 积累 ≥2~3 周（≥300 条标签），count<30 分组不调 |
| **验收** | 阈值有数据支撑；优化2 weak 判定命中率提升 |
| **工时** | 1 天（需等数据） |

| 任务 | M4 — 封板质量前瞻因子 |
|------|------|
| **目标** | 对 T 日当天涨停股拉分时，加「今天封得好不好」前瞻因子 |
| **改动文件** | 评分/后处理层 |
| **具体改动** | 把 8.2 指标用在 T 日涨停股，得封板质量，作为预测 T+1 的前瞻因子 |
| **依赖** | M3 完成 + 回测验证 |
| **验收** | 前瞻因子对 T+1 方向有正向 IC（需回测） |
| **工时** | 2~3 天 |

---

### 工时汇总 & 推进节奏

| Sprint | 任务 | 工时 | 可并行 | 上线条件 |
|:------:|------|:----:|:------:|----------|
| S0 | T0 industry 前置 | 0.5d | — | — |
| S1 | T1 板块扣分 + T2 普跌降权 | 1.5d | T1→T2 有序 | S0 完成 |
| S2 | T3 短期过热 + T4 涨停质量 | 1.5d | 可并行 | S0 完成 |
| S2b | T5 分组展示 + T7 MACD调权 | 1d | T7 需回测 | T4 / 回测通过 |
| S3 | T6 回填闭环 | 2d | — | S1/S2 完成 |
| S4 | M1~M4 分钟级 | 7~8d | 严格串行 | S3 之后 |

- **第一周**：S0 + S1 + S2（P0/P1 全部上线，约 3.5 天），剩余时间搭 S3 回填。
- **第二周**：S3 跑满 7 天回填出准确率趋势；并行启动 S4 M1。
- **2~3 周后**：数据够了再做 S2b 的 T7 回测调权、S4 的 M3/M4。

> **核心节奏原则**：先上零成本的 P0/P1（S0~S2）→ 立刻搭 S3 验证闭环（否则不知优化是否真有效）→ 数据积累够了再碰需回测/需样本的 T7、M3、M4。**先验证、后调参，不要在没有闭环数据时盲目动评分主干。**
