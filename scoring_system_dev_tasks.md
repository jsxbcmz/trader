# 主板评分系统开发任务拆解

> 状态：开发文档（v1，待 review）
> 基于：[scoring_system_design.md](scoring_system_design.md)
> 创建：2026-05-16
> 范围：从现有 V4 砖形图评分（`core/screening/brick_pattern/`）扩展为主板全量评分 + 实盘回填 + 因子健康度报告 + 每日诊断面板

---

## 〇、开发前需用户拍板的决策点

| 决策项 | 影响任务 | 备选 | 当前建议 |
|--------|----------|------|----------|
| **TopK 的 K** | P0-4 | 10 / 15 / 20 | **K=20**（已拍板） |
| **是否对未命中三定式的票评分** | P0-2 | (a) 只评定式命中 (b) 全主板都打分 | (a) 只评命中 |
| **每日评分触发时机** | P0-7 | (a) 收盘后自动 (b) 手动按钮 | **(b) 手动按钮**（已拍板）|
| **诊断面板放哪里** | P0-8 | (a) `stats_page` 加 tab (b) 新建 `scoring_page` | **(b) 新建 `ScoringPage` 独立页面**（已拍板）|

> 说明：原"阶段 0 是否包含硬过滤"决策项已**取消** — 经讨论后确认硬过滤层不需要独立存在（详见 [P0-2 任务说明](#p0-2把现有-brick_pattern-评分扩展到主板全量)）：
> - 当日停牌 → 归入 P0-2 的数据加载阶段健壮性检查
> - N 型下跌段 → 下推到 `detect_n_shape_jump` 内部硬剔除
> - 黄线之下 → 与必备前提"短趋 > 多空"语义重合，删除
> - 老庄股 → 推迟到 P3

> 没拍板前，下文涉及这些决策的字段按"当前建议"列编写，进入开发时再确认。

---

## 一、总览

### 1.1 四阶段路线

| 阶段 | 名称 | 目标 | 完成标志 |
|------|------|------|----------|
| **P0** | 基础设施 | 全主板每日评分 + 三窗口实盘回填 + 最小闭环 | 能拉出"过去 60 天每日 TopK 实际表现"|
| **P1** | 截面归一化 | 绝对阈值改成当日截面分位 | S 级胜率跨年差异显著缩小 |
| **P2** | 因子健康度报告 | IC / 单调性 / 分箱收益曲线，月度自动 | 用户每月 30 分钟看报告就能决定调权 |
| **P3** | OAMV 阶段化 + 战法因子补缺 | 多/空头分组训练 + 红柱比/地量等加分项 | 评分图形与人工选股重合度 > 70% |

### 1.2 依赖关系（关键路径）

```
[P0-8 ScoringPage(含 P0-7 "运行今日评分"按钮)]
                ↓ 按下后触发
P0-1 主板池过滤 ──→ P0-2 评分扩到全主板 ──┬─→ P0-3 落盘 scoring_daily ──┐
                                          ├─→ P0-4 落盘 scoring_picks ──┤
                                          └─→ P0-5 三窗口回填 ──────────┤
                                                                          ↓
                                                              结果显示回 P0-8

P0 完整 ──→ P1 截面归一化 ──→ P2 因子健康度报告 ──┐
                                                  ├──→ P3 战法因子 + 阶段化
P0-5 完整 ──→ P3-1 OAMV 阶段标签 ─────────────────┘
```

**说明**：P0-7 是 P0-8 页面里的一个按钮，按下后串行触发 P0-1 → P0-2 → P0-3/4/5；结果产出后 P0-8 读文件刷新显示。开发顺序见 §九 末尾的"开发顺序建议"。

P0-3 / P0-4 在 P0-2 完成后可并行；P0-5 用同一份指标缓存但读的是回填窗口，逻辑独立可并行。

### 1.3 新增文件清单

| 路径 | 用途 | 阶段 |
|------|------|------|
| `core/scoring/__init__.py` | 主板评分模块入口 | P0 |
| `core/scoring/main_board_pool.py` | 主板股票池过滤 | P0 |
| `core/scoring/engine.py` | 主板评分调度（包装现有 brick_pattern + 扩展） | P0 |
| `core/screening/brick_pattern/detectors.py` | **修改** `detect_n_shape_jump`：加 N 型下跌段硬剔除（用现有 `_is_in_n_shape_decline`）| P0（含在 P0-2）|
| `core/scoring/storage.py` | scoring_daily / picks / outcomes / regime 读写 | P0 |
| `core/scoring/outcomes.py` | T+1/T+2/T+3 回填逻辑 | P0 |
| `core/scoring/cross_section.py` | 截面分位计算 + 缓存 | P1 |
| `core/scoring/factor_health.py` | IC / 单调性 / 分箱收益 | P2 |
| `core/scoring/regime.py` | OAMV 阶段标签 + 3 日平滑 | P3 |
| `core/scoring/regime_training.py` | 按阶段分组累积训练集 | P3 |
| `core/screening/brick_pattern/helpers.py` | 补充：红绿砖累计长度比、地量判定 | P3（扩展现有文件）|
| `core/screening/brick_pattern/scoring.py` | 补充：加分因子 + 阶段权重切换 | P3（扩展现有文件）|
| `core/screening/brick_pattern/scoring_risk.py` | 补充：第三浪末端扣分 | P3（扩展现有文件）|
| `app/pages/scoring_page.py` | **新建** 评分诊断页（独立顶级页面）| P0 + P2 + P3 |
| `app/pages/__init__.py` | 导出 `ScoringPage` | P0（含在 P0-8）|
| `app/main_window.py` | 注册 `ScoringPage` 到左侧导航 | P0（含在 P0-8）|

### 1.4 新增数据落盘

| 路径 | 内容 | 频率 | 阶段 |
|------|------|------|------|
| `output/scoring_daily/{YYYY-MM-DD}.json` | 当日命中候选 + 每个子项得分 | 每日 1 次 | P0 |
| `output/scoring_picks/{YYYY-MM-DD}.json` | 当日 TopK 候选 + 评分明细（人类可读）| 每日 1 次 | P0 |
| `output/scoring_outcomes/{YYYY-MM-DD}.csv` | T+1/T+2/T+3 每日收益率 + 每日是否绿砖 | T+1 起逐日补 | P0 |
| `output/scoring_regime/{YYYY-MM-DD}.json` | OAMV 阶段标签 + 短/长斜率 + 平滑状态 | 每日 1 次 | P3 |
| `output/scoring_factor_health/{YYYY-MM}.json` | 月度 IC 报告（按多/空头分组）| 每月 1 次 | P2 |

> **存储格式说明**：原设计文档计划用 parquet，实际实施按数据结构选型 — 含嵌套字段的用 **JSON**（`scoring_daily` 的 items、`scoring_picks` 的 breakdown），扁平时序的用 **CSV**（`scoring_outcomes`，便于 P2 阶段 IC 计算多日 `pd.concat` 聚合）。理由：① 避免引入 pyarrow / fastparquet 新依赖；② 与项目惯例对齐（`output/*.json` + `*_data/*.csv`）；③ 单文件数据量小（每天 < 200 条），性能差异可忽略。

---

## 二、阶段 0：基础设施

> **完成标志**：能拉出"过去 60 天每日 TopK 实际表现"——三窗口收益 + 是否红转绿都齐全。

### P0-1：主板股票池过滤器

| 项 | 内容 |
|----|------|
| **目标** | 从 `stocklist.csv` 筛出主板候选池（剔创业板/科创板/北交所/ST）|
| **新增文件** | `core/scoring/main_board_pool.py` |
| **输入** | `stocklist.csv` (3055 行)，字段 `ts_code, symbol, name, area, industry` |
| **输出** | `MainBoardPool.list_active(date) -> list[StockMeta]`，含每日有效候选清单 |
| **过滤规则** | ① `ts_code` 不以 `30`/`68`/`8`/`4` 开头  ② `name` 不含 `ST` 或 `*ST` |
| **验收** | 从 3055 行能筛出预期数量的主板非 ST 票（手工抽查 10 只确认）|
| **复用** | 参考现有 `core/stock_pool/manager.py` 模式 |
| **估时** | S |

### P0-2：把现有 brick_pattern 评分扩展到主板全量

| 项 | 内容 |
|----|------|
| **目标** | 每天对 P0-1 给出的主板候选池逐只跑 brick_pattern 三定式检测 + V4 评分 |
| **新增文件** | `core/scoring/engine.py` |
| **修改文件** | `core/screening/brick_pattern/detectors.py`（详见子项 P0-2c）|
| **核心接口** | `MainBoardScoringEngine.score_date(date: str) -> ScoringResult`，返回当日所有命中票的评分明细 |
| **复用** | `BrickPatternEngine` (`core/screening/brick_pattern_engine.py`) 已有的进程池并行 + `screen_with_indicators` 接口 |
| **范围决策** | **只对命中三定式的票打分**（未命中不进评分系统）|
| **验收** | 在 T 日跑完，输出当日命中评分的所有票（用 2026-05-15 数据测试）|
| **估时** | M（含三个子项）|

#### P0-2a：评分主流程

调度引擎核心：加载候选池 → 逐只加载日线 → 计算指标 → 必备前提 → 三定式 → V4 评分 → 聚合返回。

#### P0-2b：数据有效性前置检查（原 P0-6 ① 的归处）— 已省略

**实测发现该检查在当前数据下无效**：
- 抽样主板 300 只 × 全量历史 = 113 万行，`volume=0` 行数为 0
- 当日停牌（无记录）由现有 `locate_time_index` 在 `_worker_screen_stock` 中自然过滤
- OHLC 字段 NaN 由 `core/data/io.py` 的 `load_daily_csv()` 的 `dropna(subset=["open","high","low","close"])` 在加载时剔除

所以 P0-2 主流程**不做**额外的数据有效性前置检查。如果未来数据源变化（如改其他渠道补全历史），再在 `engine.py` 加一个 `is_data_valid()` 函数即可。

#### P0-2c：修改 `detect_n_shape_jump` 加 N 型下跌段硬剔除（原 P0-6 ② 的归处）

在 [detectors.py:33-59](core/screening/brick_pattern/detectors.py#L33) 现有硬条件后加一条同类硬剔除：

```python
def detect_n_shape_jump(indicators, index):
    if index < 10:
        return PatternMatchDetail(matched=False, description="数据不足")
    # ... 现有硬条件 ...
    if prev_j >= 40:
        return PatternMatchDetail(matched=False, description=f"J值过高({prev_j:.1f}≥40)不符合N型")
    # ↓ 新增（原 P0-6 ② 下推到此）
    if _is_in_n_shape_decline(indicators, index):
        return PatternMatchDetail(matched=False, description="处于N型下跌段(非真起跳)")
    # ... 后续评分逻辑不变 ...
```

**回归验证**：用下方"评估方法说明"里列出的代表案例（茅台 2025-11-28、五粮液 2025-09-08 等）验证修改后这些日子的 N 型起跳 `matched=False`。

#### P0-2 评估方法说明（必读，禁止再用错误口径）

> ⚠️ **特别声明**：今后**任何时候**评估过滤项（包括硬剔除、风险扣分、定式硬条件等）的实际影响，**必须**使用下方"✅ 正确口径"。**禁止再用** "⛔ 错误口径" — 这会得出完全相反的结论，并误导设计决策。

##### ⛔ 错误口径（曾经踩过的坑，不要再用）

**做法**：对历史每一天直接调用判定函数（如 `_is_in_n_shape_decline()`），统计命中比例。

**陷阱**：
- 把"全部交易日"当分母，会得出"30%~50% 的天数都被判定为 N 型下跌段"的离谱数字
- 误以为这个过滤"偏宽 / 会误杀大量真起跳点"
- 一旦带着这个错觉，会反向把硬剔除降级为风险扣分，结果是 TopK 里全是"下跌中反弹"的假命中

**为什么错**：砖形图候选必须先通过 `check_prerequisites`（绿转红 + 力度 + 短趋 > 多空），没过这一关的日子根本不会进评分系统。在"不会成为候选"的日子上算命中率，**分母完全错了**。

**反例**：曾经用格力电器 000651 `2025-09-01`（收盘 41.41，从 8/27 顶部跌 14.35%）作为案例 — 但格力 2025-09 ~ 2026-05 整整 8 个月里**一天都没通过必备前提**，这个案例完全无效。

##### ✅ 正确口径

**做法**：先调用 `check_prerequisites(indicators, i)` 过滤出候选日，**只在候选日里**统计判定命中率。

```python
for i in range(20, len(df)):
    ok, _ = check_prerequisites(indicators, i)
    if not ok:
        continue                          # 没过必备前提的日子根本不算样本
    # ↓ 只在这里统计判定命中率
    if _is_in_n_shape_decline(indicators, i):
        blocked.append(i)
```

##### N 型下跌段实测数据（8 只代表性票 × 8 个月 = 2025-09-01 ~ 2026-05-15）

| 票 | 通过必备前提的候选日 | 被 N 型下跌段拦下 | 拦截比例 |
|----|--------------------|-----------------|---------|
| 000001 平安银行 | 0 | 0 | — |
| 000002 万科A | 4 | 4 | 100% |
| 600000 浦发银行 | 2 | 1 | 50% |
| 600036 招商银行 | 2 | 0 | 0% |
| 601318 中国平安 | 12 | 5 | 41.7% |
| 000651 格力电器 | 0 | 0 | — |
| 000858 五粮液 | 1 | 1 | 100% |
| 600519 贵州茅台 | 6 | 4 | 66.7% |
| **合计** | **27** | **15** | **55.6%** |

**关键发现**：
1. **候选日本身就很少**（一只票一个月 0~3 天），必备前提已经做了主要筛选
2. **在候选日里 N 型下跌段命中率 55.6%** — 这是非常强的信号，绝不是误杀
3. **拦下的案例都是真"假命中"** — 看图就是"下跌中反弹的小红砖"，符合战法明确要避免的"接飞刀"陷阱

基于这个评估，决定**把 N 型下跌段下推到 `detect_n_shape_jump` 内部作为硬剔除**（即 P0-2c），不再作为独立硬过滤模块。

##### 真正的"假命中"代表案例（这些日子已通过绿转红+力度+短趋>多空，但仍被 N 型下跌段拦下）

| 票 | 日期 | 收盘 | 形态 |
|----|------|------|------|
| 600519 贵州茅台 | 2025-11-28 | 1450.50 | 下跌中反弹小红砖 |
| 600519 贵州茅台 | 2025-09-29 | 1460.86 | 同上 |
| 601318 中国平安 | 2025-09-05 | 57.90 | 同上 |
| 000858 五粮液 | 2025-09-08 | 129.09 | 同上 |
| 000002 万科A | 2025-09-08 | 6.76 | 同上 |

P0-2c 实现完成后用这些案例做回归验证：这些日期的 `detect_n_shape_jump()` 应返回 `matched=False, description="处于N型下跌段(非真起跳)"`。

##### 历史：曾考虑但放弃的"硬过滤层"

最初设计有独立的 `P0-6 硬过滤层`，包含 4 项：①当日停牌 ②N型下跌段 ③黄线之下 ④老庄股。经过逐项审视后全部找到更合适的归处：

| 原项 | 处理 | 理由 |
|------|------|------|
| ① 当日停牌 | → 归到 P0-2b 数据加载阶段健壮性检查 | 这是数据问题不是战法判断 |
| ② N 型下跌段 | → P0-2c 下推到 `detect_n_shape_jump` 内部 | 它只服务于 N 型起跳定式，不是全局过滤 |
| ③ 黄线之下 (close < long_short) | → 删除 | 与必备前提"短趋 > 多空"用同样的 long_short 指标，语义重合（实测：候选日里仅 11.1% 出现 close < long_short，且这些边缘案例多数被 N 型下跌段或其他风险机制捕获） |
| ④ 老庄股 | → 推迟到 P3 | 判定较模糊，需要先跑起来后根据实测案例校准 |

**P0-6 这个独立任务/模块已被取消**。文档其他位置如有 "P0-6" 引用均为历史遗留，应替换为对应的归处。

### P0-3：评分明细落盘 `output/scoring_daily/{date}.json`

| 项 | 内容 |
|----|------|
| **目标** | 当日全候选评分明细持久化，供后续 IC 计算和回测使用 |
| **新增文件** | `core/scoring/storage.py`（含 `save_scoring_daily`, `load_scoring_daily` 等 API）|
| **schema** | `symbol, name, date, pattern, total_score, grade, specific_score, common_score, macd_score, signal_score, risk_penalty, items (dict), regime` |
| **格式选型** | **JSON**（实施变更：见 §1.4 存储格式说明）|
| **验收** | `load_scoring_daily(root, date)` 能读出 `list[ScoringRecord]` + 字段完整 |
| **估时** | S |

### P0-4：TopK 候选存档 `scoring_picks/{date}.json`

| 项 | 内容 |
|----|------|
| **目标** | 当日 TopK 的人类可读 JSON，供 UI 展示和后续手工 review |
| **依赖** | P0-3 |
| **schema** | `{"date": "...", "regime": "...", "picks": [{"ts_code", "name", "total", "breakdown": {...}, "pattern": "...", "rank": 1}]}` |
| **K 值** | **K = 20**（已拍板）|
| **验收** | JSON 能被 UI 渲染，字段含中文标签便于人类阅读 |
| **估时** | S |

### P0-5：T+1/T+2/T+3 三窗口自动回填

| 项 | 内容 |
|----|------|
| **目标** | 每天补 T-1/T-2/T-3 评分对应的实盘结果（收益率 + 当天是否绿砖）|
| **新增文件** | `core/scoring/outcomes.py` |
| **核心接口** | `OutcomesFiller.fill_for_today(today)` ——在 today 收盘后，扫描 today-1/today-2/today-3 的 `scoring_picks`，把 today 的实际表现写进 `output/scoring_outcomes/{score_date}.json` |
| **schema** | `ts_code, score_date, t1_return, t2_return, t3_return, t1_is_green, t2_is_green, t3_is_green` |
| **难点** | ① 跨节假日的日期计算 ② 增量更新（T+1 时只写 t1 列，T+2 时再写 t2 列）|
| **复用** | 日线数据从 `core/data/io.py` 读；绿砖判定用 `core/screening/brick_pattern/helpers.py:_is_green_brick` |
| **验收** | 用历史数据回放 60 天，能完整生成 60 天的 outcomes |
| **估时** | M |

### P0-6：~~硬过滤层~~（已取消）

> ⚠️ 此任务**已取消**。原计划的 4 项过滤经讨论后全部找到更合适的归处。详见 [P0-2 任务说明](#p0-2把现有-brick_pattern-评分扩展到主板全量) 末尾的"历史：曾考虑但放弃的硬过滤层"小节。
>
> - 当日停牌 → **P0-2b** 数据有效性前置检查
> - N 型下跌段 → **P0-2c** 下推到 `detect_n_shape_jump` 内部
> - 黄线之下 → **删除**（与必备前提"短趋 > 多空"冗余）
> - 老庄股 → 推迟到 **P3**
>
> 任务编号 P0-6 保留为占位，避免后续 P0-7/P0-8 编号变动引起的引用混乱。

### P0-7：手动触发任务

| 项 | 内容 |
|----|------|
| **目标** | 手动按钮触发：① P0-2 当日评分 ② P0-5 三窗口回填 |
| **方案** | 在 P0-8 `ScoringPage` 顶部加按钮"运行今日评分"，点击后串行执行：拉最新日线 → 计算评分 → 回填昨日 outcomes |
| **触发** | **仅手动**（不做定时调度，用户自己决定何时跑）|
| **进度反馈** | 复用现有 `start_worker()` 线程管理模式 + 状态栏进度条 |
| **验收** | 一次按钮点击能跑完整链路；过程中能看到进度，能取消 |
| **估时** | M |

### P0-8：评分诊断页 MVP（新建独立顶级页面 `ScoringPage`）

| 项 | 内容 |
|----|------|
| **目标** | 用户每天打开就能看到：当日 TopK + 每只票的评分子项明细 + 历史实盘表现 |
| **新增文件** | `app/pages/scoring_page.py` |
| **同步修改** | ① `app/pages/__init__.py` 导出 `ScoringPage`  ② `app/main_window.py` 注册到左侧导航（参考现有 `self.statsPage = StatsPage(self.root)` 模式）|
| **MVP 内容** | ① 顶部工具栏：当日日期 + 当日 OAMV 阶段标签（P3 完成前显示"未启用"）+ "运行今日评分"按钮（P0-7 入口）<br>② 中部：当日 Top20 表格，列出 ts_code/name/total_score/pattern/各子项分<br>③ 双击行：弹出该股 K 线图（复用现有 `StockChartWidget`）<br>④ 底部：前 N 日 TopK 的 T+1/T+2/T+3 收益回填情况（简单表格即可）|
| **P0 阶段省略** | IC 趋势图、因子健康度区域、权重调整入口（推到 P2/P3 阶段）|
| **风格参考** | 参考现有 `screening_page` / `stats_page` 的视觉风格保持一致 |
| **验收** | 跑完 P0-1~P0-5 + P0-7 后，从左侧导航打开新页面 → 显示当日 Top20 → 双击能弹 K 线 → 点"运行今日评分"能触发完整流程 |
| **估时** | M |

### P0 验收门槛

- [ ] 跑过去 60 个交易日的回放数据，每天都能生成 `scoring_daily` + `scoring_picks`
- [ ] 60 天后的 `scoring_outcomes` 完整率 ≥ 95%（允许节假日/停牌缺失）
- [ ] 诊断面板能展示任意一天的 TopK + 评分明细
- [ ] 端到端跑一次"今日评分 + 昨日 outcomes 回填"在 5 分钟内完成

---

## 三、阶段 1：截面归一化

> **完成标志**：S 级胜率在 2020 牛市 / 2022 熊市 / 2024 震荡三个时期之间的差异显著缩小（设计文档原文）。

### P1-1：截面分位计算 + 每日缓存 ✅ 已完成

| 项 | 内容 |
|----|------|
| **目标** | 每天计算"全主板涨幅分位"、"翻红力度比分位"、"短趋斜率分位"等截面统计量 |
| **新增文件** | `core/scoring/cross_section.py` |
| **核心接口** | `CrossSectionStats.compute(date) -> DataFrame`；`compute_and_save(date)` 一并落盘；`load_cross_section(root, date)` 读取 |
| **待归一化因子** | ① 信号日涨幅 ② 翻红力度比 ③ 短趋斜率 |
| **缓存** | 独立 CSV：`output/scoring_cross_section/{date}.csv`（扁平 schema，便于 `pd.concat` 多日聚合）|
| **实测** | 3055 主板 ~5.3 秒（多进程），3 个 pct 列范围严格 [0, 1] |
| **估时** | M（实际半天）|

### P1-2：把绝对阈值因子改成截面分位查表 ✅ 已完成

| 项 | 内容 |
|----|------|
| **目标** | 修改 `core/screening/brick_pattern/scoring.py`，把 3 项从绝对阈值改成分位查表 |
| **修改文件** | `core/screening/brick_pattern/scoring.py`（加 `_pct_to_score` + 三处分支）；`core/screening/brick_pattern/pipeline.py`（`screen_with_indicators` 加 `cs_pcts` 可选参数）；`core/scoring/engine.py`（重写自有 worker，按 symbol 把 pcts 传到 worker）|
| **分箱实现** | ≥ 0.95 满分 / 0.80~0.95 高（75%）/ 0.50~0.80 中（50%）/ 0.20~0.50 低（25%）/ < 0.20 零 |
| **兼容性** | `cs_pcts=None`（默认）→ 走原绝对阈值；`MainBoardScoringEngine` 有 `use_cross_section: bool = True` 开关切换 |
| **实测**（2026-05-15）| 启用前后 Top 10 重合度 9/10；新模式下 Top 票的"力度比"分位普遍 ≥ 0.95 → 拿满分 3 |
| **估时** | M（实际半天）|

### P1-3：跨时期回测对比 ✅ MVP 完成

| 项 | 内容 |
|----|------|
| **目标** | 验证截面归一化是否真的让评分更好（IC 提升、Top K 胜率提升）|
| **新增文件** | `tests/scoring/test_p1_cs_impact.py`（独立脚本，未污染现有 backtest.py）|
| **MVP 结果（近 5 个交易日 2026-04-30 ~ 2026-05-11）** | Top20 平均 T+1：A 旧 +2.20% / B 新 +2.25%（+0.06%）；胜率 A 65% / B 66%（+1%）；平均日 IC A 0.1143 / B 0.1146（+0.0004）|
| **结论** | 方向对（B 全面微优于 A），但样本太小不显著；扩到 30 天约需 5 分钟，可作为远期加强验证 |
| **远期扩展** | 完整版"5 年 / 2020/2022/2024 三时期" 跨期对比留作后续——需要更大算力（数百天 × 双模式 ≈ 1 小时）|
| **估时** | M（MVP 半天，远期 1~2 天）|

### P1 验收门槛

- [ ] P1-2 完成后，2020 / 2022 / 2024 三个时期的 S 级胜率标准差显著降低
- [ ] 任意一天的评分能展示"该项分位"作为可解释信息

---

## 四、阶段 2：因子健康度报告

> **完成标志**：用户每月 30 分钟看报告就能决定要不要调权。

### P2-1：IC 计算模块

| 项 | 内容 |
|----|------|
| **目标** | 计算每个评分子项与 T+1/T+2/T+3 收益的秩相关（Spearman IC）|
| **新增文件** | `core/scoring/factor_health.py` |
| **核心接口** | `FactorHealth.compute_ic(start_date, end_date, target='t1') -> DataFrame` |
| **数据源** | 读 `scoring_daily/*.parquet` + `scoring_outcomes/*.parquet`，inner join 后算 IC |
| **输出指标** | IC、IC_IR（IC 均值 / IC 标准差）、t 统计量 |
| **验收** | 能输出过去 6 个月每个子项的 IC 表 |
| **估时** | M |

### P2-2：单调性 + 分箱收益曲线

| 项 | 内容 |
|----|------|
| **目标** | 每个子项分箱（5 档），看分越高收益是否单调递增 |
| **输出** | 每个因子的"分箱 × T+1/T+2/T+3 平均收益"矩阵 |
| **验收** | 能自动判定"S 级 > A 级 > B 级"是否成立，不成立的因子标红警告 |
| **估时** | S |

### P2-3：月度报告自动生成

| 项 | 内容 |
|----|------|
| **目标** | 每月 1 号自动生成上月报告，写入 `scoring_factor_health/{YYYY-MM}.json` |
| **报告结构** | ① 整体统计（TopK 平均收益、胜率）② 各因子 IC ③ 各因子分箱单调性 ④ "异常因子"清单（IC 突变 / 单调性破坏）|
| **分组** | **按多头 / 空头分别出一份**（呼应 OAMV 阶段化设计；P3-1 完成前先全样本统一出）|
| **验收** | 月初自动生成上月报告，UI 能加载展示 |
| **估时** | M |

### P2-4：评分诊断页增强：因子健康度区域

| 项 | 内容 |
|----|------|
| **目标** | 在 P0-8 的 `ScoringPage` 里加"因子健康度"区域 |
| **内容** | ① 因子 IC 趋势图（滚动 90 天）② 当前月份 IC 排行榜 ③ "异常因子"红色提示 |
| **修改文件** | `app/pages/scoring_page.py` |
| **验收** | 用户能从页面直接看到"翻红力度比 IC 持续低于 0.02"这类警告 |
| **估时** | M |

### P2-5：人工 Review + 半自动调权工作流 ⏸ 已推迟

| 项 | 内容 |
|----|------|
| **目标** | UI 上能编辑因子权重 → 触发回测验证 → 确认后写回 `scoring.py` 配置 |
| **方案** | 把硬编码的权重抽到 `core/scoring/weights.yaml` 或 JSON 配置；UI 编辑后写新配置；回测用新配置跑过去 6 个月 |
| **推迟理由** | 1) L 量级，跨多模块改动；2) 需要 30+ 天样本回测才有意义，当前 5 天数据不可靠；3) 当前 V4 权重已经人工调过一轮（"翻红力度比 7→3"），不是急需。等 P3 完成 + 实盘运行 3+ 个月有足够数据后再考虑 |
| **估时** | L（远期） |

### P2 验收门槛

- [ ] 月初能自动生成上月报告
- [ ] 诊断面板能展示因子 IC 趋势 + 异常因子警告
- [ ] 调权工作流端到端跑通（即使阈值随便选，流程要顺）

---

## 五、阶段 3：OAMV 阶段化 + 战法因子补缺

> **完成标志**：评分系统选出的图形和用户人工选股的重合度 > 70%。

### P3-1：OAMV 阶段标签生成器（含 3 日平滑）✅ 已完成

| 项 | 内容 |
|----|------|
| **目标** | 每天写入 `output/scoring_regime/{date}.json`：当日 OAMV 阶段标签（MVP 多/空头二分；含 `tempo: fast/slow` 字段为四象限预留）|
| **新增文件** | `core/scoring/regime.py` (~170 行) |
| **核心算法** | ① 加载 `stock_daily_data/oamv_930903_CSI.csv` ② 计算 MA20(close)、slope5、slope20 ③ raw_phase = `bull if close >= ma20 else bear` ④ **连续 3 日 raw 同向才确认切换**（smoothed_phase）|
| **实测验证** | 多头时段 2025-12-19~2026-01-28 → 85% bull；空头时段 2025-09-03~2025-12-12 → 79% bear；平滑使近 6 月切换次数从 5 → 3（减少 40%）|
| **估时** | M（实际半天）|

### P3-2：分阶段累积训练集 + 分别算分位 / IC / 权重 ⏸ 已推迟

| 项 | 内容 |
|----|------|
| **目标** | 按 OAMV 阶段（多头集/空头集）分别累积训练 |
| **推迟理由** | 依赖 P2-5（权重配置抽取）已推迟；当前数据样本（5 天）远不足以分组训练（每阶段需 100+ 天）；P3-1 阶段标签已就位，未来可启用 |
| **接口预留** | `CrossSectionStats` / `FactorHealth` 后续按需加 `regime` 参数 |
| **估时** | L（远期） |

### P3-3：分阶段权重实盘切换 ⏸ 已推迟

| 项 | 内容 |
|----|------|
| **目标** | 评分系统根据当日 regime 自动加载对应权重 |
| **推迟理由** | 依赖 P3-2 已推迟（没有"两套权重"何来切换）|
| **当前替代** | engine 直接调 `RegimeAnalyzer.save_for_date()` 落盘当日 regime（写到 picks 的 `regime` 字段供 UI 显示），但不切换权重 |
| **估时** | M（远期） |

### P3-4 + P3-5 + P3-7：战法加分因子合并实现 ✅ 已完成

| 项 | 内容 |
|----|------|
| **目标** | 三个独立战法因子合并到一个 `compute_p3_bonus()` 函数，命中加分（非对称）|
| **修改文件** | ① `core/models/brick_pattern.py` 给 `ScoreBreakdown` 加 `bonus_score` + `bonus_items` 字段（向后兼容）② `core/screening/brick_pattern/scoring.py` 新增 `compute_p3_bonus()` + 3 个辅助函数 ③ `core/screening/brick_pattern/pipeline.py` 调用 bonus 挂到 breakdown ④ `core/scoring/storage.py` 把 bonus 同步到 picks/daily 落盘 |
| **三因子实现** | P3-4 红柱比 `_red_green_ratio_ok`（红/绿累计长度 ≥ 2/3）+2 分；P3-5 地量 `_is_dry_volume`（信号日 volume 在近 60 日 20 分位以下）+2 分；P3-7 `_diff_dea_cross_age` ≤ 2 日 +1 分 |
| **实测**（2026-05-15）| Top 10 大部分拿到 +2 红柱比；通达电气 +1 DIFF/DEA 刚金叉；多头阶段无地量触发（量普遍偏高，符合预期）|
| **估时** | 3 × S（实际合并 1 天）|

### P3-6：第三浪末端扣分 ✅ 已完成（早已实现）

| 项 | 内容 |
|----|------|
| **目标** | 第三浪末端命中定式时大幅扣分 |
| **现状** | [`scoring_risk.py:316 _check_third_wave`](core/screening/brick_pattern/scoring_risk.py#L316) 早已实现，触发扣 -8 分，已被 `compute_risk_penalty` 主流程调用 |
| **本任务** | 无需新增代码，确认生效即可 |

### P3-8：评分诊断页阶段标签 UI ✅ 简化版完成

| 项 | 内容 |
|----|------|
| **目标** | ScoringPage 显示当日 OAMV 阶段标签 |
| **简化版实现** | 顶部 `regime_label` 实时从 `RegimeAnalyzer` 取当日标签，显示"多头 · 缓"等中文文字，多头绿色、空头红色加粗 |
| **远期完整版** | 90 天阶段切换时序图（需要 pyqtgraph 画图）暂未实现，留作可选增强 |
| **修改文件** | `app/pages/scoring_page.py` + ScoringWorker 内集成 `RegimeAnalyzer.save_for_date()` |
| **估时** | S（简化版半天）|

### P3-9（可选）：四象限细化

| 项 | 内容 |
|----|------|
| **目标** | 从多/空头二分扩到四象限（快速上涨/平缓上涨/快速下跌/平缓下跌）|
| **前置** | P3-1~P3-3 二分版本稳定运行 ≥ 1 个月 + IC 收敛 |
| **数据验证** | 近 5 年每象限 ≥ 265 天（已数据验证）|
| **验收** | 四象限各自 IC 收敛 |
| **估时** | M（在二分基础上扩展）|

### P3 验收门槛

- [ ] OAMV 阶段标签能在面板上看到，与历史走势一致
- [ ] 多头/空头各有独立权重配置，切换日自动应用
- [ ] 红柱比、地量、第三浪、金叉时间四项新因子全部接入并参与 IC 评估
- [ ] 评分系统选出的票与用户人工选股的重合度 > 70%（需要用户协助评估）

---

## 六、跨阶段任务

### X-1：回测框架增强

| 项 | 内容 |
|----|------|
| **目标** | 支持按时间段、按阶段标签筛选回测样本；输出标准化的因子 IC 报告 |
| **修改文件** | `core/screening/brick_pattern/backtest.py` |
| **服务于** | P1-3 / P2-1 / P3-2 |
| **估时** | M |

### X-2：单元测试

| 项 | 内容 |
|----|------|
| **覆盖范围** | `core/scoring/` 全部新模块（特别是 cross_section / regime / outcomes 的边界用例）|
| **重点** | ① 跨节假日的日期计算 ② OAMV 阶段切换的滞后窗 ③ outcomes 增量更新的幂等性 |
| **估时** | M（贯穿全程，每个 P 任务完成时补对应测试）|

### X-3：模块文档

| 项 | 内容 |
|----|------|
| **新增文档** | `.claude/modules/scoring_system.md`（与 chart_system.md 等同级）|
| **更新** | `CLAUDE.md` 加入新模块索引行 |
| **触发** | P0 完成后初版，P3 完成后定稿 |
| **估时** | S |

---

## 七、与现有 V4 评分的关系（再强调一次）

**不推翻**。所有改动都是在 V4 现有四模块结构上扩展：

| V4 现状 | 改造方式 |
|---------|----------|
| 砖形图三定式（`detectors.py`） | 不动，复用 |
| 必备前提（`check_prerequisites`）| 不动，复用 |
| 风险过滤三项 + 高位放量阴线（`scoring_risk.py`）| 不动，**P3-6 加第三浪末端扣分** |
| 通用质量 30 分 | **P1-2 把信号日涨幅 / 翻红力度比 / 短趋斜率改成分位**；**P3-4/P3-5 加红柱比 + 地量** |
| MACD 辅助 25 分 | **P3-7 细化金叉时间** |
| 信号强度 15 分 | 不动 |
| 权重硬编码 | **P2-5 抽到配置文件；P3-2 改成多/空头双套配置** |

---

## 八、不在本开发文档范围内（避免发散）

- 板块/行业评分系统（设计文档 §八 已明确推迟）
- 基本面因子
- 实盘下单接口
- 多周期评分（周线+日线联合）
- 概念维度（依赖 Tushare 5000 积分接口，推迟到 P3 完成后视情况）
- 涨停封板含金量（依赖 `pro.limit_list_d` 付费接口）

---

## 九、估时汇总

| 阶段 | 任务数 | 估时分布 | 备注 |
|------|--------|----------|------|
| P0 | 7 | 3×S + 4×M | P0-6 已取消并入 P0-2；P0-2 含 a/b/c 三子项 |
| P1 | 3 | 3×M | — |
| P2 | 5 | 1×S + 3×M + 1×L | — |
| P3 | 9 | 4×S + 4×M + 1×L | — |
| X | 3 | 1×S + 2×M | — |

> S = 半天到 1 天；M = 2~4 天；L = 5~10 天（含调试和测试）

### 9.1 P0 开发顺序建议（自下而上、可独立验证）

| 顺序 | 任务 | 完成后可验证 |
|------|------|-------------|
| 1 | **P0-1** 主板池过滤 | 命令行调用能返回主板候选 ~2500 只 |
| 2 | **P0-2** 评分扩展（含 a/b/c 三子项）| 给定一天能输出几十只命中评分明细；茅台 2025-11-28 等案例验证 P0-2c 下推 |
| 3 | **P0-3** scoring_daily 落盘 | 文件系统能看到 JSON 文件 + `load_scoring_daily` 能读 |
| 4 | **P0-4** scoring_picks 落盘 | 文件系统能看到 JSON 文件 + Top 20 列表正确 |
| 5 | **P0-5** 三窗口回填 | 历史回放：跑 60 天能生成 60 天的 outcomes |
| 6 | **P0-7** 手动触发按钮 | 命令行/脚本能串起 P0-1→P0-5 全链路 |
| 7 | **P0-8** `ScoringPage`（含 P0-7 按钮接入）| 应用左侧导航能打开新页面、显示 Top20、点按钮触发流程 |

每步完成后立刻能验证，不必等所有任务齐全才能跑通端到端。
