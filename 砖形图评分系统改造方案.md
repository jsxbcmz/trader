# 砖形图评分系统 V2 改造方案

> 基于 `例子/砖形图评分系统V2.md` 的设计，对比现有 `core/screening/brick_pattern_engine.py` 实现，梳理需要改动的模块和具体改造内容。

---

## 一、现有系统 vs V2 差异总览

| 维度 | 现有系统 | V2目标 |
|------|----------|--------|
| **评分结构** | 每种定式内部独立打分（0~100），无统一框架 | 定式专属(70分) + 通用质量(30分) + 风险扣分 |
| **评分等级** | 无等级划分，只输出原始分数 | S/A/B/C/D 五级（85/70/55/40 分界） |
| **N型检测** | 基于极值点+回调比例的几何结构判断 | 基于超卖深度(J值)+回调充分度+支撑验证+波段基础 |
| **横盘检测** | 基于收盘价波动率+振幅的统计判断 | 基于砖色切换频次+突破弹性+KDJ动能 |
| **上升延续检测** | 基于红绿砖计数+趋势斜率 | 基于红砖连续性+回调极短性+砖值水平+KDJ超买 |
| **风险过滤** | 3条规则，触发即否决（二值） | 通用风险(3条)+定式专属风险(5条)，按分值扣减 |
| **KDJ参数** | 已用(5,3,3)，N型取前日J值 | (5,3,3)，N型取前日J值 —— **已对齐** |
| **通用质量** | 不存在 | 翻红质量(15分) + 趋势环境(15分) |
| **数据模型** | `PatternMatchDetail.score` 为单一浮点 | 需要专属分/通用分/风险扣分/最终分/等级 |

---

## 二、需要改动的文件清单

| 文件 | 改动级别 | 改动内容 |
|------|----------|----------|
| `core/models/brick_pattern.py` | **重构** | 新增评分详情数据结构 |
| `core/screening/brick_pattern_engine.py` | **重写核心** | 三种定式检测函数+评分+风险全部重写 |
| `app/pages/brick_pattern_page.py` | **修改** | 显示新的评分等级、分数分解 |
| `app/chart_indicators.py` | **小改** | 新增 MA 计算辅助函数（MA14/28/57/114） |

---

## 三、数据模型改造（`core/models/brick_pattern.py`）

### 3.1 新增评分详情结构

```python
@dataclass(frozen=True)
class ScoreBreakdown:
    """评分分解详情"""
    # 定式专属评分（满分70）
    specific_score: float = 0.0
    specific_items: dict[str, float] = field(default_factory=dict)
    # 例: {"超卖深度": 21, "回调充分度": 20, "价格与黄白线": 15, "前段上涨": 8, "缩量附加": -3}

    # 通用质量评分（满分30）
    common_score: float = 0.0
    common_items: dict[str, float] = field(default_factory=dict)
    # 例: {"翻红力度比": 8, "信号日涨幅": 5, "短趋vs多空": 6, "均线排列": 3, "短趋斜率": 1}

    # 风险扣分
    risk_penalty: float = 0.0
    risk_items: dict[str, float] = field(default_factory=dict)
    # 例: {"绿砖期放量": -15}

    @property
    def base_score(self) -> float:
        """基础分 = 专属 + 通用"""
        return self.specific_score + self.common_score

    @property
    def final_score(self) -> float:
        """最终分 = max(0, 基础分 + 风险扣分)"""
        return max(0, self.base_score + self.risk_penalty)

    @property
    def grade(self) -> str:
        """评分等级 S/A/B/C/D"""
        s = self.final_score
        if s >= 85: return "S"
        if s >= 70: return "A"
        if s >= 55: return "B"
        if s >= 40: return "C"
        return "D"

    @property
    def risk_level(self) -> str:
        """风险等级"""
        p = self.risk_penalty
        if p == 0: return "无风险"
        if p >= -15: return "低风险"
        if p >= -30: return "中风险"
        return "高风险"
```

### 3.2 修改 PatternMatchDetail

```python
@dataclass(frozen=True)
class PatternMatchDetail:
    pattern_type: PatternType
    matched: bool = False
    description: str = ""
    score: float = 0.0
    score_breakdown: ScoreBreakdown | None = None  # 新增
    extra: dict[str, Any] = field(default_factory=dict)
```

### 3.3 修改 BrickPatternMatch

```python
@dataclass(frozen=True)
class BrickPatternMatch:
    # ... 现有字段保留 ...
    final_score: float = 0.0        # 新增：最终得分
    grade: str = ""                  # 新增：等级 S/A/B/C/D
    score_breakdown: ScoreBreakdown | None = None  # 新增：评分分解
```

### 3.4 新增风险过滤类型

```python
class RiskFilterType(Enum):
    LIMIT_DOWN = "一字板跌停"
    HEAVY_VOLUME_DROP = "高位放量大阴线"
    TREND_BROKEN = "短趋势跌破多空线"         # 新增
    CHASE_HIGH = "追高离心"                   # 新增（上升波段延续专属）
    GREEN_VOLUME_UP = "绿砖期放量"            # 新增（N型专属）
    PEAK_VOLUME = "天量见顶"                  # 新增（上升波段延续专属）
    TREND_EXHAUST = "趋势衰竭"               # 新增（横盘起跳专属）
    FAKE_SIDEWAYS = "假横盘"                  # 新增（横盘起跳专属）
    LONG_SIDEWAYS = "横盘时间过长"            # 删除（V2中不再使用）
```

### 3.5 修改 RiskFilterDetail

```python
@dataclass(frozen=True)
class RiskFilterDetail:
    filter_type: RiskFilterType
    triggered: bool = False
    description: str = ""
    penalty: float = 0.0            # 新增：扣分值（负数）
    extra: dict[str, Any] = field(default_factory=dict)
```

---

## 四、引擎核心改造（`core/screening/brick_pattern_engine.py`）

### 4.1 指标计算扩展（`_calc_indicators`）

**新增计算项**：

| 指标 | 用途 | 计算方法 |
|------|------|----------|
| MA14/MA28/MA57/MA114 | 通用质量-均线排列 | `moving_average(close, period)` |
| 10日砖色切换次数 | 横盘起跳-蓄势充分度 | 遍历前10日砖形图涨跌状态切换次数 |
| 信号日涨幅 | 通用质量-信号日涨幅 | `(close[i] - close[i-1]) / close[i-1]` |

在 `_calc_indicators` 返回值中追加 `ma14`、`ma28`、`ma57`、`ma114` 四个数组。

### 4.2 N型起跳重写（`detect_n_shape_jump`）

**现有逻辑**：基于几何极值点搜索，Fibonacci 回调比例评分。评分范围 0~100，内部自成体系。

**V2逻辑**：完全不同的评分维度，重写整个函数。

#### 主要改动点

| 评分因子 | 满分 | 现有实现 | V2要求 |
|---------|------|----------|--------|
| 超卖深度 | 25 | J值线性衰减评分 | 前日J值分段评分（<-5满分25，≥40得0） |
| 回调充分度 | 20 | 不存在（用回调比例替代） | **新增**：回调阶段概念，最长连续绿砖段+打断修正 |
| 价格与黄白线 | 15 | 不存在 | **新增**：收盘vs短趋势分段评分+多空线封顶扣罚 |
| 前段上涨基础 | 10 | 简单的趋势评分(20分) | **新增**：波段涨幅(4分)+持续天数(3分)+K线趋势质量(3分) |
| 缩量附加 | ±3 | 不存在 | **新增**：绿砖期成交量/红砖期成交量比值 |

#### 回调阶段算法（新概念）

```
从信号日向前追溯绿砖段：
  1. 统计紧邻信号日的连续绿砖天数
  2. 若遇到 ≤2天 红砖打断，跳过打断继续纳入绿砖段
  3. 遇到 ≥3天 连续红砖则回调阶段终止
  4. 取回调阶段内最长连续绿砖段天数评分
  5. 打断修正：1天红砖打断 -2分，2天红砖打断 -3分
```

#### 前段上涨基础算法（新概念）

```
1. 从回调阶段起点向前追溯
2. 找到波段高点（回调前的局部最高收盘价）
3. 从高点前30日内找到起涨点（区间最低收盘价）
4. 计算三个维度：
   A. 波段涨幅：(高点-起涨点)/起涨点
   B. 上涨波段持续天数：起涨点到高点的总交易日
   C. K线趋势质量：上涨区间三等分，比较每段收盘均价是否逐步抬升
```

### 4.3 横盘起跳重写（`detect_sideways_jump`）

**现有逻辑**：基于收盘价统计波动率（标准差/均值 < 2%）判断横盘，窗口遍历 3~10 天。

**V2逻辑**：完全不同的检测和评分方式。

| 评分因子 | 满分 | 现有实现 | V2要求 |
|---------|------|----------|--------|
| 蓄势充分度 | 25+3 | 收盘价振幅窄度(40分) | **新增**：10日砖色切换次数+10日振幅辅助 |
| 突破弹性 | 20 | 今日涨幅(40分) | 砖型差值单日跳升幅度 |
| KDJ动能 | 15 | 不存在 | **新增**：J值65~85区间最佳 |
| 价格强度 | 10 | 不存在 | **新增**：收盘vs多空线 |

**关键差异**：
- 现有用"收盘价波动率"判断横盘 → V2用"砖色切换频次"判断
- 现有要求连续绿砖仅1天是定式特征中自然筛出 → V2明确要求绿砖仅1天（通过前提条件已保证绿转红，紧邻前1日为绿砖）
- 现有突破力度用今日涨幅 → V2用砖型差值跳升

### 4.4 上升波段延续重写（`detect_uptrend_continue`）

**现有逻辑**：红砖≥3 + 绿砖1~2 + 趋势斜率为正。评分 = 红砖数×8 + 绿砖1天奖励 + 斜率分 + 20。

**V2逻辑**：

| 评分因子 | 满分 | 现有实现 | V2要求 |
|---------|------|----------|--------|
| 趋势连续性 | 25 | 红砖数×8（上限40） | 分段评分：≥7天→25，5~6→20，4→15 |
| 回调极短性 | 20 | 绿砖1天+10 | 绿砖天数(14/8/2) + 砖值下降幅度(+6/+4/+2/0) |
| 砖值绝对水平 | 15 | 不存在 | **新增**：≥130→15，100~115→9，<85→0 |
| KDJ超买动能 | 10 | 不存在 | **新增**：J>100→10，90~95→8，≤80→1 |

**关键差异**：
- 现有要求红砖≥3 → V2要求红砖≥3（保持，但评分梯度不同）
- 新增砖值绝对水平要求（必须>85才有分）
- 新增KDJ超买要求

### 4.5 通用质量评分（全新模块）

这是 V2 新增的30分模块，现有系统完全没有，需要新建函数。

```python
def compute_common_quality_score(
    indicators: dict,
    index: int,
    pattern_type: PatternType,
) -> tuple[float, dict[str, float]]:
    """计算通用质量评分(30分)。"""
```

#### 7.1 翻红质量（15分）

| 子项 | 满分 | 计算 |
|------|------|------|
| 翻红力度比 | 8 | `砖值变化 / max(abs(前日砖值变化), 2)` → ≥3→8, 2~3→6, 1.5~2→4, 1~1.5→2, 0.5~1→1, <0.5→0 |
| 信号日涨幅 | 7 | 按定式分别评分（N型/横盘/波段延续各有不同的理想涨幅区间） |

注意：翻红力度比的除数设下限2，防止前日砖值变化趋零时比值虚高。

#### 7.2 趋势环境（15分）

| 子项 | 满分 | 计算 |
|------|------|------|
| 短趋势vs多空线 | 6 | 差距 2%~8%→6, 0.5%~2%→4, >8%→3, ≤0→0 |
| 均线排列 | 5 | MA14>MA28>MA57>MA114 满足对数：3/3→5, 2/3→3, 1/3→1, 0→0 |
| 短趋势斜率 | 4 | 10日线性回归斜率/价格×100：≥0.5%→4, 0.2%~0.5%→3, 0~0.2%→1, <0→0 |

### 4.6 风险扣分系统（重写）

**现有系统**：3条规则，触发任一即否决（二值判断），股票直接被过滤。

**V2系统**：8条规则（3通用+5定式专属），触发后按严重程度扣分，不直接否决。

#### 通用风险项（全部重写）

| 风险项 | 现有实现 | V2改造 |
|--------|----------|--------|
| 一字板跌停 | 触发→否决 | 触发→扣 -30~-50分（按距今天数分级） |
| 高位放量大阴线 | 触发→否决 | 触发→扣 -15~-35分（按位置分级） |
| 短趋势跌破多空线 | 不存在（在前提中检查） | **新增**：扣 -15分 |

#### 定式专属风险项（全部新增）

| 风险项 | 适用定式 | 扣分 |
|--------|----------|------|
| 追高离心 | 上升波段延续 | 收盘远离短趋势>25%→-10, >35%→-15, >45%→-20 |
| 绿砖期放量 | N型起跳 | 缩量比>1.3→-5, >1.5→-15 |
| 天量见顶 | 上升波段延续 | 量比>4且为近20日最大量→-10~-20 |
| 趋势衰竭 | 横盘起跳 | 短趋势10日斜率≤0→-10, <-0.1%→-15 |
| 假横盘 | 横盘起跳 | 10日砖色切换<3→-10 |

#### 架构变化

```
现有流程：前提检测 → 定式检测 → 风险过滤（触发即否决）
V2流程：  前提检测 → 定式检测+专属评分 → 通用质量评分 → 风险扣分 → 最终得分+等级
```

**关键变化**：风险不再直接否决，而是叠加扣分。最终得分 = max(0, 基础分 + 风险扣分)。低于40分(D级)的信号可视为无效信号。

### 4.7 定式分类逻辑调整

**现有**：三种定式各自独立检测，取最高分者为匹配定式。

**V2要求**：保持三种定式独立检测，但评分逻辑完全不同。不再是简单的取最高分——因为三种定式的专属评分逻辑不同，它们的分数不可直接比较。

**建议实现**：
1. 每种定式先做形态匹配（是否符合该定式的基本结构特征）
2. 对匹配的定式分别计算专属分(70分)+通用分(30分)+风险扣分
3. 如果多个定式同时匹配，取最终得分最高者

### 4.8 `screen_single_stock` / `screen_with_indicators` 改造

主流程从"风险触发即否决"改为"计算最终得分"：

```python
# 现有流程
if triggered_risks:
    return ... risk_rejected=True ...

# V2流程
# 1. 专属评分
specific_score, specific_items = compute_specific_score(...)
# 2. 通用质量评分
common_score, common_items = compute_common_quality_score(...)
# 3. 风险扣分
risk_penalty, risk_items = compute_risk_penalty(...)
# 4. 组装评分详情
breakdown = ScoreBreakdown(
    specific_score=specific_score,
    specific_items=specific_items,
    common_score=common_score,
    common_items=common_items,
    risk_penalty=risk_penalty,
    risk_items=risk_items,
)
# 5. 返回结果（包含等级和分数分解）
return BrickPatternMatch(
    ...,
    final_score=breakdown.final_score,
    grade=breakdown.grade,
    score_breakdown=breakdown,
)
```

### 4.9 并行工作函数改造（`_worker_screen_stock`）

序列化返回值需要包含新字段：`final_score`、`grade`、`score_breakdown`（序列化为dict）。

---

## 五、UI 展示改造（`app/pages/brick_pattern_page.py`）

### 5.1 表格列调整

| 现有列 | V2改造 |
|--------|--------|
| 评分(score) | 改为显示"最终得分(等级)"，如 "84 (A)" |
| 风险(risk) | 改为显示风险等级+扣分值，如 "低风险(-10)" |

### 5.2 评分详情展示

点击某一行可展开查看评分分解（或用 tooltip 显示）：
- 专属评分：各因子名称+得分
- 通用质量：各因子名称+得分
- 风险扣分：各风险项+扣分值

### 5.3 结果行颜色

| 等级 | 行颜色 |
|------|--------|
| S级(85+) | 深绿 `#D5F5D5` |
| A级(70~84) | 浅绿 `#F6FFED` |
| B级(55~69) | 黄色 `#FFFBE6` |
| C级(40~54) | 橙色 `#FFF1E6` |
| D级(<40) | 红色 `#FFF1F0` |

---

## 六、`chart_indicators.py` 小改

### 6.1 需要新增的辅助函数

V2 评分中的趋势环境子项"均线排列"需要 MA14/MA28/MA57/MA114。现有 `moving_average` 函数已支持任意周期，但 `_calc_indicators` 未返回这些 MA 值。

**改动**：在 `_calc_indicators` 中新增计算并返回 MA14/MA28/MA57/MA114。

> 注意：`compute_zx_long_short` 已使用 `ZX_MULTI_PERIODS = (14, 28, 57, 114)`，多空线就是这四条MA的均值。但均线排列检查需要比较它们各自的值，不能只用均值。

---

## 七、开发任务拆分与优先级

> 按依赖关系分为 6 个批次，每个批次内的任务可并行开发。标注预估工作量（S/M/L）和优先级（P0必须/P1重要/P2锦上添花）。

---

### 批次一：地基层 —— 数据模型 + 指标扩展（无外部依赖，最先做）

| # | 任务 | 文件 | 工作量 | 优先级 | 验收标准 |
|---|------|------|--------|--------|----------|
| 1.1 | 新增 `ScoreBreakdown` 数据类 | `core/models/brick_pattern.py` | S | P0 | 包含 specific/common/risk 三段分数、`final_score`/`grade`/`risk_level` 属性、`to_dict()`方法 |
| 1.2 | 扩展 `PatternMatchDetail` 和 `BrickPatternMatch` | `core/models/brick_pattern.py` | S | P0 | 新增 `score_breakdown`/`final_score`/`grade` 字段，保持 frozen=True |
| 1.3 | 重构 `RiskFilterType` 枚举 + `RiskFilterDetail` | `core/models/brick_pattern.py` | S | P0 | 删除 `LONG_SIDEWAYS`，新增6种风险类型，`RiskFilterDetail` 加 `penalty` 字段 |
| 1.4 | `_calc_indicators` 追加 MA14/MA28/MA57/MA114 | `brick_pattern_engine.py` | S | P0 | 返回值新增4个MA数组，现有调用方不受影响 |

**产出**：模型层就绪，后续所有评分函数可直接使用新数据结构。

---

### 批次二：评分框架 —— 通用质量评分 + 风险扣分（依赖批次一）

| # | 任务 | 文件 | 工作量 | 优先级 | 验收标准 |
|---|------|------|--------|--------|----------|
| 2.1 | 新建 `compute_common_quality_score()` | `brick_pattern_engine.py` | M | P0 | 翻红质量(15分) + 趋势环境(15分)，按V2文档评分表精确实现 |
| 2.1a | ├ 翻红力度比（8分） | — | — | — | 除数下限=2，阈值 ≥3→8 / 2~3→6 / 1.5~2→4 / 1~1.5→2 / 0.5~1→1 / <0.5→0 |
| 2.1b | ├ 信号日涨幅（7分） | — | — | — | 按定式分三套评分表（N型/横盘/波段延续） |
| 2.1c | ├ 短趋势vs多空线（6分） | — | — | — | 差距 2%~8%→6 / 0.5%~2%→4 / >8%→3 / ≤0→0 |
| 2.1d | ├ 均线排列（5分） | — | — | — | MA14>MA28>MA57>MA114 递进比较 |
| 2.1e | └ 短趋势斜率（4分） | — | — | — | 10日线性回归斜率/价格×100 |
| 2.2 | 新建 `compute_risk_penalty()` —— 通用风险 | `brick_pattern_engine.py` | M | P0 | 一字板跌停(-30~-50) / 高位放量大阴线(-15~-35) / 短趋势跌破多空线(-15) |
| 2.3 | 新建 `compute_risk_penalty()` —— 定式专属风险 | `brick_pattern_engine.py` | M | P0 | 追高离心/绿砖期放量/天量见顶/趋势衰竭/假横盘，共5条 |

**产出**：通用评分和风险扣分模块可独立测试，不依赖定式检测函数。

---

### 批次三：核心定式重写（依赖批次一，可与批次二并行）

> 三个定式互相独立，可分配给不同开发周期或并行完成。

| # | 任务 | 文件 | 工作量 | 优先级 | 验收标准 |
|---|------|------|--------|--------|----------|
| **3.1** | **重写 `detect_n_shape_jump()`** | `brick_pattern_engine.py` | **L** | **P0** | 见下方子任务 |
| 3.1a | ├ 超卖深度评分（25分） | — | S | — | 前日J值分7段评分，≥40得0 |
| 3.1b | ├ 回调阶段识别算法 | — | M | — | 向前追溯绿砖段，≤2天红砖打断视为延续，≥3天终止 |
| 3.1c | ├ 回调充分度评分（20分） | — | S | — | 最长连续绿砖段天数评分 + 打断修正(-2/-3) |
| 3.1d | ├ 价格与黄白线评分（15分） | — | S | — | 收盘vs短趋势分6段 + 跌破多空线封顶8分 |
| 3.1e | ├ 前段上涨基础评分（10分） | — | M | — | 波段识别(高点/起涨点) + 涨幅(4分) + 天数(3分) + 趋势质量(3分) |
| 3.1f | └ 缩量附加评分（±3分） | — | S | — | 绿砖期成交量/红砖期成交量比值，专属总分上限70 |
| **3.2** | **重写 `detect_sideways_jump()`** | `brick_pattern_engine.py` | **M** | **P0** | 见下方子任务 |
| 3.2a | ├ 蓄势充分度评分（25+3分） | — | M | — | 10日砖色切换次数(主) + 10日振幅(辅助加分) |
| 3.2b | ├ 突破弹性评分（20分） | — | S | — | 砖型差值单日跳升，≥15→20 / <6→2 |
| 3.2c | ├ KDJ动能评分（15分） | — | S | — | J值65~85→15，中心对称衰减 |
| 3.2d | └ 价格强度评分（10分） | — | S | — | 收盘vs多空线，+5%~+15%→10 |
| **3.3** | **重写 `detect_uptrend_continue()`** | `brick_pattern_engine.py` | **M** | **P0** | 见下方子任务 |
| 3.3a | ├ 趋势连续性评分（25分） | — | S | — | 前红砖天数分5段评分 |
| 3.3b | ├ 回调极短性评分（20分） | — | S | — | 绿砖天数(14/8/2) + 砖值下降幅度(+6/+4/+2/0) |
| 3.3c | ├ 砖值绝对水平评分（15分） | — | S | — | ≥130→15 / <85→0 |
| 3.3d | └ KDJ超买动能评分（10分） | — | S | — | J>100→10 / ≤80→1 |

**产出**：三种定式的形态匹配 + 专属评分(70分)输出完成。

---

### 批次四：主流程串联（依赖批次二 + 批次三）

| # | 任务 | 文件 | 工作量 | 优先级 | 验收标准 |
|---|------|------|--------|--------|----------|
| 4.1 | 改造 `screen_with_indicators()` 主流程 | `brick_pattern_engine.py` | M | P0 | 定式评分→通用评分→风险扣分→组装ScoreBreakdown→返回含等级的结果 |
| 4.2 | 改造 `_worker_screen_stock()` 序列化 | `brick_pattern_engine.py` | S | P0 | 返回dict含 `final_score`/`grade`/`score_breakdown`(展平为dict) |
| 4.3 | 改造引擎 `run()` 结果解析 | `brick_pattern_engine.py` | S | P0 | 反序列化 `BrickPatternMatch` 含新字段 |
| 4.4 | 删除旧风险过滤函数 `filter_long_sideways` | `brick_pattern_engine.py` | S | P0 | 清理引用，确保无残留 |
| 4.5 | 选股页兼容性检查 | `app/pages/screening_page.py` | S | P1 | 确认 `screen_single_stock` 接口改造后选股页正常工作 |

**产出**：引擎完整可运行，命令行/脚本调用可得到V2评分结果。

---

### 批次五：UI 展示（依赖批次四）

| # | 任务 | 文件 | 工作量 | 优先级 | 验收标准 |
|---|------|------|--------|--------|----------|
| 5.1 | 修改 `_verify_row()` 展示逻辑 | `app/pages/brick_pattern_page.py` | M | P0 | 评分列显示"84 (A)"格式，风险列显示"低风险(-10)"格式 |
| 5.2 | 按等级设置行颜色 | `app/pages/brick_pattern_page.py` | S | P1 | S→深绿 / A→浅绿 / B→黄 / C→橙 / D→红 |
| 5.3 | 评分详情 tooltip | `app/pages/brick_pattern_page.py` | M | P2 | 悬浮显示专属/通用/风险三段评分明细 |

**产出**：定式验证页可视化展示V2评分结果。

---

### 批次六：验证与调优（依赖批次五）

| # | 任务 | 文件 | 工作量 | 优先级 | 验收标准 |
|---|------|------|--------|--------|----------|
| 6.1 | 14案例逐项评分验证 | — | M | P0 | 每个案例最终得分与V2文档偏差 ≤3分 |
| 6.2 | 边界案例重点验证 | — | S | P0 | 002792(分段回调)、002536(翻红力度比修正)、600744(趋势衰竭扣分) |
| 6.3 | 批量选股回测验证 | — | M | P1 | 对全市场跑一次选股，确认性能无退化、结果分布合理 |
| 6.4 | 评分阈值微调 | `brick_pattern_engine.py` | S | P2 | 根据回测结果调整分段边界（如需要） |

---

### 任务依赖关系总图

```
批次一（地基）
  ├──→ 批次二（通用评分+风险）──┐
  └──→ 批次三（三定式重写）────┤
                                 ├──→ 批次四（主流程串联）
                                 │        │
                                 │        ├──→ 批次五（UI展示）
                                 │        │        │
                                 │        │        └──→ 批次六（验证）
                                 │        │
                                 │        └──→ 6.1/6.2（引擎验证，不依赖UI）
```

### 总工作量预估

| 批次 | 任务数 | 预估工作量 | 说明 |
|------|--------|-----------|------|
| 批次一 | 4 | 半天 | 纯数据结构，无逻辑复杂度 |
| 批次二 | 3(含子项) | 1天 | 评分表较多但都是分段查表 |
| 批次三 | 3(含子项) | 1.5~2天 | N型最重（回调阶段+波段识别），另两个中等 |
| 批次四 | 5 | 半天 | 胶水代码，串联已有模块 |
| 批次五 | 3 | 半天 | UI调整，tooltip可选 |
| 批次六 | 4 | 1天 | 14案例手动核对 + 回测 |
| **合计** | **22** | **约4~5天** | |

### 建议开发顺序

1. **先做批次一**（半天），把数据结构定好，后面所有代码都基于此
2. **批次二和批次三并行推进**：通用评分模块与三个定式重写互不依赖
3. **批次三内部建议顺序**：先做上升波段延续（最简单，快速跑通链路）→ 横盘起跳 → N型起跳（最复杂放最后）
4. **批次四做完立刻跑批次六的 6.1/6.2**（案例验证），不用等 UI
5. **批次五和6.3/6.4可最后收尾**

---

## 八、风险与注意事项

1. **向后兼容**：选股页（`screening_page.py`）也调用 `screen_single_stock`，需确认改造后接口兼容，或同步更新选股页的调用方式。

2. **序列化**：进程池 `_worker_screen_stock` 通过 dict 序列化结果跨进程传递。`ScoreBreakdown` 需要提供 `to_dict()` / `from_dict()` 方法，或在序列化时展平。

3. **性能**：新增 MA14/MA28/MA57/MA114 和砖色切换次数计算，额外开销很小（`moving_average` 是 O(n) 的滑窗均值）。回调阶段算法和波段识别算法是 O(30) 量级的回溯，不会影响批量选股性能。

4. **前提条件保留**：V2 仍要求"绿转红 + 短趋线>多空线"作为前提，但"力度达标(≥0.5)"这个前提在 V2 中被弱化——低力度不再直接否决，而是在通用质量的"翻红力度比"中拿低分。建议保留前提中的力度检查但降低阈值（如 ≥0.3），或完全移除力度前提让评分体系自行处理。

5. **`LONG_SIDEWAYS` 规则删除**：现有的"横盘时间过长"风险规则在 V2 中不再使用，改为"假横盘"（砖色切换<3）和"趋势衰竭"（斜率≤0）两条更精确的规则。删除时注意 `RiskFilterType` 枚举和引用处同步清理。
