# 通达信选股条件适配设计文档

## 一、背景与目标

### 1.1 背景
当前系统的选股条件模板使用 JSON 格式的结构化表达式定义，用户需要手动构建 AST（抽象语法树）格式的条件，学习成本高，易用性差。

通达信是国内最常用的股票分析软件之一，其选股条件语法简洁直观，用户群体广泛。支持直接导入通达信选股条件可大幅降低用户使用门槛。

### 1.2 目标
1. 支持直接粘贴通达信选股条件代码创建模板
2. **移除原有的 JSON 条件模式，统一使用通达信模式**
3. 提供通达信语法到内部表达式的自动转换
4. 扩展指标函数支持，确保通达信常用函数可用

### 1.3 废弃内容
以下内容将被移除：
- `ScreeningTemplate.condition` 字段（JSON 结构化条件）
- `core/expression/builder.py` 中的 `build_expression` 函数调用
- `core/expression/validator.py` 中的 `validate_expression` 函数调用
- 模板编辑对话框中的 JSON 条件编辑区域

---

## 二、通达信语法分析

### 2.1 语法特性

| 特性 | 示例 | 说明 |
|------|------|------|
| 变量赋值 | `VAR1A:=(HHV(HIGH,4)-CLOSE)/(HHV(HIGH,4)-LLV(LOW,4))*100-90;` | 使用 `:=` 定义变量 |
| 输出变量 | `砖型图:=IF(VAR6A>4,VAR6A-4,0),COLORRED;` | 变量可命名并附带样式 |
| 中文变量 | `当日收盘价:=C;` | 支持中文命名 |
| 行内注释 | `{基础变量定义}` | 大括号包裹 |
| 字段简写 | `C` (CLOSE)、`H` (HIGH)、`L` (LOW)、`O` (OPEN)、`V` (VOL) | 单字母简写 |
| 周期引用 | `REF(变量, N)` | 引用 N 周期前的值 |
| 条件输出 | `选股:条件表达式;` | 最终选股结果 |

### 2.2 支持的函数

#### 已支持函数（原有）
- `MA(X, N)` - 简单移动平均
- `EMA(X, N)` - 指数移动平均
- `SMA(X, N, M)` - 通达信加权移动平均
- `HHV(X, N)` - N 周期最高值
- `LLV(X, N)` - N 周期最低值
- `REF(X, N)` - N 周期前的值
- `MAX(A, B)` - 取大值
- `MIN(A, B)` - 取小值
- `ABS(X)` - 绝对值

#### 已新增函数（已完成）
- `IF(条件, 真值, 假值)` - 条件判断 ✅
- `CROSS(线1, 线2)` - 上穿判断 ✅
- `COUNT(条件, N)` - N 周期内满足条件的次数 ✅
- `SUM(X, N)` - N 周期求和 ✅
- `BETWEEN(X, A, B)` - 区间判断（闭区间） ✅
- `RANGE(X, A, B)` - 开区间判断 ✅
- `EVERY(条件, N)` - N 周期内全部满足 ✅
- `EXIST(条件, N)` - N 周期内存在满足 ✅
- `BARSLAST(条件)` - 距离上次满足条件的周期数 ✅
- `STD(X, N)` - N 周期标准差 ✅

### 2.3 字段别名映射

| 通达信简写 | 标准字段 | 说明 |
|------------|----------|------|
| C | close | 收盘价 |
| O | open | 开盘价 |
| H | high | 最高价 |
| L | low | 最低价 |
| V | volume | 成交量 |
| VOL | volume | 成交量 |
| AMOUNT | amount | 成交额 |

---

## 三、系统架构设计

### 3.1 模块架构

```
core/
├── expression/
│   ├── nodes.py          # 表达式节点定义（保留）
│   ├── evaluator.py      # 表达式执行器（保留）
│   ├── builder.py        # [删除] 不再需要
│   ├── validator.py      # [删除] 不再需要
│   └── parser/
│       ├── __init__.py
│       ├── tdx_lexer.py      # 通达信词法分析器 [新增]
│       ├── tdx_parser.py     # 通达信语法分析器 [新增]
│       └── tdx_transpiler.py # 通达信到AST转换器 [新增]
├── indicators/
│   ├── registry.py       # 函数注册表（保留，需扩展）
│   ├── builtin.py        # 内置函数实现（保留，需扩展）
│   └── tdx_compat.py     # 通达信兼容层（保留，需扩展）
└── models/
    └── template.py       # 模板模型（修改：用 tdx_source 替换 condition）
```

### 3.2 数据流

```
通达信条件文本
      │
      ▼
┌─────────────────┐
│   TdxLexer      │  词法分析：文本 → Token 序列
└─────────────────┘
      │
      ▼
┌─────────────────┐
│   TdxParser     │  语法分析：Token → Tdx AST
└─────────────────┘
      │
      ▼
┌─────────────────┐
│  TdxTranspiler  │  语义转换：Tdx AST → 内部 ExpressionNode
└─────────────────┘
      │
      ▼
┌─────────────────┐
│ ScreeningTemplate│  模板存储（仅 tdx_source 字段）
└─────────────────┘
      │
      ▼
┌─────────────────┐
│ Evaluator       │  执行选股（运行时转换并执行）
└─────────────────┘
```

---

## 四、详细设计

### 4.1 词法分析器 (TdxLexer)

**职责**：将通达信条件文本拆分为 Token 序列

**Token 类型**：
```python
class TdxTokenKind(Enum):
    # 字面量
    NUMBER = "NUMBER"        # 数字：123, 3.14
    STRING = "STRING"        # 字符串（较少用）
    
    # 标识符
    IDENTIFIER = "IDENTIFIER"  # 变量名、函数名
    
    # 运算符
    PLUS = "+"               # 加
    MINUS = "-"              # 减
    STAR = "*"               # 乘
    SLASH = "/"              # 除
    ASSIGN = ":="            # 赋值
    OUTPUT = ":"             # 输出定义
    
    # 比较运算符
    GT = ">"                 # 大于
    GE = ">="                # 大于等于
    LT = "<"                 # 小于
    LE = "<="                # 小于等于
    EQ = "="                 # 等于（注意：通达信用单等号）
    NE = "<>"                # 不等于
    
    # 逻辑运算符
    AND = "AND"              # 与
    OR = "OR"                # 或
    
    # 括号
    LPAREN = "("
    RPAREN = ")"
    COMMA = ","
    SEMICOLON = ";"          # 语句结束
    
    # 特殊
    COMMENT = "COMMENT"      # {...} 注释
    EOF = "EOF"
```

**实现要点**：
- 支持中文标识符（Unicode 字符）
- 自动过滤注释内容
- 处理字段简写（C、H、L、O、V）

### 4.2 语法分析器 (TdxParser)

**职责**：将 Token 序列解析为通达信 AST

**语法规则（EBNF）**：
```
program      → statement* EOF
statement    → assignment | output | expression_stmt
assignment   → IDENTIFIER ":=" expression ";"
output       → IDENTIFIER ":" expression ("," style)? ";"
expression   → logical_expr
logical_expr → comparison_expr (("AND" | "OR") comparison_expr)*
comparison   → additive_expr ((">" | ">=" | "<" | "<=" | "=" | "<>") additive_expr)?
additive     → multiplicative_expr (("+" | "-") multiplicative_expr)*
multiplicative → unary_expr (("*" | "/") unary_expr)*
unary        → ("-" | "+")? primary
primary      → NUMBER | IDENTIFIER | function_call | "(" expression ")"
function_call → IDENTIFIER "(" arguments? ")"
arguments    → expression ("," expression)*
```

**AST 节点设计**：
```python
@dataclass
class TdxAssignment:
    name: str
    value: TdxExpression
    
@dataclass
class TdxOutput:
    name: str
    value: TdxExpression
    style: str | None
    
@dataclass
class TdxProgram:
    statements: list[TdxStatement]
    variables: dict[str, TdxExpression]  # 变量表
```

### 4.3 转换器 (TdxTranspiler)

**职责**：将通达信 AST 转换为内部 ExpressionNode

**转换策略**：

1. **变量内联展开**
   - 收集所有变量定义
   - 在最终表达式处展开所有变量引用
   - 避免循环依赖

2. **函数映射**
   ```python
   FUNCTION_MAP = {
       "MA": "MA",
       "EMA": "EMA",
       "SMA": "SMA",
       "HHV": "HHV",
       "LLV": "LLV",
       "REF": "REF",
       "IF": "IF",        # 新增
       "CROSS": "CROSS",  # 新增
       # ...
   }
   ```

3. **字段映射**
   ```python
   FIELD_MAP = {
       "C": "CLOSE",
       "O": "OPEN",
       "H": "HIGH",
       "L": "LOW",
       "V": "VOLUME",
       "VOL": "VOLUME",
   }
   ```

4. **运算符转换**
   - `=` → `==`（等于比较）
   - `<>` → `!=`（不等于）
   - `AND` → `and`
   - `OR` → `or`

### 4.4 模板模型改造

**ScreeningTemplate 字段变更**：
```python
@dataclass
class ScreeningTemplate:
    id: str
    name: str
    description: str = ""
    tdx_source: str = ""                    # 通达信原始代码（替换原 condition）
    default_time_mode: str = "exact"
    stock_pool_name: str = "default"
    include_debug: bool = False
    created_at: str = ""
    updated_at: str = ""
```

**变更说明**：
- 移除 `condition: dict[str, Any] | None` 字段
- 新增 `tdx_source: str` 字段，直接存储通达信代码文本
- 选股执行时动态解析转换，不再缓存结构化条件

### 4.5 指标函数扩展

**新增函数实现**：
```python
# core/indicators/builtin.py

def if_series(condition, true_value, false_value) -> np.ndarray:
    """条件判断函数"""
    cond = _as_bool_array(condition)
    true_arr = _to_float_array(true_value)
    false_arr = _to_float_array(false_value)
    return np.where(cond, true_arr, false_arr)

def cross(line1, line2) -> np.ndarray:
    """上穿判断：line1 上穿 line2"""
    l1 = _to_float_array(line1)
    l2 = _to_float_array(line2)
    prev_cross = l1[:-1] <= l2[:-1]
    curr_cross = l1[1:] > l2[1:]
    result = np.zeros(len(l1), dtype=bool)
    result[1:] = prev_cross & curr_cross
    return result

def count(condition, period: int) -> np.ndarray:
    """统计周期内满足条件的次数"""
    arr = _as_bool_array(condition)
    return pd.Series(arr).rolling(window=period).sum().to_numpy()

def sum_series(values, period: int) -> np.ndarray:
    """周期求和"""
    arr = _to_float_array(values)
    return pd.Series(arr).rolling(window=period).sum().to_numpy()

def between(value, low, high) -> np.ndarray:
    """区间判断（闭区间）"""
    v = _to_float_array(value)
    l = _to_float_array(low)
    h = _to_float_array(high)
    return (v >= l) & (v <= h)
```

---

## 五、UI 改造设计

### 5.1 模板编辑对话框改造

**简化为单一通达信输入模式**：
```
┌──────────────────────────────────────────────────────────────┐
│ 编辑模板                                                      │
├──────────────────────────────────────────────────────────────┤
│ ┌─ 模板信息 ────────────────────────────────────────────────┐│
│ │ 名称: [砖型图绿转红选股        ]                            ││
│ │ 描述: [基于砖型图指标，捕捉绿转红信号...                    ]│
│ │ 默认时间模式: [精确日期 ▼]                                  ││
│ └────────────────────────────────────────────────────────────┘│
│                                                              │
│ ┌─ 通达信条件代码 ──────────────────────────────────────────┐│
│ │ VAR1A:=(HHV(HIGH,4)-CLOSE)/(HHV(HIGH,4)-LLV(LOW,4))*100-90││
│ │ VAR2A:=SMA(VAR1A,4,1)+100;                                ││
│ │ ...                                                        ││
│ │ 选股:绿转红 AND 有效红柱 AND 有效绿柱...                   ││
│ └────────────────────────────────────────────────────────────┘│
│                                                              │
│ [校验语法]                                                    │
│                                                              │
│                          [确定] [取消]                        │
└──────────────────────────────────────────────────────────────┘
```

**功能点**：
1. 移除原有的 JSON 条件编辑区域
2. 直接使用通达信代码文本编辑器
3. "校验语法"按钮执行解析验证
4. 保存时仅存储 `tdx_source` 字段

### 5.2 模板列表展示

**简化展示**：
- 名称、描述、默认时间模式、更新时间
- 移除"来源类型"列（不再区分条件模式）

---

## 六、实施计划

### 阶段一：核心解析能力（3天）✅ 已完成
- [x] 实现 TdxLexer 词法分析器
- [x] 实现 TdxParser 语法分析器
- [x] 实现 TdxTranspiler 转换器
- [x] 添加单元测试

### 阶段二：指标函数扩展（2天）✅ 已完成
- [x] 实现 IF 函数
- [x] 实现 CROSS 函数
- [x] 实现 COUNT/SUM 函数
- [x] 实现 BETWEEN/RANGE 函数
- [x] 实现 EVERY/EXIST/BARSLAST/STD 函数
- [x] 更新函数注册表

### 阶段三：模型与服务层改造（1天）✅ 已完成
- [x] ScreeningTemplate 移除 condition，添加 tdx_source
- [x] TemplateService 适配新字段
- [x] 删除 builder.py 和 validator.py（不再需要）
- [x] 更新选股执行流程（ScreeningEngine 集成解析器）

### 阶段四：UI 改造（1天）✅ 已完成
- [x] 模板编辑对话框简化为通达信代码输入
- [x] 移除 JSON 条件编辑区域
- [x] 更新模板详情展示

### 阶段五：清理与测试（1天）✅ 已完成
- [x] 删除废弃代码（builder.py, validator.py, 相关测试）
- [x] 更新测试用例
- [x] 集成测试恢复运行

---

## 七、风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| 通达信语法变体多 | 解析器复杂度高 | 先支持核心语法，逐步扩展 |
| 循环变量引用 | 解析死循环 | 检测循环依赖，报错提示 |
| 函数行为差异 | 选股结果不准确 | 严格对照通达信文档，编写对照测试 |
| 旧模板数据丢失 | condition 字段废弃 | 提供迁移脚本，或手动重新创建 |

---

## 八、测试用例

### 8.1 词法分析测试

```python
def test_tokenize_assignment():
    code = "VAR1:=MA(CLOSE,5);"
    tokens = TdxLexer(code).tokenize()
    assert tokens[0].kind == TdxTokenKind.IDENTIFIER
    assert tokens[0].value == "VAR1"
    assert tokens[1].kind == TdxTokenKind.ASSIGN

def test_tokenize_chinese_identifier():
    code = "当日收盘价:=C;"
    tokens = TdxLexer(code).tokenize()
    assert tokens[0].value == "当日收盘价"
```

### 8.2 语法分析测试

```python
def test_parse_assignment():
    code = "VAR1:=MA(CLOSE,5);"
    program = TdxParser(TdxLexer(code).tokenize()).parse()
    assert len(program.statements) == 1
    assert program.statements[0].name == "VAR1"

def test_parse_complex_condition():
    code = """
    短趋线:=EMA(EMA(C,10),10);
    选股:C > 短趋线;
    """
    program = TdxParser(TdxLexer(code).tokenize()).parse()
    assert "选股" in program.outputs
```

### 8.3 转换测试

```python
def test_transpile_simple():
    code = "选股:CLOSE > MA(CLOSE,5);"
    program = TdxParser(TdxLexer(code).tokenize()).parse()
    node = TdxTranspiler(program).transpile()
    assert isinstance(node, ComparisonNode)
    assert node.operator == ">"
```

---

## 九、附录

### 9.1 示例：砖型图选股条件转换

**通达信源码**：
```
VAR1A:=(HHV(HIGH,4)-CLOSE)/(HHV(HIGH,4)-LLV(LOW,4))*100-90;
VAR2A:=SMA(VAR1A,4,1)+100;
...
选股:绿转红 AND 有效红柱 AND 有效绿柱 AND 长度条件 AND 股价区间 AND 趋势条件 AND 价格条件;
```

**转换后结构化条件**：
```json
{
  "kind": "logical",
  "operator": "and",
  "operands": [
    {
      "kind": "comparison",
      "operator": "=",
      "left": {
        "kind": "function",
        "name": "REF",
        "args": [
          { "kind": "identifier", "name": "AA" },
          { "kind": "constant", "value": 1 }
        ]
      },
      "right": { "kind": "constant", "value": 0 }
    },
    ...
  ]
}
```

### 9.2 参考资料
- 通达信公式编辑器帮助文档
- 通达信函数大全
- 现有项目选股逻辑开发设计文档

---

## 十、实现清单（已完成）

### 10.1 新增文件

| 文件路径 | 说明 | 行数 |
|----------|------|------|
| `core/expression/parser/__init__.py` | 解析器模块导出 | 17 |
| `core/expression/parser/lexer.py` | 词法分析器 | 306 |
| `core/expression/parser/parser.py` | 语法分析器 | 375 |
| `core/expression/parser/transpiler.py` | AST 转换器 | 180 |
| `tests/unit/test_tdx_lexer.py` | 词法分析器测试 | 128 |
| `tests/unit/test_tdx_parser.py` | 语法/转换器测试 | 196 |

### 10.2 修改文件

| 文件路径 | 变更内容 |
|----------|----------|
| `core/models/template.py` | `condition` → `tdx_source` |
| `core/models/screening.py` | `condition` → `tdx_source` |
| `core/templates/service.py` | 移除 condition 相关逻辑，适配 tdx_source |
| `core/templates/builtin.py` | 4 个默认模板改用通达信语法 |
| `core/indicators/builtin.py` | 新增 IF/CROSS/COUNT/SUM 等函数 |
| `core/indicators/registry.py` | 注册新增函数 |
| `core/screening/engine.py` | 集成通达信解析器 |
| `app/dialogs/template_editor_dialog.py` | JSON 编辑器 → 通达信代码编辑器 |
| `app/pages/template_page.py` | 条件详情展示改为通达信代码 |
| `templates.json` | 模板数据改用 tdx_source |
| `tests/conftest.py` | fixture 更新 |
| `tests/unit/test_template_service.py` | 测试更新 |
| `tests/integration/test_screening_*.py` | 恢复运行 |

### 10.3 删除文件

| 文件路径 | 说明 |
|----------|------|
| `core/expression/builder.py` | 不再需要的表达式构建器 |
| `core/expression/validator.py` | 不再需要的表达式校验器 |
| `tests/unit/test_expression_builder.py` | 对应测试 |
| `tests/unit/test_expression_validator.py` | 对应测试 |
| `tests/unit/test_expression_evaluator.py` | 对应测试 |

### 10.4 使用示例

```python
from core.expression.parser import transpile_tdx_source

# 简单条件
node = transpile_tdx_source("选股:C > MA(C,5);")

# 复杂条件（砖型图）
code = '''
VAR1A:=(HHV(H,4)-C)/(HHV(H,4)-LLV(L,4))*100-90;
VAR2A:=SMA(VAR1A,4,1)+100;
砖型图:=IF(VAR6A>4,VAR6A-4,0);
选股:砖型图>0;
'''
node = transpile_tdx_source(code)

# 在选股服务中使用
from core.screening.service import ScreeningService
from core.models.screening import ScreeningRequest

request = ScreeningRequest(
    tdx_source="选股:C > MA(C,5);",
    target_date="2026-03-27",
    time_mode="exact",
)
result = ScreeningService.from_root(root).screen(request)
```
