# 选股逻辑开发设计文档

## 1. 背景与目标

当前项目是一个本地股票查看器，已经具备以下基础能力：

- 股票列表加载；
- 单只股票日线数据读取；
- 图表展示；
- 历史更新；
- hover 某个时间点查看行情与部分指标值。

用户当前希望在此基础上，增加类似通达信软件中的“选股逻辑”能力：给定具体选股条件，在某个时间点对股票池进行批量筛选，输出满足条件的股票集合。

从现有代码看：

- `E:\charts\app\data_loader.py` 已提供股票列表和单股日线 CSV 的标准加载能力；
- `E:\charts\app\main.py` 负责 GUI、股票切换与“更新全部股票”等交互；
- `E:\charts\app\widgets.py` 中包含部分可复用的指标算法（如 rolling max/min、通达信式 SMA、KDJ、部分自定义指标），但这些逻辑目前耦合在图表组件内，尚未形成可复用的指标层与选股引擎。

因此，本阶段需要先整理出一份可执行的开发设计文档，明确 MVP 范围、模块边界、实施路径及后续扩展方向。

---

## 2. 建设目标

本次设计的目标不是一步到位实现完整通达信公式系统，而是先建立一套可演进、可落地、与现有 GUI 解耦的选股能力框架。

核心目标包括：

1. 支持在指定交易日或指定时间点语义下，对股票池进行批量选股；
2. 支持 MVP 级别的条件表达能力，而不是一次性实现完整通达信 DSL；
3. 将现有 `widgets.py` 中耦合在 GUI 内的指标逻辑抽离，沉淀为独立指标层；
4. 建立统一的数据访问、时间点定位、表达式执行和选股结果模型；
5. 保持 `main.py` 主要负责 UI 编排，不承担核心选股算法。

---

## 3. 范围定义

### 3.1 本期要解决的问题

本期设计针对以下能力展开：

- 读取默认股票池（来自 `stocklist.csv`）；
- 批量加载股票日线数据（`stock_daily_data/*.csv`）；
- 在某个指定日期上，对每只股票执行选股条件；
- 输出满足条件的股票列表及命中信息；
- 为 GUI 接入预留标准服务接口。

### 3.2 本期不做的内容

为了控制复杂度，首期明确不做：

- 完整兼容通达信文本公式 DSL；
- 多周期混合计算（如日线+周线）；
- 实时行情或分钟级别数据；
- 回测引擎；
- 高级股票池编辑器；
- 分布式计算或数据库化存储；
- 复杂排序型选股或多策略编排。

### 3.3 推荐 MVP 范围

建议 MVP 只支持以下最小闭环：

- 单一日线周期；
- 单一时间点评估；
- 结构化条件模型；
- 常见字段与常见指标函数；
- 默认股票池和指定股票代码列表；
- 输出命中股票、命中日期、关键值摘要和错误信息。

---

## 4. 现状分析

### 4.1 现有关键文件

#### `E:\charts\app\main.py`
- 当前 GUI 主入口；
- 负责股票列表、搜索过滤、图表切换、配置保存、历史更新触发；
- 后续适合作为选股功能的 UI 接入层，不适合放核心筛选算法。

#### `E:\charts\app\data_loader.py`
- 已提供：
  - `load_stock_list()`
  - `load_daily_csv()`
  - `normalize_daily_dataframe()`
- 适合作为未来数据访问层的底座。

#### `E:\charts\app\widgets.py`
- 已含部分指标算法与“按时间点读取指标值”的逻辑；
- 这些能力具备较高复用价值，但当前与图表组件强耦合；
- 建议将纯计算逻辑抽离成独立指标层，供图表与选股共用。

#### `E:\charts\stocklist.csv`
- 当前默认股票池元数据来源。

#### `E:\charts\stock_daily_data\*.csv`
- 每只股票的日线数据源，是后续选股执行的核心输入。

### 4.2 已有能力

当前系统已经具备：

- 股票列表读取与基础过滤；
- 单只股票日线 CSV 读取与标准化；
- 图表展示与 hover 某日期读取行情；
- 部分指标算法实现；
- 历史更新能力。

### 4.3 当前缺口

当前系统尚缺少：

- 独立指标层；
- 表达式/条件模型；
- 批量扫描执行器；
- 统一时间点语义；
- 股票池管理与结果模型；
- 错误策略、缓存策略与系统化测试。

---

## 5. 总体架构设计

建议将选股能力从现有 GUI 中拆分为 5 层：

### 5.1 数据访问层
职责：
- 股票列表加载；
- 单股日线加载；
- 字段标准化；
- 按日期定位索引。

复用基础：
- `app/data_loader.py`

后续方向：
- 对外暴露统一 repository API，而不是让各模块直接读 CSV。

### 5.2 指标层
职责：
- 提供基础指标与通达信兼容函数计算；
- 支持图表展示与选股执行共用同一套指标实现。

复用基础：
- `app/widgets.py` 中已有的 rolling max/min、通达信式 SMA、KDJ、自定义指标逻辑。

### 5.3 表达式/条件层
职责：
- 定义选股条件；
- 执行字段、函数、比较、逻辑组合；
- 输出布尔结果。

首期建议：
- 采用结构化表达式树；
- 不直接实现完整通达信文本公式解析。

### 5.4 选股执行层
职责：
- 遍历股票池；
- 加载数据；
- 调用表达式与指标层执行计算；
- 汇总命中结果与错误。

### 5.5 UI 接入层
职责：
- 收集选股条件、目标日期、股票池范围；
- 调用选股服务；
- 展示结果并联动现有图表。

落点：
- `app/main.py`

---

## 6. 模块设计与建议新增文件

建议新增独立 `core` 目录，避免继续把选股逻辑堆在 `app/main.py` 或 `app/widgets.py` 中。

### 6.1 模型层
- `E:\charts\core\models\market.py`
  - 定义股票基础信息、日线标准字段模型。
- `E:\charts\core\models\expression.py`
  - 定义表达式节点模型：字段、常量、函数、比较、逻辑。
- `E:\charts\core\models\screening.py`
  - 定义选股请求、单股评估结果、整体结果、错误项。
- `E:\charts\core\models\stock_pool.py`
  - 定义股票池及其来源。

### 6.2 数据层
- `E:\charts\core\data\repository.py`
  - 封装 `load_stock_list()`、`load_daily_csv()`，统一对外提供数据访问 API。
- `E:\charts\core\data\time_index.py`
  - 统一处理时间点定位、精确日期与向前对齐语义。
- `E:\charts\core\data\cache.py`
  - 管理数据缓存与指标缓存。

### 6.3 指标层
- `E:\charts\core\indicators\builtin.py`
  - 放置 MA、EMA、REF、HHV、LLV、SMA 等基础函数。
- `E:\charts\core\indicators\tdx_compat.py`
  - 放置通达信兼容计算语义，如 `_tdx_sma` 迁移后的实现。
- `E:\charts\core\indicators\registry.py`
  - 提供指标/函数注册表。

### 6.4 表达式层
- `E:\charts\core\expression\nodes.py`
  - 表达式节点定义。
- `E:\charts\core\expression\validator.py`
  - 校验表达式合法性。
- `E:\charts\core\expression\evaluator.py`
  - 表达式求值器。
- `E:\charts\core\expression\builder.py`
  - 从结构化配置构建表达式树；后续可扩展为文本公式 parser 入口。

### 6.5 选股层
- `E:\charts\core\screening\engine.py`
  - 批量选股执行引擎。
- `E:\charts\core\screening\service.py`
  - 供 GUI 调用的高层服务封装。
- `E:\charts\core\screening\result_formatter.py`
  - 结果格式化、摘要输出。
- `E:\charts\core\screening\error_policy.py`
  - 定义 skip/strict/coerce_false 等策略。

### 6.6 股票池层
- `E:\charts\core\stock_pool\manager.py`
  - 管理默认股票池与指定股票列表。

---

## 7. 表达式与条件模型设计

### 7.1 推荐首期实现方式

首期不建议直接做完整通达信 DSL，而采用“结构化表达式 + 解释执行”的方式。理由：

- 实现成本更低；
- 错误提示更清晰；
- 更易于测试；
- 更适合先完成 MVP 闭环；
- 后续可以逐步向通达信语法兼容演进。

### 7.2 MVP 支持的节点类型

#### 常量节点
- 数值；
- 布尔值。

#### 字段节点
- `OPEN`
- `HIGH`
- `LOW`
- `CLOSE`
- `VOLUME`

#### 函数节点
首期优先支持：
- `MA`
- `EMA`
- `SMA`
- `HHV`
- `LLV`
- `REF`
- `MAX`
- `MIN`
- `ABS`
- 可选支持：`CROSS`

#### 比较节点
- `>`
- `>=`
- `<`
- `<=`
- `==`
- `!=`

#### 逻辑节点
- `AND`
- `OR`
- `NOT`

#### 数学运算节点
- `+`
- `-`
- `*`
- `/`

### 7.3 条件执行约束

- 表达式最终必须返回布尔值；
- MVP 不支持赋值语句；
- 不支持多语句脚本；
- 不支持绘图语义；
- 不支持排序型选股。

---

## 8. 时间点语义设计

时间点语义必须统一放在系统层处理，不能分散在 UI、指标或选股引擎中。

### 8.1 支持模式

#### `exact`
- 仅当该股票存在指定日期数据时才评估；
- 若没有该日期数据，则视为未命中或返回时间点缺失。

#### `on_or_before`
- 若目标日期无数据，则取不晚于该日期的最近交易日评估；
- 更适合用户输入自然日或非交易日时的业务需求。

### 8.2 推荐默认方案

MVP 默认建议同时支持这两种模式，并在结果中明确输出：

- 用户指定日期；
- 实际评估日期；
- 若发生回退，记录回退原因。

### 8.3 窗口不足处理

例如：
- `MA(CLOSE, 20)` 在前 19 根 K 线上的结果应为 NA；
- 在布尔比较中默认按 false 处理；
- 同时记录调试信息，方便定位命中失败原因。

---

## 9. 数据流与执行流程

整体执行流程建议如下：

1. UI 收集选股条件、目标日期、时间点模式、股票池范围；
2. `screening.service` 构建 `ScreeningRequest`；
3. `screening.engine` 获取股票池；
4. 对每只股票依次执行：
   - 从 repository 加载日线数据；
   - 通过 `time_index` 定位目标索引；
   - 调用表达式求值器执行条件；
   - 生成单股结果；
5. 汇总为整体 `ScreeningResult`；
6. GUI 展示命中列表；
7. 点击命中股票时复用现有 `main.py + StockChartWidget` 联动图表。

单只股票评估流程可进一步抽象为：

- 输入：股票代码、目标日期、表达式；
- 读取并标准化该股票日线数据；
- 定位目标时间点索引；
- 执行字段/函数/比较/逻辑求值；
- 输出是否命中、命中日期、关键值摘要、错误信息。

---

## 10. 核心接口建议

### 10.1 数据仓库接口
建议统一提供：

- 获取股票列表；
- 获取单股日线数据；
- 获取目标日期对应索引。

要求：
- 字段标准化；
- 日期升序；
- 缺列时提供清晰错误信息。

### 10.2 表达式接口
建议提供：

- 表达式校验；
- 表达式求值。

校验内容包括：
- 字段是否合法；
- 函数是否合法；
- 参数个数是否合法；
- 运算符是否合法。

求值输出建议包括：
- 布尔结果；
- 调试值；
- 失败原因。

### 10.3 选股服务接口
建议提供统一入口：
- 执行选股请求；
- 返回选股结果；
- 可选返回调试明细与错误明细。

---

## 11. 性能与缓存设计

首期只需适度优化，避免过度设计。

### 11.1 数据缓存
- 按股票代码缓存 DataFrame；
- 在同一轮选股中避免重复读取 CSV。

### 11.2 指标缓存
- 按“股票 + 函数名 + 参数”缓存指标序列；
- 避免反复计算 `MA(CLOSE, 5)`、`REF(CLOSE, 1)` 等常用表达式。

### 11.3 子表达式缓存
- 可在单股求值上下文中缓存中间节点结果；
- MVP 可选，不作为第一批必做项。

---

## 12. 错误处理策略

建议将错误分为三类：

### 12.1 数据错误
- 文件不存在；
- 列缺失；
- 日期解析失败；
- 数据为空；
- 重复日期或排序异常。

### 12.2 表达式错误
- 未知字段；
- 未知函数；
- 参数错误；
- 不支持的运算。

### 12.3 执行错误
- 目标日期无法定位；
- 历史窗口不足；
- 除零；
- 结果为 NA。

### 12.4 默认错误策略建议
- 引擎层：`skip_symbol`，单只股票出错时跳过，继续扫描其余股票；
- 表达式局部：缺失值默认按 false 处理；
- 结果中保留错误摘要和前若干条错误明细。

---

## 13. 测试与验证方案

### 13.1 单元测试
建议覆盖：

- `data_loader/repository`：CSV 读取、字段标准化、日期排序、缺列报错；
- `indicators`：MA、EMA、SMA、HHV、LLV、REF 等结果正确；
- `expression`：字段读取、函数嵌套、逻辑组合、NA 处理；
- `time_index`：`exact` 与 `on_or_before` 模式行为正确。

### 13.2 集成测试
- 使用真实股票 CSV 对多只股票执行简单条件；
- 校验输出命中列表、实际评估日期、错误列表。

### 13.3 回归验证
- 将 `widgets.py` 中原有 hover/指标展示逻辑与新指标层结果做一致性比对，确保抽离后不影响现有图表功能。

### 13.4 推荐验证条件样例
- `CLOSE > MA(CLOSE, 5)`
- `CLOSE > REF(CLOSE, 1)`
- `HIGH >= HHV(HIGH, 20)`
- `VOLUME > REF(VOLUME, 1) * 2`
- 若实现 `CROSS`，可增加：`CROSS(CLOSE, MA(CLOSE, 10))`

---

## 14. 分阶段实施计划

### Phase 1：基础解耦
- 新建 `core/data`、`core/models`、`core/indicators`；
- 迁移并统一指标算法；
- 建立 repository 和时间点定位工具。

### Phase 2：表达式能力
- 定义表达式节点模型；
- 实现 validator 与 evaluator；
- 接入基础函数注册表。

### Phase 3：选股引擎
- 实现股票池、选股请求、批量扫描与结果汇总；
- 完成缓存与错误策略。

### Phase 4：GUI 接入
- 在 `app/main.py` 中新增选股入口、结果列表与联动查看；
- 复用现有单股图表展示逻辑。

### Phase 5：增强迭代
- 增加文本公式输入；
- 扩展通达信函数集合；
- 增加条件模板保存、导出结果、自定义股票池。

---

## 15. 关键风险与取舍

### 风险 1：过早追求完整通达信兼容
- 影响：复杂度迅速失控，开发周期不可控；
- 取舍：先做结构化表达式 MVP。

### 风险 2：指标继续留在 `widgets.py`
- 影响：图表与选股逻辑重复维护；
- 取舍：优先抽离纯计算逻辑。

### 风险 3：时间点语义不统一
- 影响：选股结果难解释、用户感知不一致；
- 取舍：统一在 `time_index` 中实现并对外暴露清晰模式。

### 风险 4：本地 CSV 数据质量不一致
- 影响：扫描过程容易被脏数据打断；
- 取舍：在 repository 层统一标准化与容错。

### 风险 5：过早做复杂性能优化
- 影响：系统复杂度升高，阻碍 MVP 落地；
- 取舍：首期先做数据缓存和指标缓存。

---

## 16. 关键复用点与改造建议

### 16.1 复用点
- `E:\charts\app\data_loader.py::load_stock_list`
- `E:\charts\app\data_loader.py::load_daily_csv`
- `E:\charts\app\data_loader.py::normalize_daily_dataframe`
- `E:\charts\app\main.py::apply_filter`（可继续作为 GUI 股票池前置过滤）
- `E:\charts\app\main.py::on_select`（后续可复用为选股结果点击联动入口）
- `E:\charts\app\widgets.py` 中现有 rolling max/min、通达信式 SMA、KDJ、自定义指标计算逻辑（需要迁移到独立指标层后复用）

### 16.2 后续需要修改的关键文件
- `E:\charts\app\widgets.py`：抽离纯指标算法，改为调用新的指标层；
- `E:\charts\app\main.py`：后续作为选股 UI 接入点，增加调用入口与结果展示；
- `E:\charts\app\data_loader.py`：继续作为读取底座，并逐步配合 repository 收敛职责。

---

## 17. 验证方案

后续正式开发时建议按以下顺序验证：

1. 对 `repository` 做真实 CSV 读取验证，确认能够正确读取股票列表和单股日线数据，并完成统一字段与日期排序；
2. 对从 `widgets.py` 抽离出的指标函数做单元测试，并与现有图表显示结果比对，确保数值一致；
3. 使用 3~5 个简单条件进行集成验证，例如：
   - `CLOSE > MA(CLOSE, 5)`
   - `CLOSE > REF(CLOSE, 1)`
   - `HIGH >= HHV(HIGH, 20)`
   - `VOLUME > REF(VOLUME, 1) * 2`
4. 分别测试 `exact` 和 `on_or_before` 两种时间点模式，确认实际评估日期与命中结果正确；
5. GUI 接入后，验证选股结果点击是否能够正确联动现有图表显示当前股票。

---

## 18. 开发任务拆分与优先级规划

### 18.1 任务拆分原则
- 以 MVP 为主线拆分任务，优先保证“能跑通一条最小选股链路”；
- 优先完成底层通用能力，再接入上层功能；
- 先做高复用、低争议、强依赖项，再做界面和增强功能；
- 每个任务都尽量具备明确输入、输出、验收标准；
- 尽量避免把大任务写得过粗，确保后续便于更新状态。

### 18.2 优先级定义
建议采用 4 级优先级：

- **P0：核心阻塞项**
  - 不完成就无法继续主链路开发；
  - 属于最先落地的基础能力。

- **P1：MVP 主链路任务**
  - 直接决定 MVP 是否可用；
  - 在 P0 完成后优先推进。

- **P2：MVP 体验完善项**
  - 不影响最小闭环，但影响可维护性、易用性和稳定性；
  - 适合在主链路跑通后补齐。

- **P3：后续增强项**
  - 不纳入首期交付；
  - 面向后续扩展，如文本公式、模板管理、更多通达信兼容能力等。

### 18.3 任务阶段划分
建议整体分为 6 个阶段推进：

1. 基础准备阶段
2. 数据与时间点能力阶段
3. 指标层建设阶段
4. 表达式与求值阶段
5. 选股执行链路阶段
6. GUI 接入与验证阶段

### 18.4 开发任务清单

#### 阶段一：基础准备阶段

| 编号 | 任务名称 | 优先级 | 主要输出 | 依赖 | 验收标准 |
|---|---|---|---|---|---|
| T1 | 建立目录与模块骨架 | P0 | `core` 目录及子模块骨架 | 无 | 模块可导入，不影响现有程序启动 |
| T2 | 统一模型定义 | P0 | `core/models/*.py` | T1 | 股票、股票池、选股请求、选股结果、表达式节点等模型边界明确 |

#### 阶段二：数据与时间点能力阶段

| 编号 | 任务名称 | 优先级 | 主要输出 | 依赖 | 验收标准 |
|---|---|---|---|---|---|
| T3 | 封装股票列表与日线数据仓库 | P0 | `core/data/repository.py` | T1, T2 | 可通过统一接口拿到股票列表和标准化日线数据 |
| T4 | 定义时间点定位能力 | P0 | `core/data/time_index.py` | T3 | 支持 `exact` / `on_or_before` 两种模式并返回目标索引 |
| T5 | 增加基础缓存能力 | P2 | `core/data/cache.py` | T3 | 同一轮选股中重复读取同一股票可命中缓存 |

#### 阶段三：指标层建设阶段

| 编号 | 任务名称 | 优先级 | 主要输出 | 依赖 | 验收标准 |
|---|---|---|---|---|---|
| T6 | 从 `widgets.py` / `chart_indicators.py` 中梳理可复用指标算法 | P0 | 指标迁移清单 | T1 | 明确哪些函数迁移、哪些保留在 GUI |
| T7 | 实现基础指标函数库 | P1 | `core/indicators/builtin.py` | T3, T6 | MA/EMA/SMA/HHV/LLV/REF/MAX/MIN/ABS 可正确返回序列 |
| T8 | 实现通达信兼容计算能力 | P1 | `core/indicators/tdx_compat.py` | T6, T7 | 通达信兼容函数可统一调用，结果与原逻辑一致 |
| T9 | 建立指标注册表 | P1 | `core/indicators/registry.py` | T7, T8 | 表达式层可通过函数名找到正确实现 |
| T10 | 让图表层复用新指标层 | P2 | `app/widgets.py` 改造 | T7, T8, T9 | 图表指标显示与迁移前一致 |

##### T6 指标迁移清单（当前判断）

**优先迁移到 `core/indicators` 的纯计算能力**
- `app/chart_indicators.py::rolling_max`
- `app/chart_indicators.py::rolling_min`
- `app/chart_indicators.py::tdx_sma`
- `app/chart_indicators.py::moving_average`
- `app/chart_indicators.py::ema`
- `app/chart_indicators.py::compute_kdj_indicator`
- `app/chart_indicators.py::compute_brick_indicator`
- `app/chart_indicators.py::compute_zx_short_trend`
- `app/chart_indicators.py::compute_zx_long_short`
- `app/chart_indicators.py::ZX_MULTI_PERIODS`

**继续保留在 GUI / 图表层的能力**
- `app/widgets.py::StockChartWidget`
- `app/widgets.py::UpdateProgressDialog`
- hover 联动、十字光标、标签渲染、plot item 组合等 PySide6 / pyqtgraph 视图逻辑

**迁移策略说明**
- `chart_indicators.py` 已经是当前项目中最接近“独立指标层”的位置，后续 T7/T8 应优先从这里迁移，而不是从 GUI 组件本体中抽算法；
- `widgets.py` 当前主要负责图表交互与显示，后续 T10 的目标应是“调用新指标层”，而不是继续承载指标计算；
- 自定义指标（砖块、主力线、多空线、KDJ）在迁移后应优先保留与现有图表一致的输出字段，降低回归成本。

#### 阶段四：表达式与求值阶段

| 编号 | 任务名称 | 优先级 | 主要输出 | 依赖 | 验收标准 |
|---|---|---|---|---|---|
| T11 | 定义表达式节点结构 | P0 | `core/expression/nodes.py` | T2 | 表达式树足以描述 MVP 条件模型 |
| T12 | 实现表达式构建器 | P1 | `core/expression/builder.py` | T11 | 能将结构化配置正确构造成表达式树 |
| T13 | 实现表达式校验器 | P1 | `core/expression/validator.py` | T9, T11, T12 | 非法表达式可在执行前给出明确错误 |
| T14 | 实现表达式求值器 | P1 | `core/expression/evaluator.py` | T4, T9, T11, T12, T13 | 给定数据、表达式、目标索引可稳定返回布尔结果 |
| T15 | 增加表达式局部缓存与 NA 处理策略 | P2 | 表达式求值增强能力 | T14 | 复杂表达式重复子树不反复计算，异常情况行为可预期 |

#### 阶段五：选股执行链路阶段

| 编号 | 任务名称 | 优先级 | 主要输出 | 依赖 | 验收标准 |
|---|---|---|---|---|---|
| T16 | 实现股票池管理器 | P1 | `core/stock_pool/manager.py` | T2, T3 | 默认股票池和指定股票列表可输出统一对象 |
| T17 | 实现选股请求与结果对象流转 | P1 | 选股请求/结果模型流转闭环 | T2 | 执行器输入输出结构清晰稳定 |
| T18 | 实现批量选股引擎 | P1 | `core/screening/engine.py` | T4, T14, T16, T17 | 可对一组股票在指定时间点执行条件并返回结果集 |
| T19 | 实现错误策略与结果格式化 | P2 | `core/screening/error_policy.py`、`core/screening/result_formatter.py` | T18 | 结果可展示、可解释、便于排查错误 |
| T20 | 提供 GUI 友好的服务接口 | P1 | `core/screening/service.py` | T18, T19 | GUI 无需了解底层细节即可发起选股 |

#### 阶段六：GUI 接入与验证阶段

| 编号 | 任务名称 | 优先级 | 主要输出 | 依赖 | 验收标准 |
|---|---|---|---|---|---|
| T21 | 在主界面增加选股入口 | P1 | `app/main.py` 界面扩展 | T20 | 用户可以从 GUI 发起一次选股请求 |
| T22 | 增加选股结果展示区 | P1 | `app/main.py` 结果区域 | T21 | 选股结果可视化且便于查看 |
| T23 | 接通结果点击联动图表 | P2 | GUI 联动能力 | T22 | 点击结果股票后图表能正确切换 |
| T24 | 建立基础单元测试 | P1 | 单元测试用例 | T3, T4, T7, T14 | 核心模块具备自动化校验能力 |
| T25 | 建立选股集成测试 | P1 | 集成测试用例 | T18, T20 | MVP 主链路能通过完整测试 |
| T26 | 做图表回归验证 | P2 | 回归验证结果 | T10 | 图表层行为与迁移前保持一致 |

#### 后续增强阶段（非首期）

| 编号 | 任务名称 | 优先级 | 依赖 | 说明 |
|---|---|---|---|---|
| T27 | 支持文本公式输入 | P3 | T12, T14 | 从结构化表达式扩展到更自然的公式输入 |
| T28 | 扩展更多通达信函数 | P3 | T8, T9 | 增加 `COUNT`、`EVERY`、`EXIST`、`BARSLAST` 等函数 |
| T29 | 支持条件模板保存与复用 | P3 | T20, T21 | 提高重复使用效率 |
| T30 | 支持自定义股票池持久化 | P3 | T16 | 实现用户管理自己的股票池 |

### 18.5 推荐执行顺序
建议按以下顺序推进：

1. T1 模块骨架
2. T2 核心模型
3. T3 数据仓库
4. T4 时间点定位
5. T6 指标梳理
6. T7 基础指标函数
7. T8 通达信兼容函数
8. T9 指标注册表
9. T11 表达式节点
10. T12 表达式构建器
11. T13 表达式校验器
12. T14 表达式求值器
13. T16 股票池管理器
14. T17 选股请求与结果模型流转
15. T18 批量选股引擎
16. T20 GUI 服务接口
17. T24 基础单元测试
18. T25 集成测试
19. T21 GUI 入口
20. T22 结果展示
21. T10 图表层复用新指标层
22. T23 图表联动
23. T19 错误策略与结果格式化
24. T26 图表回归验证
25. T5 / T15 做性能与稳定性增强
26. T27 ~ T30 进入后续增强阶段

### 18.6 当前任务状态表

以下状态表可作为后续持续更新进展的统一入口，当前先初始化为“未开始”。

| 编号 | 任务名称 | 优先级 | 当前状态 | 依赖 | 最近更新说明 | 完成日期 |
|---|---|---|---|---|---|---|
| T1 | 建立目录与模块骨架 | P0 | 已完成 | 无 | 已完成 `core` 及子包骨架初始化 | 2026-03-29 |
| T2 | 统一模型定义 | P0 | 已完成 | T1 | 已完成首批 market / stock_pool / screening / expression 模型定义 | 2026-03-29 |
| T3 | 封装股票列表与日线数据仓库 | P0 | 已完成 | T1, T2 | 已新增 `core/data/repository.py`，复用现有 data_loader 能力 | 2026-03-29 |
| T4 | 定义时间点定位能力 | P0 | 已完成 | T3 | 已支持 `exact` / `on_or_before` 两种模式 | 2026-03-29 |
| T5 | 增加基础缓存能力 | P2 | 未开始 | T3 | - | - |
| T6 | 从 `widgets.py` / `chart_indicators.py` 中梳理可复用指标算法 | P0 | 已完成 | T1 | 已补充指标迁移清单，明确优先从 `chart_indicators.py` 迁移 | 2026-03-29 |
| T7 | 实现基础指标函数库 | P1 | 已完成 | T3, T6 | 已新增 `core/indicators/builtin.py`，接入 MA/EMA/SMA/HHV/LLV/REF/MAX/MIN/ABS 基础函数 | 2026-03-30 |
| T8 | 实现通达信兼容计算能力 | P1 | 已完成 | T6, T7 | 已新增 `core/indicators/tdx_compat.py`，接入 KDJ、ZX_SHORT_TREND、ZX_LONG_SHORT | 2026-03-30 |
| T9 | 建立指标注册表 | P1 | 已完成 | T7, T8 | 已新增 `core/indicators/registry.py`，支持函数查询、别名与参数个数约束 | 2026-03-30 |
| T10 | 让图表层复用新指标层 | P2 | 未开始 | T7, T8, T9 | - | - |
| T11 | 定义表达式节点结构 | P0 | 已完成 | T2 | 已新增 `core/expression/nodes.py`，覆盖常量/字段/函数/比较/逻辑/数学节点 | 2026-03-29 |
| T12 | 实现表达式构建器 | P1 | 已完成 | T11 | 已新增 `core/expression/builder.py`，支持结构化配置构造 AST | 2026-03-30 |
| T13 | 实现表达式校验器 | P1 | 已完成 | T9, T11, T12 | 已新增 `core/expression/validator.py`，支持字段/函数/参数/节点结构校验 | 2026-03-30 |
| T14 | 实现表达式求值器 | P1 | 已完成 | T4, T9, T11, T12, T13 | 已新增 `core/expression/evaluator.py`，并完成真实日线样例验证 | 2026-03-30 |
| T15 | 增加表达式局部缓存与 NA 处理策略 | P2 | 未开始 | T14 | - | - |
| T16 | 实现股票池管理器 | P1 | 已完成 | T2, T3 | 已新增 `core/stock_pool/manager.py`，支持默认股票池与指定代码股票池输出 | 2026-03-30 |
| T17 | 实现选股请求与结果对象流转 | P1 | 已完成 | T2 | 已新增 `core/screening/result_models.py`，补充单次执行结果与调试载荷结构 | 2026-03-30 |
| T18 | 实现批量选股引擎 | P1 | 已完成 | T4, T14, T16, T17 | 已新增 `core/screening/engine.py`，完成多股票遍历、时间点定位、表达式求值与结果汇总 | 2026-03-30 |
| T19 | 实现错误策略与结果格式化 | P2 | 已完成 | T18 | 已新增 `core/screening/error_policy.py` 与 `core/screening/result_formatter.py`，支持错误策略和结果摘要格式化 | 2026-03-30 |
| T20 | 提供 GUI 友好的服务接口 | P1 | 已完成 | T18, T19 | 已新增 `core/screening/service.py`，提供统一选股入口和摘要输出 | 2026-03-30 |
| T21 | 在主界面增加选股入口 | P1 | 已完成 | T20 | 已在 `app/main.py` 左侧控制区新增选股模板、日期、时间模式与执行按钮 | 2026-03-30 |
| T22 | 增加选股结果展示区 | P1 | 已完成 | T21 | 已新增选股结果表格并接通结果刷新显示 | 2026-03-30 |
| T23 | 接通结果点击联动图表 | P2 | 已完成 | T22 | 已通过结果表选中复用主表 `_select_symbol_in_table()` 链路切换图表 | 2026-03-30 |
| T24 | 建立基础单元测试 | P1 | 已完成 | T3, T4, T7, T14 | 已新增 `tests/conftest.py` 及 `tests/unit/` 下 time_index、expression、indicator_registry、stock_pool、screening_engine 等单元测试，并通过 pytest 验证 | 2026-03-30 |
| T25 | 建立选股集成测试 | P1 | 已完成 | T18, T20 | 已新增 `tests/integration/` 下 `ScreeningService` 主链路与时间模式集成测试，并通过 pytest 验证 | 2026-03-30 |
| T26 | 做图表回归验证 | P2 | 未开始 | T10 | - | - |
| T27 | 支持文本公式输入 | P3 | 未开始 | T12, T14 | - | - |
| T28 | 扩展更多通达信函数 | P3 | 未开始 | T8, T9 | - | - |
| T29 | 支持条件模板保存与复用 | P3 | 未开始 | T20, T21 | - | - |
| T30 | 支持自定义股票池持久化 | P3 | 未开始 | T16 | - | - |

### 18.7 建议的进展跟踪方式
后续建议在文档中以任务清单方式持续维护状态，推荐使用以下状态字段：

- `未开始`
- `进行中`
- `已完成`
- `已阻塞`
- `暂缓`

建议每个任务维护以下字段：
- 任务编号；
- 任务名称；
- 优先级；
- 当前状态；
- 依赖任务；
- 最近更新说明；
- 完成日期。

可参考如下模板：

| 编号 | 任务名称 | 优先级 | 状态 | 依赖 | 最近更新 | 完成日期 |
|---|---|---|---|---|---|---|
| T1 | 建立目录与模块骨架 | P0 | 未开始 | 无 | - | - |
| T2 | 统一模型定义 | P0 | 未开始 | T1 | - | - |

---

## 19. 结论

本项目新增“类似通达信的选股逻辑”时，最关键的不是立即做完整公式语言，而是先搭建一套：

- 数据访问统一；
- 指标能力可复用；
- 表达式可扩展；
- 执行链路清晰；
- 与 GUI 解耦的基础架构。

推荐路线为：

1. 先抽离指标层；
2. 再建立结构化表达式模型；
3. 然后实现批量选股执行器；
4. 最后接入 GUI；
5. 后续再逐步演进到轻量 DSL / 通达信兼容。

该路径既能充分复用现有 `data_loader.py`、`widgets.py`、`main.py`，又能避免把选股能力继续耦合进 GUI 代码，从而保证后续功能演进的可维护性。
